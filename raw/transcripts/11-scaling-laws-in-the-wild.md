---
title: "Lecture 11: Scaling Laws (in the wild)"
lecture: 11
video: https://www.youtube.com/watch?v=vTfEyOyzV9E
source: copy-edited from the YouTube auto-captions
verbatim_original: not committed - regenerate from the video (see below)
material: ../slides/11-scaling-laws-in-the-wild.md
---

# Lecture 11: Scaling Laws (in the wild) — transcript

**This is the edited transcript.** The auto-captions have been repunctuated,
segmented into sentences, stripped of filler ("um," "uh," "you know," "sort of,"
"right?," "okay"), and had mis-heard technical terms and proper names restored. No
content was added, removed, or reordered, and every `[MM:SS]` / `[H:MM:SS]` marker
is preserved in its original position — content that was under a marker is still
under that marker.

The verbatim captions are **not committed to this repo** — they are a complete
reproduction of the lecture and none of that text is this KB's own work. They stay
reproducible on demand: the `cairn-kb` skill's `fetch_transcript.py` run on the
video id in this file's front matter, piped through `transcript_to_md.py`,
reproduces them exactly, so the checks below can be re-run by anyone who wants to
verify this edit.

**Verification.** All three checks pass.

- **Timestamps** — 101 markers, identical sequence, in order.
- **Numbers** — nothing lost. Two digits added, both normalisations to the deck's
  own spelling: "Newton-Schulz five" → "Newton-Schulz5" and "MiniMax zero one" →
  "MiniMax-01". Two further differences cancel in the count and are both
  deliberate: "96 data point per active parameter ratio" → "96-to-1", which is how
  slide 27 prints it, and "I'll take 1 minute" → "one minute", where the captions
  had rendered a spoken word as a digit.
- **Per-paragraph word ratios** — **all 101 paragraphs inside the 0.72–1.10 band**,
  at 86.5% overall retention. That is in line with the same lecturer's lecture 8
  (84.0%) and lecture 9 (84.6%). No content moved across a marker boundary.

Two corrections were made in the parent after the draft came back. The `[Ed:]` note
at [23:05] had *replaced* the words it was commenting on, deleting "Chinchilla 2"
from the lecturer's speech; the spoken words are restored and the note now follows
them. And one dictated formula at [1:06:06] had been set as `H_{L-1}`, the only
LaTeX-like construct anywhere in the file and inconsistent with the same
paragraph's own "square root of n sub L"; it is now spelled out. Transcripts in
this KB stay spelled-out — [the wiki](../../wiki/11-scaling-laws-in-the-wild.md) is
where notation is reconstructed.

Terminology was cross-checked against
[the lecture's slide deck](../slides/11-scaling-laws-in-the-wild.md)
(`lecture_11.pdf`), which is the authority for paper and author names, model names,
and the exponents and constants quoted from MiniCPM, DeepSeek, StepFun and the
other cited work. "Slide N" means PDF page N of `lecture_11.pdf`, per that file's
own numbering note — this deck prints no folios.

**Restorations.** About 25 terms were restored from the deck, the recurring ones
being **muP** (the captions render it "mu p" and "mup" throughout), **WSD**,
**IsoFLOP(s)**, **MoE(s)**, **Kimi K2** (captions: "Kimmy"), **Qwen 2.5 / Qwen 3**
(captions: "Gwen"), **Muon**, **Cautious AdamC**, **Newton-Schulz5**,
**MiniMax-01**, **Cerebras-GPT**, **Step Law** and **StepFun**. Every one of these
appears verbatim in the deck.

Three restorations are *not* in this deck, and each was checked against something
else rather than taken on trust:

- **Hestness** — absent from lecture 11's deck, but appears 10 times in lecture 9's
  deck and 11 times in its transcript. He belongs to the scaling-law prehistory the
  lecturer is recalling from that lecture.
- **Newton-Schulz** — the deck writes it **without** a hyphen, and inconsistently:
  "NewtonSchultz" in its prose and "NewtonSchulz5" in the pasted algorithm (the
  slide file records that inconsistency). The hyphenated form used here is a
  rendering of speech; the trailing "5" matches the algorithm.
- **Marin** — the captions heard "Moraine". It is Percy Liang's open-source
  language-model project, and it is attested inside this KB rather than only from
  outside knowledge: [scaling laws](../../wiki/scaling-laws.md) documents the Marin
  pre-registration example from lecture 1, and slide 41 of this very deck cites
  Will Held's blog, the source the lecturer is describing.

**Four `[Ed:]` notes** mark genuine ambiguity, at [15:19], [23:05], [29:16] and
[40:44]. The one worth knowing about is [23:05]: the lecturer names a paper
"Chinchilla 2" that appears nowhere in the deck, although the content he then
describes matches the deck's Kimi K2 sparsity-scaling slide closely. The name is
left as heard rather than corrected to a guess.

**Six student questions** are marked `*[Question from the floor: …]*`. One further
exchange, at [54:37] ("is SVD fast on GPUs?"), is left unbracketed because the
captions caught only the lecturer's paraphrase and not the question itself — the
same convention lecture 9's transcript uses.

---

**[0:05]** Okay, we are going to continue our scaling journey. Today is a bit of a grab bag of stuff — we're really going to talk about some of the more advanced details of scaling laws and how we scale up models. In doing so, we're going to cover what I feel like are some important details in how you would scale up a language model in practice. So we might want to ask some questions like: is the scaling law stuff that you talked about two lectures ago, does it actually work at real, building-open-source-model scales? It would be nice if we could optimize some of this — there's a lot of learning rate tricks and other things that people do that I think you should know as people who are informed about how to build

**[0:50]** language models. So we'll talk about some of these optimization details. And then I think the last thing I want to talk about is: when we scale up, there are certain things that are going to be a little bit trickier to tune. We know that optimizers and optimizer behavior are going to be pretty scale-sensitive, and because of that, things like initializations, learning rates, batch size — those we need to carefully think about as a function of scale. So we will talk about how to think about those carefully. Okay, so up until now, most of our discussions — what I would call the classical scaling-laws canon — we talked about Kaplan, we talked about Hestness, and then the Chinchilla paper. So we're up to date with, I don't know, 2022 or something.

**[1:36]** Since then, there have been a number of scaling papers published by people who train big models — although I think the number of such papers has decreased in recent years, and most of them have come out of the open source community in China. So today I want to talk about a number of these that I've curated and selected, to give you a sense of what scaling at the frontier — at least the open frontier — looks like: what are the things that these people care about or worry about, and what would you need to do to speed-run your way from Chinchilla all the way to, let's say, Kimi K2, the most recent release that has some scaling details. Okay, so that's going to be the first part of this lecture — we're going to go through a couple of papers and go through the details. The second part is going to be a

**[2:22]** discussion of optimizers and initializations. Remember I spent a non-trivial amount of time, two lectures ago, on the selection of batch sizes — that's going to be an important factor, and it's also going to be important thinking about how we're going to tune things like learning rates. Those are very sensitive, very important to get right. And so it turns out that a lot of effort has been invested into getting those things right, and we will talk about some of the techniques to do so. This may in fact even be useful in your day-to-day lives of training various language models, because they'll give you a sense of how to change your hyperparameters in your experiments as you scale up or down. Okay, so I'm going to spend a non-trivial amount of time talking about two papers in particular. They're not the newest papers.

**[3:07]** They're not necessarily the most — actually the DeepSeek one might be one of the more detailed ones, maybe the most detailed scaling paper in general — but I think this hits a sweet spot. Both of these papers are serious scaling investigations from people who build reasonably high-performance models, and importantly, they take pretty different approaches to some of the key questions that we will ask today. So I think they're a good way to motivate a lot of the rest of the lecture. I'm going to talk about these two: first one's MiniCPM — this was, I think, back in 2024, a high-performance small language model — and DeepSeek, which you're all now familiar with. I no longer have to justify why I'm talking about DeepSeek, which is great. We'll start by talking about MiniCPM, though. I do like this paper, especially when it first came

**[3:53]** out, it felt very fresh and new, very much at the cutting edge. Even in 2026, I feel like a lot of the things that the MiniCPM folks did is very informative to us. I'll talk about this later as I get to the newer papers, but I think it's becoming the case that a lot of this classic scaling stuff — how you set learning rates, how you do Chinchilla — those kinds of things are now taken for granted. Everyone just assumes that you know how to do those things, and so people don't really talk about them as much anymore in the papers. So it's really nice to talk about some of these older papers for that reason. MiniCPM is an effort by a bunch of industry-academic-partnership folks over in the Chinese open source community to come up with a high-performance, smallish

**[4:39]** language model. At the time this came out, it was basically state of the art for the 1-to-2-billion-parameter bracket. Nowadays Gemma and other 2B or 3B models are going to be higher performance, but these are competitive, well-trained models, at least by 2024 standards. So how do you train these models? As I said before, we have to get some of these scaling parameters right, and one of the things I'll talk about today — that I'll emphasize quite a bit — is that initializations are very important. Initializations are important because they allow you to make certain hyperparameters less scale-dependent. So the first technique that I want to talk about from this MiniCPM paper,

**[5:24]** is a special class of initializations called muP. muP — the whole point of this approach is to say, I want to make sure that my optimal learning rate is the same as I scale up and down. I talked about this very briefly two lectures ago, so you should hopefully remember that a little bit. But this is kind of the first real instantiation that we're going to talk about. What does that look like specifically? The MiniCPM paper has a nice list of things to do. They scale the embedding output, they scale the residual connections by the square root of the number of layers, they scale the initialization of all their matrix-shaped tensors by the fan-in/fan-out ratio. They scale the learning rates of tensors — and you should pay attention to this one, because this is exotic

**[6:10]** if you aren't used to per-parameter learning rates. And then they also scale the LM heads. We'll talk about why you do these things, and how you derive this particular set of scalings. Suffice it to say that you're just going to change up how you do your initializations and your learning rates a little bit, in the hopes of stabilizing your learning rate a little bit. And of course we're training a small model here, but we still want a nice scaling strategy — part of the point of the study is not that you want to brute-force, say, a 1.5B model by training hundreds of them. You want to get the hyperparameters exactly right the first time, so you train a bunch of much smaller models and then get a scaling ladder up all the way to your target size, of 5x or so. So there's about a 5x gap between

**[6:56]** the biggest model they train on their ladder and the actual model that they're going to finally release. Their goal is going to be to nail down what most people consider to be the sensitive parameters — optimal batch size, learning rate — and then the token-to-size ratio that we talked about with Chinchilla; they also want to replicate a Chinchilla analysis. Much less important to us now, and probably to you even if you're training, but I'm going to talk about it regardless, because there's one important thing in the way they do that which I think is very informative and useful. Okay, so as I said earlier, the whole point of going through this weird, non-standard initialization called muP is to try to make the optimal learning rate stable. Is it? At least in the MiniCPM case, the answer is yes. In

**[7:43]** fact, it's very, very clean in their experiments. They sweep learning rate across a variety of models, so each line is a model size, and the minimal loss is basically right on 10 to the -2. I guess the minimum is slightly shifted for the smallest model, but even then it's quite close to the minimum. So this is a nice success case of the muP approach. We will talk about several other models and papers that use muP — this is not the only way to do it, but it's one effective way to do it. And if you can get something like this, you've now kind of removed the need to tune the learning rate. But of course, even if you have a nice parameterization that fixes your learning rate to a single optimum, your optimal batch size is still not going to be the same. So the batch size actually varies as

**[8:28]** a function of both dataset size and model size. In their case, they run these model training runs, so each column of points that you see on these plots is a training run — they have a fixed batch size and they increase the number of tokens processed, from bottom to top. And then they ask: okay, we've done many of these training runs, what is the optimal batch size, fitting these quadratic curves onto each of these lines here, this equal-loss contour? What they find is this optimal batch size scaling as a function of the number of tokens processed, and they do this across different model sizes. Maybe you wouldn't be surprised by this, given the nice scalings that we saw in the Kaplan

**[9:14]** paper for critical batch sizes, but the optimal batch size does have a power law structure with respect to the target loss. So we see exactly the Kaplan-style analysis: if your loss is lower, on the left, your batch size should be bigger — critical batch size scaling. So now, given a particular loss target, which you can back out from the scaling law, you can basically set what the optimal, or critical, batch size is that would give you the best tradeoffs. There's a nice, clean trend here — this will let you set batch size very easily and precisely. Okay, so those two, the batch size and the learning rate scaling, are going to come back over and over again. Those are arguably the most important and sensitive parameters to do scaling laws on. But I'll talk

**[10:00]** about one last thing in the MiniCPM paper that I think is important. Remember, to do Chinchilla, we need to train a lot of different models, so we're going to, let's say, do IsoFLOPs — that means I'm going to fix a FLOPs budget and vary my tradeoff between number of tokens and model size. And to do that, I'm going to, let's say, increase the amount of data I train on, so increase the number of sequences. But as I increase the number of sequences, remember that each one of these gets a different learning rate schedule — you're all training on cosine learning rates, and cosine learning rates look different: you need to know the terminal, total budget before you train, in order to have a cosine schedule. And so this leads to a kind of annoying fact, which is that you just need to keep retraining your models. If you want to train an 8 million

**[10:46]** sequence model, you can't restart from the end of a 4 million sequence model — you have to restart from scratch, because your learning rate schedule is just fundamentally different. And this is very annoying, it's like a quadratic cost in some sense, because you have to keep restarting your runs from the start. This seems very wasteful, and I think lots of other people thought so too. And I think one of the nice solutions from the MiniCPM paper is what's now called the warmup-stable-decay learning rate — WSD is the popular term for this. It's basically a big trapezoid: you've got a warmup phase. The warmup phase is usually defined as a constant number of steps, not as a fraction of your training target, so this is independent of your

**[11:31]** training horizon. You've got a fairly long run where you're holding the learning rate constant — that's the stable phase. And then at the end you're going to decay fairly rapidly down to zero — that's the decay phase. Generally the decay phase is something like 10 to 20% of your total training run, so you're going to be stable for the vast majority of your training run and then do a very rapid decay down to usually about 10% of your maximum learning rate at the very end. And that's a very typical warmup-stable-decay learning rate. Okay, and why do we do this? What's the point? Well, we do WSD because now we can restart a run at any point at the end of the stable phase. So if I want to reuse a run and train it for longer, I'll just roll back my training to the last

**[12:17]** stable checkpoint, and then I'll just run the stable learning rate forward and re-decay the model down to zero. So this provides a very easy way for us to essentially run scale-in-data experiments without constantly rerunning your experiments — you do have to re-decay every time, but that's maybe 10% of the total cost. It's a much better situation to be repeatedly decaying than to repeatedly rerun your pre-training runs. And this is a very common trick that's used in many places. I'll show you a performance comparison — there have been many plots of this form. I think the most recent folks — maybe it was Martin Jaggi and others over at ETH — had an

**[13:03]** extensive study of WSD performance, but I think a lot of papers have shown similar things: you have a well-tuned cosine learning curve — this is the orange line over here — and then you've got various WSD curves. WSD curves always look a little funky, because it looks like you're way underperforming until you hit the decay phase, where suddenly you reclaim all of your gains and then match, or even sometimes exceed, the cosine learning rate performance. Anecdotally, I think if you talk to a bunch of people training networks, a lot of people will say cosine is slightly better in many cases, but WSD is basically as good in many other situations. It's a very versatile default learning rate, and it doesn't necessarily suffer from having to rerun things if you want to just continue runs.

**[13:49]** It is very cool, though, to see just how much of an impact learning rate decay has, if you haven't played with that before. This part of decaying the learning rate is just incredibly important to getting the final, annealing parts of your training together. Okay, so that's the comparison there. So now that we're equipped with WSD-style learning rates, Chinchilla-style analyses are actually very easy. What you're going to do is essentially run one long run, and then you'll rewind to the checkpoints and repeatedly decay. That will give you sweeps along the data dimension, and then you'll also do sweeps along the model dimension — you can do IsoFLOPs or any other kind of analysis you'd like. The MiniCPM authors, for whatever reason, choose to try to

**[14:34]** replicate methods one and three, which to me are the least reliable of the Chinchilla methods, but they do get fairly nice-looking scaling curves. If you look at the MiniCPM Chinchilla method one, they see nice lower envelopes that look mostly linear across these, which is good. And then for method three, they get nice, smooth scaling for the most part in the compute-to-non-embedding-parameter tradeoff. So it seems like for the most part they get reasonable Chinchilla-looking fits. One thing I will point out is that they get pretty different exponents for their overall joint fit compared to Chinchilla. They claim in the paper that this means that they

**[15:19]** should be *[Ed: trained with way more tokens of text than Chinchilla recommends — "should be be with way more tech pieces of text than Chinchilla" does not parse in the captions; the surrounding argument (MiniCPM's fitted exponents differing from Chinchilla's) suggests this is the intended sense, but the exact phrase is not recoverable]*. It is highly unclear to me whether this is really true, or whether their Chinchilla fits are a little bit strange relative to the original Chinchilla paper. Okay, I'll stop here for a moment in case someone has questions about MiniCPM. Maybe the main couple of things to take away: one is that the use of muP is a useful trick to try to stabilize how your learning rate changes over scale. And then the second thing to remember is the WSD learning rate — that's just a very common trick, you should all know about this learning rate, that's a basic thing to know. Okay, good. All right, so then I'll move on to DeepSeek. This is the kind of original DeepSeek paper, before they

**[16:05]** started talking about MoEs and all these other very cool things. And I think even at the very first DeepSeek paper, you could tell that these were very serious people, because DeepSeek LLM, even now, is I think one of the more nicely executed scaling analyses in the open world, and I feel like they have great taste in the experiments they run. So even before R1 and so on came out, it was pretty clear that these people were going places. All right, so DeepSeek takes a very different scaling strategy than MiniCPM and several others in trying to optimize their hyperparameters — they're not going to really do muP, they're not going to do these interesting scaling adjustments. Instead, they're going to directly try to

**[16:51]** fit a scaling law to estimate both the optimal batch size and the optimal learning rate. We know that the learning rate and batch size should actually change with scale, but if the way in which they change is predictable, just like a scaling law, then we're good to go — we don't really need to worry about it very much. So what they do is they run extensive grid searches at various scales and try to find the optimum. This is an example of the kind of thing they're doing: they're sweeping learning rates, they're sweeping batch sizes, and they're looking at the terminal loss if they're processing a particular number of tokens. So they fix the compute and vary these two. They'll do this across several different scales and then try to identify what the lowest loss is — in

**[17:37]** this case, the lightest color. They'll put a little star there, and do this across scales. If you do this across sufficiently many scales, you get a plot that looks a little like this: it's saying, okay, I'm going to vary my non-embedding training FLOPs across some range — you can see these gray dots over here — and as I vary this, my optimal batch size will change. Remember, the optimal batch size is derived by looking at these big grids and finding the minimizer. And I'll get something that looks like a line: the optimal batch size should scale with my non-embedding FLOPs. And similarly for my optimal learning rate — I'll get a bunch of gray dots in my non-embedding FLOPs

**[18:25]** range, and then I can fit a line to that to estimate what my optimal learning rate should look like. This is fairly nice. One thing I'll point out is that their full runs are on the star — these are the runs that they chose. At higher FLOPs, they have much higher batch sizes and much lower learning rates; the stars are the things they actually ran for their big runs. These fits look reasonable for the batches — this does look like a line. The one on the learning rate, I'm not quite sure that constitutes the best linear fit I've seen in my life, but I guess it works, it's probably fine — the models do train, the models do get reasonable things.

**[19:11]** I think this is the one drawback of the learning rate approach: it's not really clear that if you try to do this grid-search kind of thing, unless your grid is in the right place, you've got a lot of quantization error, and then you're not getting very good scaling law fits, and you end up with something that looks like an arbitrary linear thing going on. Okay — oh yeah, sorry. *[Question from the floor: For the same number of training FLOPs, you get multiple data points with a large range of optimal learning rates. Why is it that you get a large range of optimal learning rates for different training FLOPs? Is that the question, on the y-axis over here?]* Yeah. So the reason, I think, is because of the changes to the model sizes and things like this.

**[20:01]** And so, you know, 2024 was back in the era where I think every open model builder was replicating the entire scaling law stack. And so DeepSeek also complements the learning rate analysis with basically a Chinchilla replication. To do that, they're also going to do the same thing as MiniCPM — they're going to follow the WSD-style learning rate. They have a weird little variation where they do two decay phases instead of one; I'm not really sure why this is. It hasn't really been a thing that's taken off afterwards, but it is a thing that they do. Regardless, the whole point here is to say WSD is an effective approach, and you can use that to replicate Chinchilla. And DeepSeek has much

**[20:48]** nicer scaling curves than MiniCPM, because of their choice to go with IsoFLOPs, so they get these very nice, clean sweeps across the fixed FLOPs ranges, and then they also get fairly nice and clean Chinchilla-style tradeoffs as before. So this allows them to essentially replicate the Chinchilla results and get a similar kind of data-model scaling law. Really, the point I'm showing with this is just to say: look, the Chinchilla laws have been replicated by other people at large-ish compute ranges, so that analysis is generally sound. Finally — I'm a little bit sad that these kinds of plots are now somewhat rare in open-source release papers, but

**[21:33]** this is really the final punchline of a lot of scaling law work: you train a bunch of small, curated models at smaller compute ranges, draw out the power law, and then in the end you get something pretty close to reality if you scale out enough. So the two stars are the models that they actually trained; the gray dots are their scaling law fits, and they're getting pretty good predictions on their star. It could be better, but it's not bad at all. So they're able to get some pretty accurate scaling law predictions on real, open-source models that they've trained. Okay, I'll actually stop here — if people have questions about the DeepSeek stuff, feel free to ask them. I think my one-line summary here is to say this is the other approach to dealing with the sensitive hyper

**[22:19]** parameters — you can either attempt to stabilize them, or you can attempt to just scaling-law fit them and go with the scaling law. And DeepSeek represents the more fit-the-scaling-law approach to this problem. Good. Okay, so there are other, more recent models that have nice scaling laws they talk about. As I said before, though, I think a lot of this work is now kind of — it's kind of all known to everybody, so no one is really putting a lot of this stuff into big sections in the paper. So if you look at Qwen 2.5, they say something like: okay, well, we need to tune some optimal hyperparameters like batch sizes and learning rates, so we do a bunch of scaling experiments to figure out what the optimum is, and then we

**[23:05]** have a scaling law, we predict the optimum, and then we're able to do that even for MoEs. So this is basically the same thing as the DeepSeek analysis that they're talking about in Qwen 2.5. Same thing with Qwen 3 — by the time it's Qwen 3, they say, all right, we've already figured out what to do in Qwen 2.5, we're just doing exactly the same thing. So this has become part of a very standard recipe that you use in order to nail down the optimal learning rates and batches. Chinchilla 2 *[Ed: no paper by this name appears in the deck. The description that follows — MoE sparsity-versus-loss tradeoffs, settling on sparsity 48 for diminishing returns — matches the deck's Kimi K2 sparsity-scaling-law slide almost exactly, but the name itself could not be verified, so it is left as heard]* is probably the newest paper that I'll talk about of the series. At this point I'm just going to quickly go through several papers, mainly to give you an overview of what people are doing in terms of scaling work in the open-source model releases. So Chinchilla 2 and a few others

**[23:54]** have focused on essentially MoE scaling laws. It's 2026, most people know how to do a Chinchilla scaling law, or even a learning rate scaling law — what's new? Well, a lot of these companies are now switching to MoEs, fully switching to MoEs, and if you're doing that, you really want to know: am I training the right amount of active parameters? So they do things like: okay, we're going to vary the FLOPs and look at the losses, and do so for different levels of sparsity, so we can figure out how much sparsity helps or hurts the losses. And their conclusion, of course, is that if you look at the different sparsity levels, the more sparse your network, the better your validation loss given a FLOP — unsurprising. But now, the fact that they can quantitatively know what the relationship is between

**[24:39]** sparsity and loss means they can make more rational decisions about how much sparsity to put into their model. They look at this and say: okay, what we're going to do is use a sparsity of 48, because at that point we're hitting diminishing returns on our scaling law. So you can see how this kind of analysis is used to justify different architectural decisions across different open source releases. Hunyuan, which is another open source MoE from a few years back now, does a very similar thing — I think every company, once they switch to MoE, has to do this kind of MoE scaling law, to figure out how the number of activated parameters or the amount of sparsity changes training loss, and then they redo a lot of these analyses. So for example, in

**[25:26]** Hunyuan's case they end up with a 96-to-1 data-to-active-parameter ratio. In their case, they're fixing the sparsity level, so that's not a thing they're searching over. LLaMA 3 also has some nice scaling laws — unfortunately, not particularly useful ones. They do IsoFLOP-style scaling and get slightly different, but similar, ratios for tokens to model size. The most interesting thing they do in terms of scaling in LLaMA 3 is the plot on the right here. They have the usual scaling law, which is the left panel — that's saying if I have more compute, then the log-prob on the answer for a benchmark decays consistently. And on the right side they show: okay, well, if you have a better

**[26:14]** log loss, the downstream accuracy also goes up. So they're basically saying: well, we can just fit a sigmoid on these purple points, and that might give us a sense of how good these different models are. You can see there are systematic deviations from the curve, so I don't really know that I'd buy this curve as the one truth that's going on, but you can certainly see that it's consistent with a sigmoidal scaling mapping from loss to accuracy, which I think is very useful, because we've been talking only about log losses, but there is a tight coupling, in some cases, or in many cases, between log losses and downstream accuracy. Okay, and then for one last paper that I want to talk about for

**[26:59]** scaling, MiniMax-01 from last year had this nice set of scaling experiments, where they were doing exactly the kind of thing I said is a useful affordance of scaling, from two lectures ago — architecture variations. They wanted to understand: okay, I have lightning attention, which is some kind of linear attention architecture; I have a softmax version, like full attention; I have a hybrid version that mixes the two. How do these things scale? Is it the case that if I scale up one, it'll get much worse than the other? So you can see they're replicating essentially the Kaplan plots: as I increase FLOPs, how do the learning curves change, and is the number of parameters I need different for different levels of

**[27:45]** compute for these different architectures? And the answer is, for the most part, no — hybrid, lightning, and softmax are all comparable for the most part, and require similar model sizes. This set of plots is used to justify their use of the hybrid architecture in the actual deployed system. So this is exactly the kind of decision-making you would do based on a scaling law. Okay, so that's basically what I wanted to cover for the recent scaling law recipes from the various companies. So the two things, as I said before: for the DeepSeek recipe, really the key thing is the scaling analysis on the batch and learning rate — this is the scaling-based approach. And MiniCPM follows more of a muP-style approach, to try to get invariances. And then

**[28:31]** we see some trends. We see that Kimi and Qwen, and others, and Hunyuan, are doing these MoE scaling experiments as they switch to MoEs. We see some architecture work by MiniMax. And we also see some IsoFLOPs analysis being replicated by several others. And to return to my previous point: I think a lot of companies are now less detailed, because the core scaling machinery — doing Chinchilla, or doing learning rate scaling — is now fairly well understood by everybody, so there's no necessary extra value in showing those experiments in some of these releases. Or at least that's my guess for why we have much fewer of these kinds of experiments in the releases. Okay, I'll take one minute and then pause and let you digest and ask a

**[29:16]** question or two, in case you have questions for this part. *[Question from the floor: Okay, so all these cover the pre-finished stage. So my question is what changes, or what does not change, now that post-training is a kind of standardized process for quality?]* *[Ed: probably "pre-training stage" — the captions read "pre-finished stage," which does not parse, but the contrast with post-training in the next sentence strongly implies this]* Yes — the question was about what changes about this when you think about post-training, broadly. I think that's still a big open question. There's no great way to do fully integrated scaling with post-training accounted for, because post-training can sometimes really change the nature of what pre-training you should do. The

**[30:02]** closest thing I can think of to thinking about post-training is there's been some work on understanding coverage, or diversity notions, in pre-training that would help predict what happens in post-training, but it's still pretty nascent work. I don't think there's any good answer to the question of what we do about synergy with post-training. Cool, okay. So now we're going to talk about the other piece. I think the two remaining parts of this lecture map cleanly onto the two examples: I'm going to talk about one approach, one set of approaches, which is about scaling laws for learning rates, batch sizes, and optimizers — that corresponds to the DeepSeek

**[30:48]** approach to the world. And then I'm going to talk about the muP stuff — that's the MiniCPM approach to the world: can we just reparameterize everything so that we don't have to worry about scale? So we're going to now talk about the DeepSeek approach: how do we scale learning rate properly? There's going to be a hyperparameter part and an optimizer-choice part. I'm going to draw quite a bit from this pretty recent preprint from the StepFun folks. I don't think there's a great, truly reliable, robust study on hyperparameters, especially learning rates, but this is probably getting somewhat close. The StepFun folks train credible

**[31:33]** large models and release them, and they burned a ton of compute grid-searching the space of hyperparameters in their scaling law studies. So I'll go through this and show you some intriguing empirical phenomena about how learning rate, batch size, and so on affect downstream losses. Okay, so lots of people have talked about lots of different ways of scaling learning rate and batch size, roughly as a function of compute. And the StepFun — not survey paper, the hyperparameter-tuning paper — has this very nice table that captures a lot of this. I'll point out a couple of different things; some of this we already know. So the very top row, this OpenAI scaling law, is from Kaplan. If you look at

**[32:19]** the batch size over there, notice it says something like 2e18, capital L to the exponent. That's critical batch size — it's basically saying you should scale your batch size as a function of your terminal loss. Great. DeepSeek we just saw — this is the compute-based scaling laws for both learning rate and batch size. There've been a couple of other folks that have proposed various kinds of scaling, including MiniCPM, which we talked about as well. Another important thing I want to point out is that these formulas for learning rate and batch size don't even have the same inputs — people don't even seem to agree on what variables should affect these. Like, the OpenAI one is terminal loss, whereas the Step Law folks,

**[33:04]** they claim that the right batch size scaling is a function of the data. So there's a lot of disagreement about the nature of scaling for hyperparameters, and I think one thing I'll say as I go through this is not to take even this last row as gospel — a lot of these things are pretty brittle in many ways, but the experiments and the phenomena are things you can learn well from. So I'll do that. The design of this paper is very similar to the DeepSeek one — they're just going to train a ton of models to grid out both the learning rate space and the batch size space, across a range of model sizes and dataset sizes. And by doing so, they hope to essentially saturate the

**[33:49]** hyperparameter space for a set of small models and then nail down what the optimum is. And much like in the DeepSeek design, after they grid up the space — in this case they have a contour plot, but the underlying objects are just that they run a bunch of models and aim to minimize the losses. The contour plots are quite nice, I feel like they give a good visualization. They ran a pretty high-resolution grid, and thanks to that, one of the things we can see is that they can plot out a fairly high-resolution contour of what the hyperparameter space looks like. In this case, this is a slice at 1B parameters and 100B tokens, I believe, and you can see on the right side that surface is

**[34:35]** the hyperparameter landscape, as a function of the batch size, on one axis, and learning rate, the other axis — how does the loss change if you fix the total amount of data and the model size? Now, we see that each slice — if you fix the batch size, or fix the learning rate — is nice and convex, almost smooth. And this is quite nice, because it means you can search for the optimal hyperparameter fairly nicely, and be confident that the minimum isn't too far off. If this were very jagged, you might have some very serious doubts about whether this kind of program of gridding the space is even viable. This should also maybe give you some nice intuition about the smoothness of the hyperparameter space in general.

**[35:20]** And I think there's one really interesting general scaling trend, and looking at other related papers on this topic, I don't think I've seen any contradicting evidence to this observation. The one thing that seems very interesting, and one of the key takeaways of this work, is that if you're looking at the optimal batch size, the only thing it seems to depend on is the total amount of data you're training on. So this right-hand plot is the one to look at: the x-axis here is the amount of data you're training on, the different colored dots are different models, and notice how the different colored dots seem to follow roughly the same trend line — fairly predictable. This is in log-log scale, so you should expect some sort of power law as a function of D. On the left

**[36:08]** panel, we have the dependence of learning rate, and we see this is very different behavior. Bigger models — so if you go from blue all the way to this pink color — bigger models have smaller optimal learning rate, and larger amounts of data have higher optimal learning rate, which is kind of counterintuitive, actually. So this might be a little fragile — there are other papers that argue the dependence on D should be reversed — but the trends here, at least in this experiment, are quite clear. Okay, and one final thing I want to point out is that learning rates are actually pretty robust, and I think this is backed up by a lot of experience and experiments

**[36:55]** that people have. I think people have a sense of — actually, as a poll — do people know what a good hyperparameter is? If you were told to train a language model tomorrow, would it be 1e-3, 1e-4? I feel like we roughly have a sense that this range of hyperparameters for learning rates is generally a good starting point, and that is generally true. So the scaling laws that they got actually transfer over to, for example, MoEs, to a certain extent. The yellow stars, which might be a little harder to see, are the predictions from their scaling laws; the red X's are the optima derived directly on the MoEs. So when you switch to an MoE, you don't end up with a very different scaling law, as long as you're

**[37:40]** roughly controlling for active parameters. The one thing that does seem a little more potentially dicey is that as you shift the training data, the optimal learning rate and the optimal batch size do shift a little bit, which means all of these phenomena are likely contingent — the specific numbers in these scaling laws and so on are potentially contingent on the data you train on, more so than others. So I want to flash this back up before I move on. You end up with some simple scaling law at the end, which says: okay, batch size should maybe be roughly the square root of the number of data times some constant; your learning rate should scale upwards with batch size, or upwards with data, but downwards with the model

**[38:26]** size. And that's kind of a surprising effect, but not crazy, because these experiments, in many cases, are assuming Chinchilla-like scaling, in which case those two would cancel, and as you increase compute, your learning rate should go down. Hopefully that Chinchilla comment was clear — what I mean is, when you're scaling by Chinchilla, N and D are both driven by compute, and as compute goes up, you expect your learning rate to go down. And so you end up with something like the DeepSeek law, where the learning rate decreases with compute and the batch size increases with compute, albeit with different exponents than the DeepSeek law. Cool, okay. So in some sense, this is the newest and most large-scale version of the DeepSeek analysis, and this is

**[39:13]** one way that, if you were doing the scaling-law-style approach, you would approach this problem of fitting a learning rate. Okay, so that's the hyperparameter story — if someone has questions, feel free to stop me. And then right after, I'm going to move on to talking about optimizers. I think this is one thing that I didn't have time to talk about in the architectures lecture — that was already a pretty packed lecture, but I think it would be a disservice to you if I don't talk about optimizers, and optimizers are scale-dependent, so this is a good place to put them and talk about some of the latest and greatest developments in optimizers and their relationship with scale. Oh, yeah.

*[Question from the floor: Um, so I guess

**[39:59]** I'm trying to figure out how to interpret this — does that mean we should just take the scaling laws that we have and then apply them, rather than running our own grid search?]* Yeah, I mean, I think it depends on what regime you're in. The question — oh sorry, I have to repeat it for the online folks — is: should we just take the scaling law and run with it, or should we do our own grid search? It does kind of depend on the amount of compute budget that you have, and how much you need to nail it. I do think, for example, maybe the Step Law, if you're in the right compute regime, close to where they were running the grid, these are probably really good defaults — I would bet on these over something I pull out of my hat. That said, if you were doing your own big pre-training run, you are probably going to have minor differences,

**[40:44]** Someone might say: I really believe we should have a big weight decay, my architecture has a big weight decay — but we don't necessarily know that those things are going to scale the same. So you might want to actually redo a lot of this. And this is why I think a lot of the reports I showed you were redoing Chinchilla, redoing all these things — because there are enough minor differences that people want to check and make sure they're roughly first-order correct with these results. I think this will come up again in the next, optimizer part, and *[Ed: "I'll say email this with earlier" does not parse; possibly "I'll echo this from earlier," since the sentence that follows returns to a point made earlier in the lecture, but the exact wording is not recoverable]* — which is that scaling laws have this very scientific feel to them, like: oh yes, fit these lines and extrapolate them. But ultimately, a big part of scaling laws is still vibes — it's like, do I really believe that the experimental setting of the Step Law survey

**[41:30]** is similar enough to mine that they will transfer? We can't possibly know — there are always tiny differences. Good. Okay, so now I get to talk about optimizers, and I think there've been some really exciting developments in LLM optimizers, and maybe even deep learning optimizers in general, over the past year or two. So this is an exciting time to talk about them. The story I want to start with — one that should maybe motivate this subsection — is this comparison over here. Or maybe we'll start on this left panel: this is from the NanoGPT speedrun, which inspired our assignment one. There you have a really teeny-tiny model that you're training for a fairly short amount of time, and you want to

**[42:16]** get to — I forget — 3-point-something loss, as quickly as possible. Many people have been hill-climbing on this benchmark, and I think one of the more remarkable things that's happened on this benchmark is that an optimizer called Muon, in purple over here, came in and got really significant gains over Adam, which is blue — it's right underneath the green line over there. So you can just look at the green and the purple and that gap, and say: wow, lots of big gains from just changing the optimizer to this Muon thing. And it's not even slower than Adam — well, not much slower than that. That's great. But then there's a question of: well, is this thing actually good at scale? And then you go and run some scaling studies, and maybe they're not quite as good. This is a really tricky and

**[43:02]** important part of how we do research. We have a small-scale experiment, we have something that's scale-dependent for sure, like an optimizer, and then we see big gains at small scale — what do we do about the large scale? And more than providing a solid, concrete prescription, I want to almost provide a story here, so you can get a sense of what we can do and what things we can't necessarily know. Okay, actually, before I move on to the next slide — this kind of thing happens all the time, where you come up with a great thing at small scale and it just doesn't pan out at large scale. So what's the difference? I'm going to spend a few slides talking about Kaiyue, and Tengyu, and Percy, and David's paper, on basically doing a large-scale set of experiments on

**[43:49]** comparing optimizers, trying to do that comparison better. There's a lot to learn about scaling and other things from that, but also just standard deep learning stuff. There are really basic things, like the hyperparameters for different models are different — so if you're scaling things up, maybe they even have different scaling exponents. So you have to tune Adam really well: if you tune it badly, you get this darker brown line, and you're like, wow, this thing is much worse than all these other algorithms I have over here on the left. But then you change the learning rate a little bit, and you're like, wow, actually all my gains went away. Similarly, on the right, you can have cases where maybe you've tuned your learning rate well, but now you've got other stuff, like weight decay, that isn't optimally tuned. And so if you use the same weight decay, you're certainly going to get a much worse

**[44:35]** result for this green line — if you pick this small weight decay, you'll get a much worse result for this green line than if you pick the optimal weight decay. So there are all sorts of pretty tricky things going on — technically not a scaling thing, but a very important, general empirical machine learning thing I want to point out. But maybe more relevant to the discussion in this class right now is the scale-dependence of these things, and I think this is a good case study and example of how to really think about algorithm development as a function of scale. Because to me, there are two variables that are always important when thinking about scale, and this paper exposes, to most extent, both of them. One axis is compute — and if you're fixing the compute, or

**[45:22]** sorry, if you're fixing the model-size-to-data ratio, so the two are coupled together, then this is essentially a compute axis on the x-axis here. So you always want, whenever you're doing a study, to essentially scale up the compute and look at the trends as a function of scale. And we see right here that if we look at the relative speedup between Adam, which is at 1.0 by definition, and all these other alternatives, Muon is really good at small scale, and that gain diminishes as we increase scale. That's not really necessarily the picture you want to see. There's another axis you should always be vigilant of if you're doing algorithm development, and that's the Chinchilla ratio. The Chinchilla ratio can be thought of

**[46:08]** as the ratio between how much data you have and the amount of model you have. And this is a really, really important parameter, because some algorithms work well in the overparameterized case, where you have a way bigger model than you have data — maybe your algorithm adds some implicit regularization, or maybe it's data-inefficient but works well because of compute resources. So some algorithms might work well at the small-Chinchilla-ratio case; in contrast, other algorithms might work well in the large-Chinchilla-ratio case, where you have lots of data per unit parameter, because you can pack knowledge more efficiently into the parameters. So this is a big, big confounder. Even papers that I think have pretty good hygiene in doing scaling experiments on the model-size axis often

**[46:56]** neglect to, or just don't have the compute to, study the Chinchilla token-to-parameter ratio, and this turns out to be surprisingly important a lot of the time. In this case, it's not — you see that the optimizer gains are pretty consistent, the ratios between all the lines are the same at different Chinchilla ratios. But this is not always the case; you always want to be vigilant about both of these scaling factors, that's quite important. And then I'll also point out that if you have a new algorithm or a new setting, establishing scaling is quite non-trivial. I've taken this from one of Will Held's posts — he was working with Marin, which is Percy's open-source language model training project. And there, they

**[47:42]** were basically trying to prove out a particular set of hyperparameters — this isn't even a new algorithm. This is Cautious AdamC, which is an Adam variant, with square-root batch-size scaling of learning rates and a bunch of other standard-looking things, and they're doing essentially the same Chinchilla analysis you've seen all the other papers do. But because this isn't necessarily a paper — this is a bunch of people doing experiments — you don't just publish the successful one; they have the niceness of publishing their failed runs. And the example here is that they're doing basically standard Chinchilla stuff and it's looking great. If you go up to this dashed vertical line, at 10 to the 20-something, this is a really beautiful-looking scaling law up to here. And then, as you scale up, it's like: well, it's a little bit worse, it's a lot worse, and then — wow — now that run is

**[48:29]** not so good anymore — it's totally blown up on you. And the point here is that even good-looking scaling trends, for many orders of magnitude, can suddenly bite you: it can deviate from linear and then blow up almost immediately. In their case, the fix is just picking more careful muP-style parameterizations — optimizer changes, they switch to a different kind of optimizer — and they get nicer scaling across broader orders of magnitude. But the point here is to say: when I go back and say, okay, well, you should all do scaling experiments, you should scale all your learning rates and so on — that's not easy. You end up, a fairly non-trivial fraction of the time, with plots that look like this, and it's all kind of horrible and you're not quite sure what has gone

**[49:15]** wrong. Good. Okay, and then the last thing I want to end with is talking a little bit about Muon, and closing up the Muon-nuance story. I feel like I have to talk about Muon in a bit more detail than I already have, just because it's beginning to be integrated into large-scale model training runs — that's kind of the inclusion criterion for a lot of new research topics in this class: has it been in a big training run? And Muon now checks that mark. It's also just kind of an interesting algorithm, so I'll mention it and talk about it for a few minutes and then move on. So in gradient descent, we usually have a fairly simple thing that's happening.

**[50:01]** Ignore line four for the moment — sorry, line five for the moment — and then you get roughly a standard momentum-based optimizer: I take a gradient, I add it to B of T, this is my momentum step, I continue my momentum from before with parameter mu, and then I update my parameters based on this momentum gradient. Very standard momentum-gradient stuff. But then I think the really interesting, and to me at least a bit of an out-of-the-box, idea is to say: well, gradients have this property that they treat all the parameters as the same. But we know that not all the parameters in a language model, or even a deep neural network, are the same. Some parameters are vector parameters — they belong to the RMSNorm. Some parameters are matrix parameters,

**[50:47]** like for attention or for MLPs — and matrix parameters are probably different from vector parameters. So that's kind of the starting point for this idea, and I think a really interesting one. The one thing you could do about matrix parameters is start to look at their spectra — they're matrices, matrices have eigenvalues and singular values, so you can do things to the singular values. So what you do here, in Muon, is this fairly simple idea: you compute a gradient, you take a momentum step, and now, instead of directly applying my update B of T to my parameters in line six, what I'm going to do is this Newton-Schulz thing — Newton-Schulz5, to be specific. And what that does is orthogonalize the B of T matrix. So I've

**[51:34]** written the operation down there at the bottom: let's say I take B of T, write it as a singular value decomposition, U S V transpose, and then I make all of my singular values one. So if my singular values are really big, I shrink them back to unit; if they're really small, I expand them back up to unit. And then I take U V transpose, and that's my new update, which I'm going to apply. If you want intuition for this — and you already have intuition for something like Adagrad, or Adam even — in Adagrad or Adam, you divide by roughly the size of the gradient, so every coordinate is roughly going to be the same size. In Muon, you're operating in the spectral norm instead, so every direction is going to be unit size. And notice that this only makes sense for a

**[52:20]** matrix — I can only define this notion of a singular value decomposition for a matrix-valued B of T. So the interesting thing about Muon is that it's a matrix optimizer: you apply it to these matrix-valued parameters, and for the vector-valued ones, you might still use something like AdamW, because those don't really have any interaction with the Newton-Schulz term. Also, Newton-Schulz is an approximation to that operation that only uses matrix multiplies, so it's systems-efficient and not exactly the orthogonalization. But okay, why have I spent all this time talking about Muon? Well, first of all, it's very powerful for things like the NanoGPT speedrun — it was discovered for this set of things. So you might think that it works well.

**[53:06]** As I said on the previous slide, there have been some scaling studies that have argued that the gain for Muon decreases as scale goes up. But I want to close off this story: I think when this initially came out I was like, wow, Muon's kind of interesting, I wonder if anyone will scale it up. And then Kaiyue and others scaled up Muon and said: well, maybe it doesn't work super well. And I thought the story was kind of closed there — I was like, well, maybe this is done, no one will spend the big compute to scale this up to full pre-training size. And then Kimi K2 comes out, and that whole model is trained with Muon — fully with Muon, with a few bells and whistles to prevent Muon from blowing up on you; they found a whole bunch of instability issues. But Kimi K2 is an outstandingly good

**[53:52]** model — it's a very good model, and the training curves look reasonable. So really what this suggests is: well, Muon worked at scale. Is it better than Adam? Kimi K2 certainly doesn't have any ablation, so we don't know at that scale, but it is certainly a valid and workable solution. So what's the point of this story? It's to say it's really hard to know whether something works at scale — it's very, very hard. But thus far, a lot of small-scale experiments, and transferring them up to large scale, has been the way by which we do science. So you still want to respect things like the NanoGPT speedrun — we learn quite a bit from that and get good ideas, and then eventually they make their way to the big models. We'll see if people continue to do ablations at big scale and really nail down: is

**[54:37]** Muon better than AdamW at scale, and so on — those are interesting questions to which we don't yet have the answers. Okay, I'll pause here for a moment; this is the end of my optimizer section. I want to make sure everyone's good, especially since I was talking about Muon, and that's a whole new can of worms I haven't talked about much before. Yeah, okay, right — yeah, the question was: is SVD fast on GPUs? SVD is not fast on GPUs, but they're not doing SVD — what they're doing is Newton-Schulz. Newton-Schulz is a matrix-multiply-only, finite-iteration algorithm that approximates the process of orthogonalizing a matrix. So there are several clever pieces here: one of them is the idea of treating it as a

**[55:24]** matrix on its own, and then there's this orthogonalization idea, and then there's a systems piece of doing it with Newton-Schulz. So those are all important, interesting pieces. Yeah, back there.

*[Question from the floor: What are the hyperparameters for Muon here? Adam, for the AdamW ones? Do they have the hyperparameters shared?]* No, they're different. There are several people who've been working on this muP-style line of work, and one of them, I think, Jeremy Bernstein — I think he's been saying that actually every layer should have its own learning rate. I think the ultimate limit of this is — or even, I'm sorry, every layer should have its own optimizer. The ultimate limit of this is to say every parameter in a transformer is different, so why not have a different optimizer for each one of them?

*[Question from the floor: And so you would have to do the hyper

**[56:10]** parameters for each one of them as well.]* Which I do not want to do, but that's maybe the future of optimization. Yes.

*[Question from the floor: What's the limit of the hyper— like, they only consider learning rate and batch size, they just consider these two things, and then you pick the best out of those and use it to determine other things. But do these things interact with each other?]* Yeah — or maybe the version of the question that I interpreted was: a lot of these people are just tuning learning rate and batch size, and do they do this on a grid, but presumably these things interact with a whole bunch of other hyperparameters — aren't you worried about that? And I think, in general, yes, you are worried about hyperparameter interactions, but realistically they grow exponentially. This is not a thing where you can grid out the whole

**[56:55]** space. So I think the thing people often do is grid the stuff that's most concerning and most sensitive, and learning rates are very important — I think if you ever tune stuff, learning rates are the most important thing to tune, so they very aggressively go after that. And then stuff like weight decay, they might do a univariate sweep afterwards, to be locally optimal with respect to those parameters. Good, okay, all right. So the final part of this lecture: I want to talk about muP. muP is a bit of a mysterious object — even with many papers written about it, and in fact many implementations, I don't think all of them even agree on what the underlying math is, or what the underlying implementation should be.

**[57:41]** But there's a core set of ideas that are shared, and it does seem generally effective. So I'll talk about my interpretation of the whole program, let's call it. The goal, hopefully, is very clear, and I think this picture is the goal: if we increase the size of our model — in this case, just width — our optimal learning rate normally shifts, but ideally we want the optimal learning rate to be the same no matter what. That is the game we'd like to play, and the knobs I'm willing to tune to do this are: I'm willing to mess with the initializations on a per-layer basis, I'm willing to mess with the learning rates on a per-parameter basis as well, and then I'm sometimes willing to scale up or down things like residual connections, based on the size of my model.

**[58:27]** Okay, so that's the game I'd like to play. And how well does it work in practice? There have been several papers that have studied this fairly extensively. Cerebras — Cerebras, which is really a chip company — also has a language model training arm, run by Hestness, of scaling-law-paper fame. And they actually, I feel like they do very interesting things, because they're now writing lots of papers about muP scaling of language models. They have Cerebras-GPT, from a couple of years ago, where they were training the 0.1-to-13B models using the usual Chinchilla recipe, but also including a muP variant, which they used to try to tune the hyperparameters more effectively. And I think one of the nice

**[59:13]** things about this is that they find, if they use the muP parametrization, their scaling law fits are generally more stable. Their projected muP trends are almost right on the money for their actual models, whereas their non-muP models fluctuated much more wildly in terms of the loss predictions they had. So their claim was: okay, muP is maybe a really good idea, and this is validated at scale. You saw MiniCPM and others — many people have trained models with muP variants. So I could also talk a lot about training, or examples of models trained with muP, but I think maybe the more interesting thing for me to do right now is actually to talk about the conceptual foundations, and the math, sort

**[59:59]** of for muP. And if you're interested in this, and want to read a somewhat accessible paper, I'd recommend this one — this is the paper that, for me, was the clearest. There are several papers: Greg Yang, who kind of started this research program, has a series of papers all called Tensor Programs — I find them somewhat inscrutable. Jeremy Bernstein, who was also working on the muP stuff, has written this nice — not a recap paper, more like a review paper — putting it into a different framework, which I think is very accessible. And there are several others, by physicists, that are also interesting in terms of explaining what muP is; the Cerebras people actually do a bit of that as well. Okay, but anyway, returning to this paper, the core idea of muP is based off of two

**[1:00:45]** kind of assertions, and this is a very physicist way of thinking about this. So if I make my network big — let's say I'm only changing the width for now — I'm making the width of my network big, and I'm going to assert two invariants that should hold as I do the scaling limit. First, the activations at initialization: if I initialize my model and put in some random data, the activations should remain roughly the same size as a function of the network. If they blow up with the network size, or shrink to zero with the network size, I've chosen the wrong parametrization — and that kind of makes sense, that my activations should be roughly the same scale regardless of the size of my network. Now, after one gradient step, the change in my activations should be O of 1. So this is what people call

**[1:01:30]** feature learning. Feature learning means if I take a gradient step, things in my network change a significant amount, and that amount should be a fixed function of the width of the network. In case some of you are theorists, you might be familiar with neural tangent kernels — those would have a very different behavior. That's the kind of setting where the change in activations actually vanishes as a function of the network width, and you don't want that, because you do want the network to change and learn features, even in the limit of your big neural networks. Okay, so these two are the invariants; they kind of make sense — they're basically saying activations, and the change in the activations, should remain roughly similar as a function of the width of the network. And note that I'm going to sometimes go back and

**[1:02:15]** forth between individual activations — which is this — and then the norm: if each activation is roughly O of 1, the norm of the whole activation vector should be the square root of the number of hidden units in that layer. Okay, so that's the main thing. Now, the thing about muP is it's kind of interesting, because it starts with this very simple and palatable set of assumptions — you'll all probably agree that this is a reasonable thing to do — and then we're going to make a number of additional, strong assumptions. But in the end, we're going to get a very simple set of scaling rules that drop out of this. So remember, what we want to do is make sure the activations at init are roughly unit. So what do we do? Well, if we have a simple, deep linear network, and we initialize all of

**[1:03:02]** our weights as a sigma-squared-scaled Gaussian, then what's the spectral norm of this matrix going to be? You can write that down — I'm not going to ask you to verify it, but that's standard matrix-concentration arguments for the operator norm of a random Gaussian matrix. And now note that in certain regimes, where the fan-out is not much bigger than the fan-in, you get that the output of the layer is similar to the product of the input of the layer times the operator norm. This is always an upper bound, but it's approximate in certain high-dimensional regimes. Now, if this is true, what can we do? Well, we can solve for sigma to try to keep our activations the same across all of the layers and across all the

**[1:03:47]** width. So imagine that at layer L minus 1 I have activation size square root of n sub L minus 1. And remember, this is consistent with the notation that, at least for this whole vector, I should have square root of n sub L. So this is our base case for the induction, and then our inductive case says: well, remember I have this operator norm at layer L, which is a function of the fan-in and the fan-out, and this is going to equal this if I pick a particular sigma. So let's say this is the ansatz — I'm going to assume that this is the answer, and then we'll see what happens in the induction. If I pick this as the hypothesis and plug it into sigma, notice that I get the ratio of the operator norm is this — the ratio of the fan-out over the — sorry — square root of the fan-out

**[1:04:33]** over the square root of the fan-in. And then I plug this back in, and I get that the norm of the output is going to equal the square root of the number of units in my layer, plus some lower-order terms I'm not going to bother to keep track of. So, where did this sigma come from? I mean, this is drawing it out of a hat, but we can verify that if I plug this noise initialization into my formula, then essentially at every layer, the size of my activations is roughly equal to the square root of the number of hidden units at that layer. There are some assumptions made here, like this approximate-equals, but this is mostly a legitimate set of assumptions. So condition A1 is pretty simple; condition A2 is a little hairier,

**[1:05:21]** to be honest. So really, we need to deal with the updates now, and I'm only going to consider the case of a single example in a batch, for SGD, for a linear neural network — a deep linear network. So what does this look like? Well, the change in my weights for a particular layer L — if you think about how this behaves, it's going to be a rank-one update between my gradient for the loss and the activations. This is true just because of the behavior of the backward pass: the forward is a rank-one multiplication, the backward is going to be a rank-one outer product. So now this is the change in the weights, and what's the change in the activations at that layer?

**[1:06:06]** Well, the change in the activations — I take H equals W times H sub L minus one, and expand this out into this formula. This is going to be exactly the change in the activation at layer L, and it has two terms: one is the direct term, from changes in the activation at L minus one, and the cross term, in some sense, of what happens because my weights have changed, not just because the activations at the previous layer have changed. So there are two terms to this update — well, this is the change in the activation at layer L. Now what I'm going to do is look through each of these three terms: I want each of these three terms to have the same order of magnitude, and I want that order of magnitude to be square root of n sub L. Why is that? Remember, if I go back two slides, the condition that

**[1:06:53]** I want for A2 is that, after one gradient step, the change in activation should be O of one, or Omega of one. So if I want that to be true, then the norm at that layer should be square root of n sub L. And to do that, what I want is for each of these three terms to have that order of magnitude, and if that's true, and there's no cancellations, then this is going to be the right order of magnitude. You can see the sketchy pieces adding up — we're assuming things like no cancellations and so on — but this is giving us some interesting ways of thinking about the magnitude of each of these objects. So at this point, the right way to think about this is not: okay, Tatsu is sitting here doing rigorous mathematics. What I'm doing here is basically keeping track of the orders of magnitude of all the terms, and saying that

**[1:07:39]** given our assumptions, how big should these roughly be. It's kind of physicist math — no offense to physicists, this was developed by physicists in this way. Okay, so now — oh, sorry, let me go back one slide just to make this clear. So the first term, this comes by almost assumption — it's the inductive assumption, we assume that Delta H at L minus 1 has the right order of magnitude, so this comes for free. These other two terms are trickier: we have terms that multiply the change in my weights, in operator norm, times square root of n sub L minus 1. And because these two are the orders of magnitude of both of these terms, we know how big this Delta W_L needs to be — we can solve for this, because we know the left-hand side needs to be square root n

**[1:08:25]** sub L, and then we have this term, so we solve for this operator norm, and that's exactly this quantity. So this is going to be the goal for the next slide: I want to figure out what kinds of learning rates are going to give me an update that's roughly scaled as the square root of the fan-out over the square root of the fan-in. That's the goal — as I said, after I pick a learning rate, this should roughly be true. So how can we do that? Well, we're going to make yet another assumption, and this is probably the least palatable of the assumptions, in my opinion: we're going to assume that the loss update is going to be scaled by unit. In other words, we're going to assume that our model is actually making some appreciable, non-trivial progress as a function of scale, and that that's roughly similar.

**[1:09:11]** Not quite sure that's palatable to me, but that's yet another assumption, so we throw that down. But the point of this assumption is that, if it's true, the rest of the math works out quite cleanly, because the change in the loss is the product of my gradient and how much my weights change — this is a Taylor approximation. And because this is rank one, I can rewrite this as a Frobenius norm, and as an operator norm. So this right-hand side now lets us get what we want: this is O of one, this term we want to be square root of n sub L over square root of n sub L minus one — that's this term on the right here — and then finally I have this gradient-of-L term, and this is going to be square root of n sub L minus one over square root of n sub L. So now what eta do I need? Well, the eta I need is going to be solving for this quantity, which

**[1:09:58]** is the ratio of these two, which is n sub L over n sub L minus one. So we made this one assumption, that the left-hand side of this is O of one, and then we solved for all the quantities, and ended up with a fan-out-over-fan-in ratio for the learning rate of our layer. One side note I'll make is that for Adam, because of some changes in this, you get a one-over-n-sub-L-minus-one scaling, which I'll mention here. Okay, so I've walked through a bunch of hopefully intuitive-looking scaling arguments for various components, but in the end, maybe you're interested in: what does this mean, what does this do for me? What this means is that we're going to change how we set our initializations and learning rates a little bit.

**[1:10:44]** So our initialization is going to look like — so for muP, the blue box is what we do. Our initialization is going to be one over the square root of n sub L minus one, times a component that is one over the square root of fan-out over fan-in. So if this ratio is one, this is equivalent to the usual initialization — that's the standard parameterization — but if this ratio is smaller than one, you get something different, something closer to the square root of fan-out over fan-in. The learning rate, normally you'd set to be a constant as a function of your model size, but you'd want to set this to the ratio of fan-out and fan-in. And maybe more interestingly, for Adam this whole thing is a bit different — I'm not even going to touch the derivation for that — where you drop this one-over-n-sub-L term and

**[1:11:29]** you get one over the fan-in as the layer-specific learning rate. So basically, layers with big fan-ins get smaller learning rates, for Adam — it allows you to have these layer-adaptive learning rates, which is quite interesting. Okay, so that was the derivation of muP. I went through a lot of details, and I want to talk about what the high-level point of that was. The high-level point is that muP, as an argument, is very interesting — not only is there actually useful stuff, the way you get to the algorithm is itself interesting, because it's a very different kind of math, where you're saying: okay, I'm going to take a scaling limit of my network, then assert some invariants about my network, then make additional assumptions, and then I'm

**[1:12:15]** going to try to figure out what the constraints on my hyperparameters are. And the constraints essentially give you certain kinds of scaling limits on your hyperparameters as well. So this is a general principle for coming up with different kinds of algorithms or hyperparameter scalings — I think probably very different from what you're used to in CS or ML training. Okay, that was the muP derivation. I want to close out the muP part with just a few empirical things. There's an interesting paper by an independent researcher, basically stress-testing muP in lots of different ways. And muP can be thought of as a procedure for tuning hyperparameters. So, depending on how you

**[1:13:01]** do things, Adam is going to have a certain learning rate as a function of different components of the network — you have an embedding parameter, attention parameters, input/output MLPs, or softmax linears. All of those are going to have different kinds of scalings under muP, as well as under non-muP scaling rules. And the interesting thing is that this particular paper replicated a bunch of the muP results for language models trained at different scales. And maybe unsurprisingly, he found almost the headline result from MiniCPM and others: that if you get muP right, and you're only scaling in this case in a very controlled way, you get exactly the learning-rate-optimality invariance that baseline muP, or just looking at

**[1:13:48]** a variant of muP that tracks projection biases for attention — both of those transfer very nicely. But then you might ask: basically, there are a lot of things in the real world that muP does not handle. Technically, SwiGLU does not fit into muP; neither does initialization variation, or RMSNorm. Or if you really mess with your optimizer, that will mess it up too. So you might ask: which of these things that aren't allowed by the theory actually break muP? And actually, for the most part, these mostly work fine with muP. But one of the things that doesn't work is if you learn the gain term in RMSNorm — this turns out to break muP. Although these can

**[1:14:33]** be removed in many cases without necessarily hurting you, so maybe it's not such a bad idea to drop them. But this does seem to break muP. It also seems to break if you use more exotic optimizers like Lion, which basically rely on the sign of the gradient — this sign-gradient idea is spiritually similar to Muon and other ideas. So there might be other interesting things that break muP as well. Finally, the thing that seems most concerning is that if you do large, decoupled weight decay, you end up with significant muP failure — so this is maybe the one stress test that it does fail. But to close out this final section: you might, in the end, ask, is muP useful? And if you really want to stabilize your learning rate, it is

**[1:15:19]** actually pretty useful. There are a lot of experiments of this form, where if you increase the width, your optimal learning rate shifts very predictably — but very greatly — as a function of scale, whereas with muP it doesn't. So it's one of the many tools in the toolkit to try to control hyperparameter drift as a function of scale. I think it's an interesting, open area of research — it's not really done and dusted, like this is the one right thing to do, but there's a lot of promise in the muP-style initialization program. There's also a lot of promise in just fitting scaling laws as well. Okay, so the final thing I want to close with: scaling in the wild is very tricky. As I mentioned in one of my replies, I think the initial presentation of scaling laws makes it sound like a science — like, yes, draw this line, do

**[1:16:06]** these procedures and you'll know for sure what will happen at scale. But I think in reality it's a lot messier and a lot more unknown than that. There are lots of things that people do use scaling laws for in practice — picking architectures, picking optimizers, picking hyperparameters — all of these are things people do with scaling laws. But there is really an art to it, in the sense that you don't really know if it's truly going to extrapolate forever; you want to do reasonable things to maximize the chances of success. So there are lots of solutions people have: you can use muP, you can search for the optimal learning rate, and all of these other kinds of things, and these are ways of controlling hyperparameter drift, but there's no silver bullet yet. Maybe next year there'll be a module that's like, we solved it — but not quite

**[1:16:51]** yet. Great, thanks a lot. I can take questions afterwards.
