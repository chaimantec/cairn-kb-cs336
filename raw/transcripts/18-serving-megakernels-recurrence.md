---
title: Guest Lecture (Dan Fu): Serving, Megakernels, and Looped Recurrence
lecture: 18
video: https://www.youtube.com/watch?v=9EEm4iMAF5s
source: copy-edited from the auto-generated YouTube captions
---

# Guest Lecture (Dan Fu): Serving, Megakernels, and Looped Recurrence — transcript

This is a copy-edited version of the auto-generated YouTube captions for this
guest lecture: punctuation, capitalization and sentence boundaries have been
added, filler ("um," "uh," filler "you know," filler "kind of") and false
starts / doubled words have been removed, and student questions have been
marked. No substantive content has been added, removed or reordered, and all
93 `**[MM:SS]**` timestamp markers are preserved in their original positions.

**No course material exists for this guest lecture.** Unlike the other
lectures in this course, there is no slide deck, handout, or any other
material posted anywhere for this session — this was checked directly against
the course schedule, the lectures repo, both sibling-year archives, and the
site repo, and nothing turned up. The speaker himself says on the recording
that his slides were entirely AI-generated (by Nano Banana Pro) and that at
least one of them hallucinated its content. As a result, every restoration
below rests solely on the transcript's own internal evidence — repeated
correct usage elsewhere in the same talk, or unambiguous immediate context —
never on an outside source.

**Student questions.** This lecture has a long Q&A, and the speaker very often
repeats a question back before answering it ("Can you repeat the question?",
"So the question is..."). Where a student's question is audible on the
recording, it is rendered as a blockquote beginning `> **Student:**`. The `>>`
marks in the original captions indicate a change of speaker and are used as a
guide to where these exchanges happen, but the captions are not reliable about
who exactly is speaking at any given `>>` — a few of the marked turns are
plausibly the moderator or instructor rather than a student asking a fresh
question, and are left as plain text rather than labeled "Student" where that
seems to be the case.

**Restorations confirmed by internal repetition or unambiguous context:**
KV cache (from "KP cache" — "KV cache" also appears correctly 19 times);
speed of light (from "speed of life light"); GPT-2 (from "GPD2," confirmed by
the next sentence, "GPT2 quality model"; also normalizes "GPT2"); GPT-5.5
(from "GPT 5.5"); Claude Code (from "cloud code," in a list with Cursor);
heuristic / heuristics (from "heristic(s)"); SambaNova (from "Sabbonova,"
named alongside Cerebras as decode-specialized silicon); PARSE (from "parse" /
"Parse" / the one-off "PERS," 12 occurrences total, internally consistent as
the name of the speaker's method — heard only in speech, so its
capitalization is inferred from its use as a proper name, and no expansion of
the acronym is invented); ARC tasks (from "arc tasks"); 2e-4 (from "2 e minus
4"); 6e-4 (from "6E minus 4"); multiprocessors (from "multipprocessors");
disaggregation / disaggregated (from "disagregation" / "disagregated");
DeepSeek (from "Deepseek"); NVIDIA (from "Nvidia"); Nemotron (from "Neotron");
flash attention (from "flash tension," confirmed by correct "flash attention"
earlier in the same talk); agentic (from "aentic" / "aente," confirmed by
correct "agentic" elsewhere); prefill (from "preill" / "prefield," confirmed
by dozens of correct instances); megakernel(s) (from "mega kernel(s)," one
word, matching this transcript's own header/title convention; one instance of
"micro kernels" at ~1:03 is also restored to "megakernels" — the entire
surrounding exchange, including the speaker's own answer, is unmistakably
about megakernels); NVL72 Grace Blackwell (from "NVL sendme 2 grace
Blackwell," confirmed by the very next sentence, "these are 72 GPUs"); folks
(from "the DeepSeek foes" — only the word "foes" is fixed here; the version
number is left unclear, see below); logits (from "the activations and what
the logic should be," a routine ASR near-homophone in an ML context);
life of a token (from "life of a kernel" at ~34:00, confirmed by "lifetime of
a token" twice earlier in the same talk); attention inference kernel (from
"attention inference corner" at ~37:54, confirmed by the identical phrase
"particular attention inference kernel" one paragraph earlier); B matrix
(from "this speed matrix" at ~50:59, in a passage that otherwise consistently
says "B matrix"); recurrent block (from "this big RB block" at ~46:19,
matching the "recurrent block" introduced a few paragraphs earlier); LLMs
(from "LMS," in "if you look at modern LMS you have ... the QKV
projections"). Removed as a caption artifact: "[music]" mid-sentence at
~49:24.

**Restorations inferred from context alone, with no corroborating repetition
in this transcript** (flagged here because there is no slide deck to check
them against): Groq (from "Grock" / "Gro," matching the LPU decode-chip
company named alongside Cerebras and SambaNova; "infancy chip" nearby is
restored to "inference chip," and "rock" later in the Q&A to "Groq"); Cerebras
chip (from "cerebra strip," where "Cerebras" is already independently
confirmed elsewhere and "wafer" in the same sentence is already correct);
NCCL (from "nickel," the standard engineering pronunciation of NVIDIA's
collective-communications library, in a passage about fusing GPU
communication calls into a megakernel); NVLink (from "envy links," NVIDIA's
GPU interconnect, in a passage about flaky cables between GPUs); Qwen (from
"quen," a widely used open-weight model family); nanochat (from "nano chat,"
in a reference to a fast-training baseline transformer that a community races
to train quickly); loop (from "boot," in "is it ever optimal to boot as
opposed to just having more parameters" — the entire surrounding exchange is
about PARSE's looping); RoPE (from "rope," the standard name for rotary
position embeddings, used the same way this course's other lectures spell
it); iso-parameter and iso-FLOP (from "isometric and isopram isoflop," the
standard names for the curve families used in compute-optimal scaling-law
work, matching the parameters/data/flops discussion around it); "the
thousand-dollar CPU" (from "the thousand CPU," a word plausibly dropped by
the captioner, in a sentence contrasting it with "your $500,000,000
machine").

**Left marked `[Ed: unclear]` — not resolved:** "GP," (~11:36, in an
otherwise-unparseable quoted instruction to a tool); "at Ender" (~34:00, "some
of the things that we see when serving things at Ender"); "Claude Mythos"
(~44:46, in "some dude from OpenAI said that Claude Mythos is ... a looped
language model"); "gal" (~47:52, one item in a list of transformer-block
components); the version number in "DeepSeek 4" (~1:10); and four short
audience questions/comments (~1:00, ~1:03, ~1:09) that are audible but too
garbled to render as clean prose without guessing at missing words.

The verbatim auto-generated captions this was edited from are kept at
`raw/transcripts/original/18-serving-megakernels-recurrence.md` (gitignored,
not published to the public knowledge base).

**[0:05]** Cool. Yeah, so thanks so much, everyone, for coming. I think you have a pretty cool course here, and thanks, of course, to Percy for inviting me to give a talk. So I think in this class you're mostly talking about training and how to train language models, and getting to this place where we can have these things that can talk back to you. Today I'm going to talk a little bit about, once you have one of those models, what it looks like from the other side — what it looks like to actually serve these models, to do inference, turn these things from electricity into tokens, into intelligence — and also what are some of the fun research problems and research things that you can do.

**[0:51]** When you look at it from that standpoint, I'll start with some high-level motivation. I think one thing that's become abundantly clear to all of us is that we are really going through almost a new industrial revolution in terms of these models and their capabilities. These slides I made during my job talks, like two years ago, so the exact examples are getting pretty old. But you can do human-level text generation, code generation — Cursor, Claude Code, GPT-5.5, not just four — you can generate images and videos, or understand images and videos and process them. You can start to understand new modalities. So we're starting to see some of the applications in the sciences, in bio,

**[1:38]** in health, DNA models, all these sorts of things. One of the things that has really been driving my research for a while is the question of what has made these advances possible, and how we can improve on them in the next generations. One abundant driver of these capabilities is really scale. So again, this is at this point a pretty old figure, but these models have really scaled up in massive ways. In 2018, at the beginning of my PhD, in antiquity, these were 100-million-parameter models at their largest, and we're like, "Oh my god, these things are pretty crazy." By 2019, we thought that they were too dangerous to release — that's GPT-2. I think in this class you can train a GPT-2-quality model. I'm not

**[2:24]** sure if you did — if you tried hard enough, you could. And today, of course, you have open-source models that are a trillion parameters and more; the frontier is probably at 5 to 10 trillion parameters. It's pretty exciting. With these you have a bunch of new capabilities, like chat, writing code, analyzing complex text, doing your homework for you, etc. What's really remarkable about this is that this transition is happening faster than we think — certainly faster than I thought at the beginning of my PhD a few years ago. I think one really apt analogy — and the dates actually match up in an interesting way — is that in 1902 there were 130,000 working horses in Manhattan.

**[3:11]** These are horses that you don't just have around for giggles — you actually have them because they play some key role. Each of these horses would produce pounds of manure a day, times 130,000 horses, and you have a real manure problem. In fact, there were entire academic conferences gathered around the question of what do we do about all the poop that these horses produce. In 1898 they actually had one of these in New York, and their conclusion from that conference was there's nothing we can do about the horse manure — you just have to hold your nose and deal with it. Ten years later, by 1912, cars had already outnumbered horses in Manhattan. So that ten-year transition, you saw this, you

**[3:56]** know, these things that have been around for centuries start to be replaced by cars. And I think for us, for language models, for a lot of the stuff that we do, that 1912 moment was probably last year. At least for me, last year I started writing the majority of my code using these language models. Most people on my team do it. I tell all my students to do it — except when they're doing their homework. This is really a really exciting transition that we're living through. One of the things that is really driving this — a lot of the scale is driven by GPUs. So in a very real sense you could say GPUs are the new oil. This is

**[4:43]** again an older announcement, but you're seeing hundreds of billions of dollars and more of investment into GPUs. History rhymes in interesting ways with similar things — there are entire countries, entire sovereign wealth funds, that are making these things a major part of their piece. And one thing that is really abundantly clear is that inference is really the piece. You can think of inference as the engine that turns electricity into intelligence — the same way that oil is only useful in a car if you actually have an engine that turns that oil into useful kinetic motion. Inference engines, GPU

**[5:29]** kernels — these are the things that really turn these GPUs from sand, like Percy said, into something that we can use today. And I think that's clear. If you think about it, these machine learning models are really just DAGs of operations — there's some mathematical object that exists in the ether. The inference engines, the GPU kernels, all these pieces, are the things that you actually have to program and map down to ML operations. I think you do some of that in this class — I think you implement some flash attention, so some training side. But there's a whole world of complexity when it comes to inference, the inference side of things. So if you take away nothing from today's talk, I hope there's one thing, which is: if you understand inference, and understand the inference

**[6:14]** engines, if you understand the GPU kernels that underlie a lot of the core technology, you can enable full-stack innovation in machine learning algorithms. So I'm going to start today's talk with a high-level overview of the lifetime of a token — when you make a request to one of these models, what happens to that request, how it goes through the entire inference service, what are some of the interesting choices you have to make. Then I'll dive deeper into two research projects around: if you take certain pieces of this system, what are questions that you can ask, and what are some things that you can do? Before I get into all the technical details, I'll quickly make a plug. I'm here representing two

**[6:59]** organizations. One is UCSD, where I have a small lab, and some of their work is represented in this talk. I'm also representing Together, who did a bunch of the other work that's represented in this talk. Together is an AI cloud — there's GPUs, inference, fine-tuning, and all the rest, with a heavy research background, including Percy. I don't think you can see my mouse, but see, he's on the screen. And we've got a big research presence behind that, and behind a lot of the things that you're going to see in this talk. All right, I'll start by giving a high-level overview of the lifetime of a token — when you make a request to an inference system, what are the different pieces that are going to go

**[7:45]** through — these slides are a crib from a presentation that my student Austin made from his experiences at Together. I'll make a note that all of the slides are completely AI-generated, from Nano Banana Pro. They're pretty good, as long as you don't look too closely at the text — I'd say at a high level everything is correct, but if you look very closely you'll see things that are very wrong. Okay, this is an overview of all the pieces of an inference engine, so I'll give a high-level description of how it works. You get a request in; the first thing that's going to happen is that the request gets scheduled to various different GPUs. You might have disaggregated prefill and decode on different

**[8:31]** machines. You'll run that request against a KV cache to see: have I seen this request, or versions of this request, before? Is there some compute that I can actually save? Then you'll start executing the core machine learning code — here are new tokens, here are new operations that we need to compute. There are various optimizations that you can make: you can split that computation across different machines, you can parallelize across different nodes, you can parallelize it within a node across different GPUs, depending on the size of your model, depending on how you can split that up. And I think one of the exciting things is that as the hardware is

**[9:16]** developing, we're actually going to start to see more different choices that you can make there. But once you make all those choices, once you execute all the code, at the end you get the tokens out, and you can say, "Hey, Chat, what did Percy mean when he said linear regression?" — because I skipped that day in my class — and you get this whole end-to-end experience. One of the things that I think is useful to start thinking about is: what do these different workloads look like? There's quite a bit of different things here. This is one of the slides where, if you look too closely, some of the fives turned into S's — but one of the things you want to think about is that when you're actually serving production traffic, it doesn't necessarily look like, certainly doesn't

**[10:02]** look like the type of tokens that you see during training, but it also doesn't look like if you just made up a traffic workload in your head. So what is the type of thing that we typically see? For a particular workload, you'll see a particular distribution of input and output tokens. Let's take a coding workload — let's say you're somebody like Cursor, where your whole codebase is available to the agent, and you're asking questions about it. Typically what that'll look like is that you'll have a long amount of input, so tens of thousands of input tokens, and then, depending on how you've trained your model, the model might output some amount of thinking tokens, or it might just output something short. And this

**[10:48]** will change based on workload, it'll change based on the model. A coding workload, for example, will look very different from a summarization workload, or a narrative summarization — if your work involves pasting entire books into a chat window and then talking back and forth to figure out what's happening, that's going to look very different from a standard chat thing. If you just go to chat and say, "Hey, explain to me first-order calculus," or whatever, that's going to have a very different workload shape. The way that we use language models today, there are these turn-based agentic workflows — when you're coding, you go back and forth with your coding agent, saying, "Hey, do this. No, I didn't mean that, do that." Your coding agents themselves can iterate on the language

**[11:36]** models quite a bit — they might invoke some tools and say, "[Ed: unclear — "GP,"] search for something in my data, in my codebase," and then take that and feed it back to the model, or try to do an internet search and look up this thing that the user asked. So typically you have these multiple turns in your conversation. Another interesting piece is that different applications will have different cadences — if you're in a fast, interactive chat-based loop, or you're talking on your phone to ChatGPT in voice mode, you might have relatively quick responses. If, on the other hand, you've put together an agentic workflow where you say, "Hey, go do this for me, I'm going to leave you alone and just iterate on your own,"

**[12:22]** you'll have a different cadence. If at some point your agent gets stuck and it's, "Hey, hey, help, I need to ask for advice," and you don't notice it, there might be another gap between turns. So all of these things start to define what your workload looks like: how many new tokens do you get every time? How many tokens are you going to generate? How long is my session — am I a very sticky user who keeps going back and forth with my Claude agent, or am I a user who's just going to ask one question and then leave and come back the next day? And, of course, how long between turns do you have? I, for example, have a chat that I've had with a ChatGPT agent about how I should set up my workout, my workouts week to week, and I

**[13:08]** interact with that chat about once every other week, so that is a very different traffic pattern than some of the other ones. And then, of course, depending on your application you might have different targets — if you have an interactive application, you might say, "I want to get the first tokens back in less than a second," so that the agent can say, "Hey, I'm thinking," and then say blah blah blah blah. Or you might say, "I know I'm going to be generating 500 tokens, and I want that whole response to come back within a certain amount of time so that my user can read it fast enough." And all these pieces — so as that request comes in, there are a few basic pieces that come in. I'll rest a bit

**[13:54]** on the prefill and decode piece in a little bit. Let's say you have some amount of text that comes in — the first thing you're going to do is tokenize it, I think you're all familiar with that. But then you start getting into a somewhat complex scheduling regime, because you have to ask questions like: have I seen these tokens before? Can I just look up some of the activations in a cache? And then you get to these two major pieces of the actual machine learning computation — these are called prefill and decode, and they're very different. Prefill means: let's say you have 10,000 tokens that you've never seen before, and you want to go compute the activations and what the logits should be. So, 10,000 tokens in, one token out.

**[14:39]** That's a very compute-bound operation, so that actually looks pretty close to the things that you guys have been looking at for training time — when you're training you have some large amount of tokens, you write your flash attention kernel, you run your training loop. Prefill is very similar, you just don't run the backwards pass. And then you have this thing called decode. Decode is when you're just generating one token at a time — you pass in these 10,000 tokens, it's like, "Hey, here's my codebase, tell me what function ABC does," the model is going to process the whole prompt and then start generating one token at a time, maybe three or four tokens if you have speculative decoding working. If you think about that operation — every time you generate a new token, you then have to run that back through the model — if you do the math on that, there's actually not too many flops that you need to compute. So it's going to be

**[15:26]** a relatively light computation, but it's going to be very memory-bandwidth-bound. What that means is you have to load up the model every time just to generate a single token. At the end of that model pass, of course, you then have a single number that represents a token, that then gets turned into a string. You probably do a little bit of processing on that — you look for stop tokens, you might run a safety check to say, oh, is my user trying to hack into the system in a bad way, etc. — and then at the output you get the nice tokens. The inference engine is really running in this loop, waiting for these requests to come in, so it's running this scheduling,

**[16:13]** execution, token sampling loop, and repeating. So now we can take one step deeper and start to look at what it looks like when you have a system that is processing many different requests at a time. We have this technique called continuous batching. The way to read this figure is that time is flowing downward — as you go down, there are new requests coming in. Let's say if we go to step one, you have some long request — you have a user that is asking to analyze some long document, and the engine is generating a bunch of tokens out. You

**[16:59]** might have another request come in that starts to take up some resources. These resources are first compute resources — you have to run things over multiple requests at once. They can be memory resources if you're filling up a KV cache. So you'll see these multiple requests happening at the same time. After a step, that short request might finish, and you get a new request that comes in, and maybe it's running for a couple steps. If you have another request come in, maybe it's another very long request — that's going to be that orange one in step four — but maybe you don't have enough GPU memory, since you need to store all your KV cache on your GPU, and maybe you've run out. You might start queuing for that reason. Then, once

**[17:46]** that long request is done, you can start the new request, and so on — you can watch it run. So already you're seeing some of the complexity when you have many different requests living in a system, and how it goes on. One of the pieces that's quite important is this thing called a KV cache. The way to think about this is: you probably have a lot of users who are saying "hi" to ChatGPT or "hi" to Claude, and theoretically you don't need to compute new activations and run that again for every single user. Or if you have a long book that the user has passed in, you run the prefill over that once, and then the next time you get some

**[18:33]** addendum to that request, the next turn in the conversation, you don't need to compute the whole thing again. So we have these mechanisms called KV caches that use prefix sharing — you say, "Hey, I have this new set of tokens that comes in," use a very traditional data structure like a basic tree, look at which tokens you've seen before, which tokens are new, and then do a lookup of what those activations are going to look like once you actually run the computation on the GPUs. There are various ways to split it. I'm not sure if you talked about this too much, but let's say you have a trillion-parameter model, and you're running on, let's call it, 280-gigabyte GPUs — you're not going to be able to fit the whole

**[19:18]** model onto each GPU, so there are various ways that you might split it. You might take every tensor and split it four ways across four GPUs — this is called tensor parallelism. Or, today, a lot of the state-of-the-art models are mixture-of-experts models, so you have these individual experts that get selectively activated depending on different tokens; you can split those across different GPUs. The choices that you make at this point will determine what the bottlenecks are — how many GPUs do you need to run your model, how many sessions can you serve at the same time, etc. One of the big things that happens is that we tend to split prefill and decode onto different GPUs,

**[20:05]** different sets of machines, and that's because they have very different compute bottlenecks and compute characteristics. Prefill looks a lot like what you guys do during training — you just run it, it's very flop-heavy, you can really get the most out of your GPUs. Decode, on the other hand, is very memory-bandwidth-heavy, because you're not doing that much compute but you still have to load up all the model weights at the same time. These things will also take different amounts of time — prefill will typically take a lot longer than a single decode step, but you're going to be running a lot more decode steps, because you run prefill once for a prompt, and you run decode once for every token that you generate. So a very basic optimization that pretty much all of us have started adopting is that

**[20:51]** you'll run prefill on one set of workers, decode on another set of workers, so that you can specialize those two computations to different pieces of the stack. It turns out there's a lot of innovation that you can have when you have these splits — some things you might have heard of, like when NVIDIA bought Groq: NVIDIA, the king of GPUs, buys this new kind of inference chip. One of the reasons is that the decode workload is so different from the prefill workload that if you're looking at decode, you can be using very different chips. So, for example, in the next generation of hardware, NVIDIA is planning on using its GPUs for the prefill side, using these LPU Groq chips for the decode. You see similar things with Cerebras. So

**[21:38]** OpenAI has this compute partnership with Cerebras — it's another chip that's much better at decode. There are other companies, like SambaNova, and so on, that are also making bets along various parts of this space. Here's a fun one, an interesting one: when you actually start deploying these inference engines at scale — when you're starting to serve trillions of tokens or more a day — you start to get some pretty nasty bugs. This is a set of bugs; I think most of these happened, I want to say, late last year, with some of the open-source inference engines. And, basically, one of the characteristics of these large-scale

**[22:23]** systems is that something that will work well at a small scale will inevitably start breaking at a large scale. We're talking about events that happen 0.001% of the time, or less. Some of these can be — so, NaN: sometimes you'll have a kernel that is very slightly wrong, but the conditions for triggering it are very rare, and you'll start having some of your logits turn into NaNs halfway through the computation. When that happens, we figured out that the model starts outputting the same token — I think for some model the output just starts saying "hi hi hi hi hi" after a while, or starts outputting exclamation points, and you get caught in these loops. Another model, at

**[23:10]** some point, someone made a change to how you're handling tool calls. Tool calls are when the model says, "Hey, I want to make an internet search," or something like that — the model will say, "Go do an internet search," and then your end harness has to go use old-fashioned code to do the internet search. At some point, those tool calls stopped being processed correctly in one of the engines, and the symptom for that was that the completion length shot up, because the model would say, "Hey, make an internet search," and then — usually, when the model was behaving correctly, it says, "Hey, make an internet search, I'm done, go return to the user, go do that," whatever — but in this case it wasn't returning correctly, so it would say, "Hey,

**[23:56]** make an internet search, hey, make an internet search, hey, I don't know why there's no internet search going on" — it would just get into this very long doom loop for tens of thousands of tokens. There's another one that was interesting because it actually took out, I think, a number of inference providers at the same time, and actually got blamed on a quantization issue. But actually what was happening was a much more subtle bug, where the model would just suddenly, randomly start to output Chinese characters when it had no reason to. For some models, at some point, there was some speculation — oh, they must have fine-tuned on a Chinese model, because my model certainly starts

**[24:42]** I ask it a question in English and it starts speaking back to me in Chinese. Actually, what was happening is that there was just an off-by-one error in one of the kernels, and it was a very subtle bug — sometimes you would read in some extra, uninitialized memory space from your GPU, run it through attention, and then at the end of that whole process you get a random Chinese character. Then the model will go, "Why did I start suddenly thinking Chinese? I must be — the user must be asking me a question in Chinese," and then it will just veer off into Chinese. So sometimes when this happens it's because the model has legitimately been trained to think in Chinese; sometimes it can just be an off-by-one bug in somebody's code.

**[25:30]** Some interesting things are starting to happen in a lot of the more advanced inference stacks. A major piece of running large production systems is that you want to have as large a KV cache as possible — it's best if you can cache requests from many different users, or from the same user across many different sessions, and be able to run as many sessions as possible. You first start this by storing your KV cache on the GPU, and then you quickly run out of GPU memory, so next you can start storing into CPU DRAM. If you're paying attention to Jensen's keynotes, he's recently started getting very obsessed with CPU

**[26:16]** performance. One of the reasons is that a past generation of CPUs was actually really slow, and as a result started bottlenecking a bunch of very important workloads. As a result, you start paying attention, because if your $500,000,000 machine is being bottlenecked by the thousand-dollar CPU that you purchase to put on top of it, that's not a great place to be. One of the reasons that can happen is that you might be storing your KV cache on CPU memory, and so you really care about the speed of being able to read that KV cache back. Next, you might put more KV cache onto the disk itself, and then you start caring about SSDs, and SSD space. Again, you've probably heard

**[27:03]** these rumblings about OpenAI buying up all the SSDs, all the DRAM in the world. Part of the reason is for stuff like this, where you want to store as much stuff in your KV cache as you can. And then, of course, when you're actually building your engine, there's this complicated dance of: I haven't seen these tokens in a while, maybe I'll evict them, send them onto the CPU, or send them to disk, or send them into some other global store. When I get a new request in, I have to go look, I have to wait for those to come in, fetch them, load them up into my thing, and then we're happy.

> **Student:** CPU offloading isn't like a special kind of workload that gets offloaded — like, for something fast, you don't want to do this, right?

**[27:48]** > **Student:** Time to hit an SSD is very long.

Can you repeat the question? Yes — yeah, yeah. So the question is: for this offloading, is it a particular type of workload that you offload? So here we actually get to — I'm sure none of you guys have taken an operating systems class, but you should take your operating systems classes — we actually get to some pretty classic scheduling things. This diagram, except for the GPU things on the right, looks exactly like an operating systems diagram that you might have seen in the '70s or the '80s, because we used to have this problem where if you opened up too many applications on your computer, you would run out of CPU memory, and then you'd have to put those applications onto disk. It's exactly the same workload. So

**[28:36]** ideally — so here, actually, on the left side, Nano Banana hallucinated "evictions for least recently used," which is actually a pretty decent heuristic. There's probably some OS paper somewhere that says LRU is within 2x of what's optimal. Of course, the best thing you want to do is, if you could predict the future, you would know, "Oh, I'm about to have a request come in of a particular kind, let me go prefetch that memory in." It might actually be possible to predict the future — for example, when you go into your chat app and bring up some old conversation from a month ago, that's a very strong signal that you're going to start asking a question about it, and you might then want to go load that up onto a GPU. But

**[29:21]** yeah, in a perfect world you predict the future. If you can't predict the future, use various heuristics. Really, it's a question of how much traffic you want to put onto your GPU footprint — I've never talked to anybody who wants to put less traffic onto their GPUs. So, subject to those SLAs that we splashed up at the beginning, you want to serve as much traffic as you can, right? Some fun things that are starting to happen: in the last generation of Blackwell GPUs, NVIDIA started putting together these new GPUs called NVL72 Grace Blackwell chips. These are 72 GPUs that are

**[30:09]** connected with really fast interconnect. So you start to do things like: how can I split my trillion-parameter model across all 72 GPUs? Does this make any sense? What does it buy me? What happens? How do I start to think about fault tolerance — these things can fail a lot for various reasons. One reason is that the connectors are kind of flimsy, made of plastic, not metal, so if you jam the thing in too much, your cables are going to bend a little bit and then you get really flaky NVLinks — which is a whole thing in itself. But what happens when — okay, so this is the AI generation, it put a fan into the chips, which doesn't quite make sense. One of the things

**[30:56]** that you want to start thinking about is: if I've taken a model, split it across 64 GPUs, I'm serving production traffic against millions of users, trillions of tokens — what do I do when a single GPU goes down? Is there some way to make that fault-tolerant? There's a lot of fun things to think about there. And then, of course, you're starting to see these models have a million-token context or more — how do you actually process it? Do you split that context across many different GPUs? How do you do these different things? So I want to just very briefly highlight one example of an optimization that you can start to make when you look at this whole process from a systems level. This is a piece of

**[31:44]** work that we put out together a few months ago called cache-aware prefill/decode disaggregation. It's a very simple optimization — it's like two lines of code in the routing layer, but it can actually make a pretty big difference. The basic idea is: you have all these requests coming in, and most of them are going to be turn-based, turn-by-turn requests in a conversation where somebody's already started a conversation. Let's say your average conversation lasts 10 turns, and then the user goes away — that suggests that 10% of your requests are going to be very fresh, very new requests. When you have a new request come in that's going to be thousands of tokens, it's going to look very different, it's going to be a lot more expensive to compute. You don't

**[32:29]** necessarily want to put that prefill against a short conversation, where you're halfway through a conversation — someone passed in a book and says, "Hey, talk to me about this book." You don't want that running at the same time as someone who's midway through a conversation, like, "Chat, explain to me why one plus one equals two," and chat is like, "One plus one equals two because numbers," and whatnot, and you're like, "Oh, I don't get it." You don't want that very short question-and-answer to happen at the same time, on the same GPUs, as the very long request. So you can put together this really simple router that says, okay, if we have a new request that comes in with a very low cache-hit rate, send it to one

**[33:14]** set of GPUs, so those can all process things together, and then send all my other warm requests to another set of prefill nodes. Turns out, if you do this, you can get up to 40% faster serving with these very simple optimizations. So that's one set of things. I'd say the way that I would characterize where we are, in terms of the research and these techniques, is that we're very early — this is the type of thing that, in 10 or 20 years, people are going to look back on and be like, "Oh, why are these guys talking about this? Isn't this already obvious to folks?" But really it's because we're starting to see these things running in production in new ways, seeing these new bits of traffic in

**[34:00]** new ways. All right, so that was an overview of the inference — the life of a token. Now I'm going to talk about two interesting research projects that are very much inspired by some of the things that we see when serving things [Ed: unclear — "at Ender"]. The first one we're going to talk about is language model decode, and how you can make it go a lot faster with these things called megakernels — this is a collaboration between Stanford and Together. So, the fundamental challenge when you're running decode — decode is the process by which, okay, you've processed your prompt and now you're generating one token at a time — the fundamental challenge is that you have to run the whole model to generate a single token. So that

**[34:47]** means that instead of using all this big parallelism that you get with a GPU, that you can use during prefill or training, you've now turned this massively parallel system into basically a glorified memory loader. One of the things that makes this extra challenging is that the way we typically write kernels and run a model is that we write down kernels and program things one operation at a time. You guys can probably understand why — kernels tend to be pretty challenging to write, I'm sure you all had a lot of fun writing flash attention — but that means that typically what we do is look at all the different operations in a language model, and we

**[35:33]** will write a single kernel for that operation at a time. This makes things a lot easier to program, because you just have to write your norm kernel, or your map kernel, or attention kernel — but it ends up introducing a lot of downtime into your system. So this is an example of what it might look like when you're running inference across your kernels — this is obviously a cartoon, but the example is drawn from a particular attention inference kernel. The way to read this is that on the x-axis you have time, so time is flowing from left to right, and on the y-axis you have all the different little streaming

**[36:18]** multiprocessors on your GPU. On an H100 there are 132 of these; on the B200 there are, I think, 148, etc. The bars indicate useful work — when there's a bar here, one of the processors on the GPU is actually doing work, and the empty space is just waiting, waiting for other operations to finish so that something else can go on. So basically you get into this position where, no matter how well you try to write the kernel, you're always going to have downtime on your GPU. You're going to have things like kernel launch — the kernel launch and kernel teardown, that's these big gaps in the red and the yellow. You're going to have these things called tail effects. This is just the same way that if you have

**[37:06]** a short prompt that gets processed with a very long prompt — this same thing goes all the way down to the basic attention operation. If you're processing a batch of inputs and one input is very short, one is very long, you're going to be waiting for the very long input to finish. And then, because you're running these across multiple kernels, you'll actually start to see these gaps between kernels add up. So one thing that we put together to try to solve this is this thing called a megakernel — this is basically saying, instead of treating each operation in the model as its own operation and writing a kernel for it, let's write a single kernel to cover multiple operations at once. This is similar to the fusion that you see in flash attention, except done more aggressively, across a larger number

**[37:54]** of things. In particular, what it does is turn the GPU from a single device, a single operation, into — you start thinking of the GPU as a massive distributed system, and saying, okay, I have all this work that I need to get done, some of it has dependencies on other stuff, so that red stuff has some dependencies on some of the green bars — how can I schedule it, how can I distribute the work to maximize my GPU utilization? If you do this just to the attention inference kernel, you get 30 to 70% speedups. And the nice thing is that you can actually do this to the whole model — so this is one layer of a Llama 1B model. Here we've basically taken the entire

**[38:39]** layer and put it together into one kernel. If you look at all these different bars, you see things overlapped in really weird ways — that's because we're now starting to overlap, say, a weight load from the next layer into the attention, or starting to run parts of a reduction before the attention operation is over. Here's one interesting example — here's one thing you can do. If you look at modern LLMs, in the attention layer you have the QKV projections, and you're going to add some RoPE scaling to it — those are these blue lines, so those blue lines are QKV plus RoPE. And then the orange lines, this is the beginning of the attention. So one of the insights is

**[39:25]** that you can start loading your KV cache into attention before you're finished with QKV — particularly during decode. Some of these orange bars, with these circles, show you starting the KV cache load while QKV plus RoPE is still running. Then, once QKV is done and you have your new query tokens, you can run the rest of the attention operation. So basically, when you have this fine-grained control of GPUs, you can start some operations before others are over. Here's another one — again, the orange is the first part of the attention computation, the red is the O projection that comes after attention, and you have your O projection start loading the weights

**[40:11]** before your attention operation is over. We put this together in a relatively complex CUDA framework, with basically an instruction-based abstraction where we can implement each subkernel in its own file, and then have a big virtualized shared-memory system to orchestrate the running of these operations. To do this, we put together this library called ThunderKittens, which is one of these kernel-writing libraries — you can think of it as almost like Triton, except more low-level, with a lot more fine-grained control over things.

**[40:56]** The payoff is you get near-speed-of-light decoding inference — these numbers are actually a lot better now, but the megakernel is shown by this bar in teal, and you can see it runs a lot faster than some of the other state-of-the-art engines. On the H100, it's achieving 72% bandwidth utilization, which is near the speed of light on the GPU — if you ignore all the complexities of what we're doing here and just ask how fast the GPU can physically go to do this operation, we're pretty close, 72% of that speed of light. So one takeaway from that section is that if you have very deep control of the kernels, and an understanding of the hardware, you can start to enable very

**[41:42]** different compute paradigms, and all these things you only see when you start playing with inference at a deep level. Okay, now I'll talk about something that's a little bit new — this is now getting into the realm of new architectures, and I'm going to be talking about this new model called PARSE. This is some work that comes out of my UCSD lab, led by Hayden, and also in collaboration with two folks, Zachary and Taylor. Okay, I'm going to come back to this — at the beginning of the talk I talked about how all these new capabilities are coming because you're starting to scale these models, in parameters and data. With PARSE, we wanted to ask another question, which is: is this the only way you have to scale, or is there

**[42:27]** potentially some other way that you can get this quality. With PARSE — this was basically our take on a technique called loop transformers, where you take some blocks of your transformer and run them in a loop. So instead of having your tokens go through the model one layer at a time, at some point you say, as you're going through, just send it back through that loop. There are a couple of different pieces here — one is that we use some state space model theory, some SSM theory, to stabilize this operation. Naively, if you just run this and let the thing train, we saw that it was going to blow up. And the other thing is we started to see some interesting scaling laws that suggest that you want to be

**[43:14]** scaling the recurrence of these recurrent models as you increase the data — in order to make the best use of your parameters, you want to actually be reusing them somewhat. First, I'll talk a little bit about the motivation for why we found this looping problem to be very interesting. The basic idea is: let's say you have some activation that's going through part of your model — at some point it hits these looped blocks, and the same activation is just going to run through that same layer some number of times. That purple block is the recurrent block, and at the end you get the thing that comes back. There are some advantages to this — you can keep your parameters constant,

**[43:59]** but it gives you a dial to increase your flops. So, if you think that more flops equals higher quality, this is a way to increase your quality without paying a higher parameter cost. Another thing is there's this old work — "old" is a relative term, from a few years ago — that suggests you actually get higher expressivity, that there are things you can't express with the same number of parameters that you can express with these looped models. One of our driving questions was: what's the best quality per parameter, what's the best intelligence per parameter, or intelligence per parameter and data, that these things will allow you to do? There were some promising initial

**[44:46]** results — this is a paper from Tom Goldstein's group at Maryland that suggested, hey, this thing might be better than transformers, with some results on the ARC tasks. There was also a bunch of Twitter hype, because about a week before we released PARSE, some dude from OpenAI said that [Ed: unclear — "Claude Mythos"] is a recurrent, a looped language model. I don't think it's right, I think he was just making it up, but as a result it really blew up on Twitter, in speculation, right before this — eventually he had to write this blog post being like, "Hey, my bad, I just made that up, none of it's true." But it was sufficiently interesting timing — we were working on this before this Twitter hype. But I

**[45:32]** think there's something interesting to look at here. Now, one problem with these looped models is: if you looked at any of them, and you tried to train them, and then changed anything about the training algorithm at all — say, if you changed the learning rate by a little bit — you'd suddenly start to see these models blow up. If you did a simple thing like a learning rate sweep, you'd see that nine times out of ten this model just isn't going to converge — it's going to blow up, you're going to get NaNs, you're going to get these big loss spikes. So we were saying, hey, there seems to be something wrong with this. If you're ever training a model, and you're scaling it up, and you see these big loss spikes that suggest something has gone very wrong with your training process, you should take a deeper look and try to figure out what happened. In previous work,

**[46:19]** there were some hacks, like you can put norms in every layer to figure out what's happening, or just pick the learning rate of 2e-4, don't pick any of the other learning rates. But we were thinking, hey, the existence of these loss spikes suggests that there's probably something deeper going on. So we took a bit of a mathematical, state-space-model-esque approach to the stabilization question. Our basic insight was: you can look at this process — if you try to think about how to analyze it analytically, it's going to be very complicated, because this big recurrent block has tons of parameters, there's all sorts of nonlinearities, there's a softmax,

**[47:06]** there's RoPE, and all these other things — if you try to analyze it analytically, it's quite complex. Our insight was, we were just saying, okay, let's actually just look at the residual of this thing — how is this activation changing from block to block? Our first empirical observation was, hey, it actually doesn't change that much — each of these residual blocks is maybe changing the vector a little bit, but not really having a massive impact. And as a result — how can we, maybe we can actually model this, maybe we can look at what's happening more deeply. So what we did is we went and wrote down a dynamic system over this residual, and when we did, we realized

**[47:52]** there are a couple of pieces in here that, when you look at how you write down the model, might look relatively benign, but actually end up being a huge deal. So what we did was we said, okay, there's all this nonlinear stuff — there's this attention, there's this [Ed: unclear — "gal"], there's this big feedforward network, with the intermediates and stuff — we're going to put all that into a box, we're just going to call it R, and this R is going to be some big nonlinear thing, but we're going to stick it to the side. What you're left with is these A and B matrices. This B matrix is some transformation over your

**[48:38]** initial vector — what is that first vector before you start the loop? And then this A matrix — how do you transform that residual in each loop? And what we did was say, hey, this simple thing — if you take all the complexity of the transformer and stick it to the side — gives you a relatively simple way of looking at all the previous loop transformers. In previous cases you do end up making some pretty normal decisions: in one case you just treat it as the identity, you're just going to add things; in another case it's a fully learnable matrix. And then we said, okay, what happens if we — so, the residual, we observed

**[49:24]** empirically that these A and B matrices are actually dominating the magnitude of this equation. What happens if you just drop that complicated nonlinear piece — what do you get out? Well, you get out a pretty simple system that, if you use high school calculus, you can just solve to figure out what the answers are going to be. So here's the closed-form solution — you can actually compute, given what the initial activation is and what that initial injection is, what the activation is going to look like at step t plus one. And you notice a couple things: this thing is dominated by these A matrices, and especially this A matrix that you are powering up

**[50:11]** to a large degree. From here, what we realized is that there's this quantity called the spectral radius of this A — spectral radius is basically another word for norm. One way you can look at this is that you're taking this matrix and powering it up to huge amounts. If this matrix can learn to be something like — let's say, if you go to scalars, imagine this matrix is two, and then this t is like 16 or something — you've now taken this activation and blown it up to 2 to the 16th, and it's really big. This starts to explain some of those big loss spikes. And, in particular, what we found is that the choices that people make for these A and B matrices, in

**[50:59]** these previous papers, are either — we call them marginally stable, or unstable, and unstable makes the system unstable. So with PARSE, we said, okay, we figured out this thing where, if you take this particular look at it and let A and B do whatever they want, you're going to get things that really explode. What if we just constrain A and B such that, if you run the math, they're not going to explode? So for A, what we did is we said, okay, we're effectively going to make that A matrix a negative diagonal matrix — if you power that up, the term eventually goes to zero, so it doesn't blow up. For this B matrix, we're going to stick a really simple linear norm against it, because the B matrix actually only gets

**[51:45]** applied once, so it doesn't really blow up. If you compute the spectral radius, it's now going to be less than one — it's now actually going to be a stable system. Now, if you go train this model, what we saw is that you actually got a stable loss curve. So PARSE is this stabilized thing where we reparameterized A and B — you can see that even with the 6e-4 learning rate that was so bad for the other models, you actually got a stable model at the end. And you see that, by doing this, you can naturally constrain the state norm of the activations. So, two pieces here: that orange baseline is just a completely unconstrained model, you can see it blows up, goes to 10 to the 19th. This blue line is actually a

**[52:32]** a model where you are applying a norm to it. What happens here is that the model is actually trying to expand the activations, because it's saying, oh, with more room I can represent different things better, I can put these different concepts further away from each other, or whatnot. Then you're applying a norm to it, to take that big thing that's trying to expand and norm it back down to one. And then you have these two pressures that are kind of fighting against each other, and that manifests in loss spikes. So even though on the right your norms are very good, you're not seeing the activation actually blow up — you do see that the loss can do some pretty gnarly things. So basically this pretty simple

**[53:18]** change in the activations can stabilize the training, stabilize these recurrent loops. So it's not only a more stable system, you actually also get higher-quality models. This is a table where we're comparing PARSE models against a previous loop transformer called recurrent-depth models — you can see higher performance across a variety of applications. So you see PARSE outperforms the previous loop models, and also outperforms a strong transformer baseline — this transformer is like one of the nanochat ones, where a bunch of people are trying to just get it to learn as fast as it can. And these PARSE models, if you take that same basic transformer architecture, start looping it, and then stabilize it, you

**[54:05]** get better perplexities, better end-to-end quality as well. And then we started to run some very basic scaling laws here. Our question here was: okay, this is really cute, you can do this looping thing to make things better, but really, is there any evidence that there's something here that we want to be doing — looping more aggressively? So let me take a step back. A few years ago, there was all this work on — as we started to enter this regime where you wanted to scale up all these models — a natural question of: should I make the models bigger, or should I just train on more data? A few years ago, folks started to ask, how should I

**[54:51]** scale these two quantities with each other — should I scale model parameters, should I scale data? And we came up with all these very complicated power-law curves that all look like this, they're very pretty, lots of colors. What you want to look out for is — sorry, what you want to look out for is: if that curve is going down and to the right, it suggests you should be scaling both data and parameters at the same time. If it were going straight down, that would mean, you know, just increase your training data, there's no need to increase your parameters. If it's flat, just going straight to the right, that means don't increase your data at all, just increase your parameters. But you see it's going down and to the right, which means you should scale data and parameters at the same time. And now you see this, you

**[55:37]** know, you train a one-trillion-parameter model on 35 trillion tokens, and you get better quality. So our question was: okay, where does recurrence fit into this? There are a couple — so, down and to the right means you want to scale data and parameters — there are a couple of possibilities with recurrence. For example, you might conclude you should never run recurrence — it's better to just keep the same single recurrent model — or you might conclude you should do a ton of recurrence, or maybe only some recurrence. What we're showing here is, at least in these initial scaling laws — all of these curves are iso-parameter and iso-FLOP, so each of these curves is a

**[56:23]** model — so on the left and the right you have the same number of parameters. As you go down, as you change the colors, we are increasing the amount of flops used to train the model, by increasing the amount of data. So here we're varying data and varying the number of recurrences. What we find is that, in both of these models, you see this down-and-to-the-right trend again. So what this is suggesting is that, for these fixed-parameter training runs, as you increase the amount of data you should actually also be increasing the amount of recurrences you have. We find that these recurrences follow some pretty classic power laws, so you can actually start to predict — you can get these scaling laws to

**[57:09]** start to predict quality as you scale recurrences and your tokens jointly.

> **Student:** Question about recurrence — what about jointly, from the scaling?

Yeah, so we had this really complex 3D figure that showed recurrences, data, and parameters, and it kind of pointed, also, down and to the right, and down that way, at the same time — so if you believe that figure, it suggests you should be scaling all three together. But that figure was just really hard to look at, because it was kind of 3D and weird. But yeah, these power laws suggest that when you're increasing data you

**[57:54]** should be increasing recurrence. And then you have other power laws that suggest that when you're increasing data you should be increasing parameters. So, it suggests that you should increase all three of them if you can. But one piece here is that if you're going to fix your model size, and you're going to increase the amount of data, you should also be increasing the recurrence — which is interesting, because as far as I know, all of our models today have no recurrence in them, so they're all at the very left of these curves, and they all have a ton of data, which suggests that there might be something slightly better that we could be doing when training these models, right? And here's just that experiment in another way. So, on

**[58:41]** the orange curve — here we fixed the model size. The orange curve is a fixed-depth model, so this is like a traditional transformer model. The blue curve is: when you fix the flop budget, where do you land on the curve for a looping model? So here, these orange and blue dots have been trained with the same number of flops, but a different amount of data — but they're the same size. When you get to that number of flops by increasing recurrences, as well as not just increasing data, you start to get smaller validation losses, which again suggests it might be the case that we should be looping all of our big pre-training runs.

**[59:30]** Yep, so that's PARSE. If I take a little step back, and go back to the big takeaway of the talk — I hope I've given you a little sense today that if you understand the inference of these models, if you understand the GPU kernels, if you understand all the pieces that go into it, you can really start to enable full-stack innovation in machine learning algorithms. Whether that's through a new routing algorithm that lets you serve more traffic, or serve traffic in a different way, or new kernels that let you run part of your system way faster, or new architectures that say, if you have a lot fewer parameters, you can fit them into a subset of your GPUs in a different way, reduce the amount of communication — these are all different pieces of that

**[1:00:18]** question of that really research problem that we're seeing today. Hopefully I've inspired at least one of you in this room to go take a deeper look at some of that. So, with that — thanks, happy to take a few questions. [applause]

*"Okay, we have about five to ten minutes for questions — if you have a question, raise your hand."*

> **Student:** For today, I guess — [Ed: unclear — "are you creating from scratch on the loot? I was wondering if you could kind of take a matrix."]

Yeah, yeah, great question. So the question was, with PARSE, are we training from scratch, is there anything you can do with pre-trained models? So there was a

**[1:01:05]** really troll blog post from someone a few months ago, where he was like, hey, I won some leaderboard competition without training a single thing — what he did was he actually looped two or three layers in a Qwen model, and just saw that on some math things it started having higher quality. So we have a little bit of work trying to look into this that may be coming out soon. But there could be some models where, if you do a little bit of looping just to the pre-trained model, you can get a higher-quality thing, which is really weird — it kind of disturbs me, I don't know why that would possibly be the case. But yeah, we're quite interested in this, I think we'll be looking at it, and hopefully, if I can convince Hayden, he'll be staring at the

**[1:01:52]** at the activations, at the actual weights, to figure out why, when you loop it, it gets better.

> **Student:** Maybe just to follow up on that — you talked about compute optimality for the loop models. Can you speak about the inference implications, the memory, and how that can make you go faster?

Yeah, so one of the reasons that I was personally very excited about these loop things is that one of the big bottlenecks to serving inference efficiently actually ends up being GPU memory. If you have fewer parameters, you can fit, for example, more KV cache, or you can do less communication, because you need to split your model across fewer GPUs, and things like that. So there's actually a lot of flexibility that a smaller model will get you. I also had this dream that if you

**[1:02:38]** could make the recurrent block small enough, you could actually write a little megakernel to just do that recurrence in a very fast megakernel loop. So far we haven't been able to make those blocks small enough, but I think it's quite interesting. Certainly, with the next generation of the LPU, the Groq chips coming with NVIDIA things — those have like 250 megabytes of memory, or something like that — the thing you'll be able to fit into them is very, very small. But maybe you can design something that will actually fit into them, and then you can just keep your weights in memory the whole time and run your activations through as quickly as you can. So there are kind of nonlinear benefits that you can get if you can cross some of these thresholds, that I'm hoping we'll be able to get to pretty soon.

**[1:03:26]** > **Student:** [Ed: unclear — "talked about curious, I guess the trade-offs are always like strictly optimal"]

Great, great question — so the question is, what are the trade-offs for megakernels? The trade-offs are people's blood, sweat, and tears — megakernels turn out to be very, very labor-intensive to write. To give you some context, I think a fully talented kernel engineer, over the course of a year, will probably be able to write megakernels for one hardware, for two or three models, for batch sizes one to 16. You go to batch size 17, you're like, nope, start over, got to go again.

**[1:04:11]** So they're very, very challenging to write, and, at Together, we're trying to put together some compilers that can automate some of that process, but it's a very challenging thing to do, I'd say. I think the basic megakernel idea has kind of gone in peaks and troughs over the last few decades when it comes to GPU programming. But yeah, if you can do it, it will go super fast, you won't be able to go faster — it just takes a lot of energy and a lot of effort.

> **Student:** Cool, maybe I can ask a question — can we talk a little bit more about code design? In particular, you mentioned all this new hardware, like Groq and Cerebras, on

**[1:04:56]** the inference side — so if you're designing a model, and you know that's going to be the serving platform, how should you be changing your architecture?

Yeah, yeah, I think there are a couple of things you should look at. One is, you're going to be most constrained by memory first — so if you know you're going to be taking a model and serving it on a particular Cerebras chip, you want to go look at the Cerebras wafer, figure out how much memory you have, and then size your model so that it can fit there with enough KV cache, or whatever, to spare. If you look carefully at the Chinese models that are coming out, they've started making some interesting choices that suggest they might be starting to think about the Huawei chips that are coming out — you'll see quantization

**[1:05:43]** choices that people make. So if you have a model that you're intending to serve on NVIDIA GPUs — for example, NVIDIA's Nemotron model that they released — you will train that model in NVFP4, an FP4 format that is proprietary to NVIDIA chips. If you're not going to run it on NVIDIA chips, like if you're on AMD, then you're going to run this other format called MXFP4. They each have their pros and cons. But yeah, you'll make these kind of subtle choices based on the hardware you're choosing.

*"I think there was a question over there."*

> **Student:** For PARSE — I was wondering, if you just care about compute-optimal training, is it ever optimal to loop as opposed to just having more parameters, or is it mainly a trick to reduce inference cost?

**[1:06:29]** Yeah, so I think that — the trick with compute-optimal is, it's usually, given a flop budget —

Repeat the question?

Oh yeah, sorry, sorry — yeah. The question was, for PARSE, would you ever choose to loop instead of increasing parameters? So the trick with compute-optimal is always: given some flop budget, figure out what you want to hit. It's almost a little bit contrived, in that sense, because if you want a higher-quality model, you should just increase your flops budget, and if you've decided on your model size, just train longer, or, if you're restricted by your model size, then loop longer, or if you've run out of data, then pick the model size that you think will be as well-trained as you can for that size. So I think there are choices

**[1:07:17]** and, of course, you make all these choices in the context of: do I think it'll get picked up, how am I going to serve it, if you're going to release it open-source, what is the size of model that people can serve on their laptop today. So I think all these things go into making a choice of how big a model you choose to train. I think if you just make the model bigger and train it on more data, it's always going to get better, but yeah, I think it really depends on those kind of design points.

> **Student:** Maybe another question about code design — you mentioned at the beginning different use cases, like agentic coding versus, let's say, batch

**[1:08:03]** processing of data — what are the most dramatic differences, in terms of optimal architectures, that you see across different use cases, right, and I guess, at the end of the day, of course a model developer has to pick one and try to be reasonable across many use cases.

Yeah, yeah, that's a great question. So I think one of the big differences — when you have these agentic looped workflows, one of the things that matters a lot is that you want to keep your KV cache as hot as possible. If you're doing a big batch-processing thing, where you only see each document once and then translate it, the KV cache doesn't necessarily matter as much. If you look at something like the DeepSeek MLA

**[1:08:49]** attention, that's a radical compression of the KV cache compared to what you'd have for another model — or if you have a model that can process its KV cache in FP8 or FP4, those are pretty big departures in terms of the size of the KV cache that you'd look at if you're sensitive to an agentic workflow. Then, of course, the biggest one is causal attention versus non-causal attention, right — so if you're doing a big batch-processing workflow, like, for the longest time Google was just using BERT models, and I think probably still uses BERT models on search, and that's because you don't really need to be generating a bunch of tokens on the other end, you just do that big bidirectional attention once, get your vector out, and then stick that in

**[1:09:35]** the database, do whatever you will with it — whereas, in chat workflows, that processing, there's always going to be this decode portion of it. I think there have been intermediate things — I know T5, at some point, was a choice that people made, to do some bidirectional processing and then also some generation.

*"Okay, maybe one last question."*

> **Student:** [Ed: unclear — "Oh yeah, my question is just about everything, I was just wondering, like, how does"]

**[1:10:22]** > **Student:** it cost?

Yeah, great question. So the question is about how megakernels work when you have multiple GPUs, and communication, in the loop. We had some very early preliminary work about this — turns out you can also fuse the NCCL calls into the megakernel, if you set it up correctly. I think we haven't found a really great killer use case for that yet, where sometimes you're just bound by the latency of the NCCL call itself. So these are also pieces of things that you can fuse. I think when [Ed: unclear — "DeepSeek 4"] came out, the DeepSeek folks released a megakernel for the mixture-of-experts inference layer, that you can run just for that, where they actually did fuse some of that communication. So I think what

**[1:11:09]** we are starting to see — what you're starting to see more of is, as you get to more of these models, you'll have a little megakernel for part of the computation, but you won't necessarily have a megakernel for the whole model, unless, of course, you pay the blood, sweat, and tears price, and then you really get the whole thing going.

*"Okay, I think that's all the time we have. Let's thank Dan again."*

Thanks so much for having me.
