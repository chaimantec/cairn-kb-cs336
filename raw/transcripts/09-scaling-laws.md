---
title: "Lecture 9: Scaling Laws (Basics)"
lecture: 9
video: https://www.youtube.com/watch?v=Q15rhEWZPQ4
source: copy-edited from the YouTube auto-captions
verbatim_original: original/09-scaling-laws.md
material: ../slides/09-scaling-laws.md
---

# Lecture 9: Scaling Laws (Basics) — transcript

**This is the edited transcript.** The auto-captions have been repunctuated,
segmented into sentences, stripped of filler ("um," "uh," "you know," "sort
of," "kind of," "right?," "okay"), and had mis-heard technical terms and
proper names restored. No content was added, removed, or reordered, and
every `[MM:SS]` / `[H:MM:SS]` marker is preserved in its original position —
content that was under a marker is still under that marker. The verbatim
captions are kept at
[`original/09-scaling-laws.md`](original/09-scaling-laws.md); that file is
the record of what was actually said, and this one is the readable version of
it.

Terminology was cross-checked against
[the lecture's slide deck](../slides/09-scaling-laws.md) (`lecture_09.pdf`,
"Scaling Laws (Basics)"), which is the authority for paper and author names,
model and architecture names, and the exponents and constants quoted from
Kaplan, Hestness, Rosenfeld, Hoffmann, and the other cited work. "Slide N"
below means PDF page N of `lecture_09.pdf`, per the slide file's own
numbering note — this deck prints no folios either.

One caveat on the deck as authority: slide 52 itself is internally
inconsistent, printing slope 0.79 inside the bottom pathway's fitted equation
and 0.78 in that same panel's own title. Where the lecturer states either
number, it is left as said rather than forced to agree with one half of the
slide over the other.

**Restorations made.** These are places where the captions produced a wrong
or garbled word (or a word needing only a capitalization/spelling fix to
match the deck's own usage) and the text now reads differently. "Slide"
numbers below are PDF page numbers of `lecture_09.pdf`.

| Caption | Restored | Evidence |
| --- | --- | --- |
| "new P" (0:05) | "muP" | **Deck** — slide 40's own heading is "5. Learning rates: muP and scale-aware LR choices." The captions themselves later transcribe the same term correctly (as "μP") at 49:54, confirming the term. |
| "μP" (49:54) | "muP" | **Deck** — slide 40 spells it "muP" in plain text, not with the Greek letter; normalized to the deck's own spelling for consistency with the 0:05 restoration of the same term. |
| "Branco and Bril" (7:00) | "Banko and Brill" | **Deck** — slide 8's caption: "Log-linear scaling with data [Banko and Brill '01]." |
| "Collobert et al in 2012" (7:00) | "Kolachina et al 2012" | **Deck** — slide 9: "Kolachina et al 2012 – power law relation between data and downstream performance," a machine-translation/BLEU-score study, matching the transcript's own description in the same breath ("machine translation systems so they have blue scores"). No paper by "Collobert" appears anywhere in the deck. |
| "Hassabis et al" / "Hassabis in 2017" (8:30, both instances) | "Hestness et al" / "Hestness in 2017" | **Deck** — slide 10: "Hestness et al 2017... Predictable scaling on many tasks (MT, LM, Speech)," matching the transcript's own description word for word ("speech recognition systems, machine translation systems, language models"). |
| "Hessness" (17:41) | "Hestness" | **Deck** — same paper, slides 10, 11, 18. |
| "Hessness" (34:35) | "Hestness" | **Deck** — slide 32: "[Hestness+ 2017]," the SGD-vs-Adam recurrent-highway-net chart being described. |
| "Barri et al." (19:16) | "Bahri et al." | **Deck** — slide 20: "Some have made the following argument (Bahri 2021)." |
| "data decide" (24:38) | "DataDecide" | **Deck** — slide 23's pasted paper header: "DataDecide: How to Predict Best Pretraining Data with Small Experiments." |
| "E T and others at Google" (33:02) | "Tay and others at Google" | **Deck** — slide 31: "Cross-architecture scaling in Tay et al 2022," the same T5-style architecture-scaling study described in this paragraph (Yi Tay was at Google Research at the time). |
| "performer" (33:48) | "Performer" | **Deck** — slide 31 lists this architecture family capitalized throughout ("Performer Tiny/Small/Base/Large," panel (i) "Performer"). |
| "switch transformer" (33:48) | "Switch Transformer" | **Deck** — slide 31, panel (j): "Switch Transformer." |
| "MOE" (39:57, 40:42 ×3) | "MoE" | **Deck** — slide 36 body text: "With MoEs, we expect parameter scaling to change," and the paper title on the same slide, "...Mixture-of-Experts Language Models." |
| "ETE" (50:41) | "Tay et al" | **Deck** — slide 41: "Tay et al 2023," the upstream-perplexity-vs-downstream-SuperGlue chart (NL12 / NL32-XL) being described. |
| "NL-12" (51:27) | "NL12" | **Deck** — slide 41 labels this exact data point "NL12-" (the rest of the label is cropped in the source image; see the deck's own "Known limits" note). |
| "NL-32 XL" (51:27) | "NL32-XL" | **Deck** — slide 41 labels this exact data point "NL32-XL." |
| "Hoffman" (1:01:26) | "Hoffmann" | **Deck** — slides 51 and 53 spell the name "Hoffmann" ("Hoffmann law," "Hoffmann et al."). Note that the deck's own slides 45 and 50 print the same one-N typo the caption has ("[Hoffman et al]," "Hoffman+ 2022") — the deck is internally inconsistent on this name, and the majority/correct spelling is used here. |
| "isoflops" / "Isoflops" / "isoflop" (1:03:44; 1:06:50; 1:16:03 ×4) | "IsoFLOPs" / "IsoFLOP" | **Deck** — slide 48's heading, "Method 2 - IsoFLOPS," and the section index's "IsoFLOPs" (the deck itself varies between "IsoFLOPS," "IsoFLOPs" and "Isoflops" across slides 48/55/section-index; "IsoFLOPs" is used here as the more common of the deck's own forms). |
| "Y Yi" (1:07:36) | "Yair" | **Deck** — slide 51's byline: "...Ludwig Schmidt† Yair Carmon*" — the asterisked (corresponding) author of the very paper named in the same sentence ("Resolving Discrepancies in Compute-Optimal Scaling of Language Models"). Also matches the captions' own later, correctly spelled "Yair" at 1:11:27, describing the same author. |
| "Pearson and Song" (1:10:41) | "Pearce and Song" | **Deck** — slide 52's pasted paper header: authors "Tim Pearce (Microsoft Research)" and "Jinyeop Song (MIT)." |

**Captions-only fix** (the correct word was already present; only a stutter
was cleaned up — no outside or deck evidence needed, and none claimed):

| Caption | Restored | Note |
| --- | --- | --- |
| "a scaling law proc- procedure" (52:59) | "a scaling law procedure" | The word is restarted and immediately completed in the captions themselves; per the edit instructions, a simply-restarted word is removed rather than preserved. |

**Outside-knowledge restoration** (no deck or caption support — flagged
plainly, as instructed):

| Caption | Restored | Note |
| --- | --- | --- |
| "deep seek" (40:42) | "DeepSeek" | Standard proper noun for the well-known model family; not itself named anywhere in this deck. The adjacent word "for" in the same phrase is left as heard — see the `[Ed:]` note below rather than guessed at. |

**`[Ed:]` notes inserted, and why** (5 total):

- *At 26:09*, "This was kind of recent work with Perseus, one of our
  co-advised students" — the cited paper (slide 25, "Pre-training under
  infinite compute") lists co-authors "Konwoo Kim, Suhas Kotha, Percy Liang,
  Tatsunori Hashimoto"; neither student name resembles "Perseus" closely
  enough to identify which the speaker means, so this is left as heard with
  the ambiguity flagged rather than guessed.
- *At 40:42*, "a deep seek for inspired MOE" — the word "for" does not parse;
  it may be a mis-hearing of "V3" (i.e., "a DeepSeek-V3-inspired MoE"), which
  would match how DeepSeek's own models are named, but this is not
  confirmable from the audio, so it is left as heard.
- *At 41:27*, "you have to you know be very about the combination of your
  batch size and learning rate" — a word (most likely "careful") appears to
  be missing between "very" and "about"; left as heard rather than
  supplying the missing word.
- *At 51:27*, "This is a very common thing that does often correlated" — this
  likely drops a "-n't" ("doesn't often correlate"), which would match both
  the surrounding argument and slide 41's own framing ("downstream scaling
  can often be much less predictable"), but a dropped contraction changes
  the sentence's meaning, so the words are left exactly as captioned rather
  than silently negated.
- *At 1:03:44*, "estimating the trip optimal trade-off between flops to
  parameters" — "trip" does not parse as a word; it may be a mis-hearing of
  "compute-optimal," the standard term used throughout this lecture and
  deck for exactly this trade-off, but "trip" and "compute" are not close
  enough acoustically to justify a silent fix, so it is left as heard.

**Student questions.** The auto-captions run student questions together with
the lecturer's speech, with no speaker-change marker in the original at all
(unlike some other lectures in this course, this transcript carries no ">>"
delimiters whatsoever). Six places are marked here as `*[Question from the
floor: ...]*`, inferred from context — an abrupt topic shift, a first-person
question phrasing, and the lecturer's own "the question was..." paraphrase
immediately after — using the captions' own words with only light
punctuation, never rewritten or completed:

- 10:01 — one question (about where scaling law functional forms come from),
  contained within a single marker.
- 20:01/20:46 — one question (about what "model larger than dataset" means),
  straddling the marker boundary.
- 28:28/29:14 — one question (about a linear- vs. log-scale axis on the
  infinite-compute slide), straddling the marker boundary.
- 52:59/53:44 — one question (about variance and run counts per data point),
  straddling the marker boundary.
- 54:30 — one question (about scaling laws fit directly on downstream
  metrics), contained within a single marker.
- 55:15 — one question (about when scaling laws use test loss vs. train
  loss), contained within a single marker.

**Genuinely garbled passages, beyond the inline `[Ed:]` notes above.** None
were found that could not be handled with either a restoration or an inline
note.

**Verification.**

1. *Timestamp sequence* — 102 markers in each file, identical and in order,
   confirmed by diffing `grep -oE '\*\*\[[0-9:]+\]\*\*'` output against the
   verbatim original.
2. No sentence was moved across a marker boundary; where a floor question or
   its answer straddles a marker, the words that fell on each side of the
   marker in the original stay on that side here too. Checked mechanically by
   per-paragraph word-count ratio against the verbatim original: with the floor-
   question text counted, **no paragraph falls outside the 0.72-1.10 band** and
   overall retention is 84.6%, in line with lecture 8's 84.0% for the same
   speaker. (Counting the question blocks as stripped editorial insertions
   instead makes two paragraphs read 0.66 and 0.68 — an artifact of the checker,
   not content loss, since this transcript puts the question's full text inside
   the marker where lecture 8 used a bare label.)
3. *Number inventory* — every number in the verbatim original still appears
   here, with one deliberate exception, adjudicated in the parent. At
   **[1:13:00]** the captions read "that sort of 2D surface or sorry, 3D surface
   that was proposed"; the speaker corrected himself mid-sentence and the false
   start is removed, leaving "3D surface". Slide 49 confirms Chinchilla's method
   3 fits a 3D surface. Nothing else changed: the only other differences the
   check reported were trailing commas attached to a digit by repunctuation.
4. *Restored proper nouns* — all 20 were grepped against the finished slide file
   and **every one appears there verbatim**, so no restoration in the table above
   rests on outside knowledge. Note one correction to the evidence column: the
   "deep seek" -> "DeepSeek" fix is recorded there as outside knowledge, but the
   deck does support it — slide 13's Epoch Capabilities Index scatter labels a
   point "DeepSeek-R1".

---

**[0:05]** Okay, we will get started. We're now leaving — well, temporarily leaving — the land of systems to talk about more deep learning stuff. We're going to have two lectures on scaling laws and scaling-law-adjacent phenomena. Today is going to be the basics — we're just going to talk about pretty basic scaling law stuff, some of the classic works, how the basic idea of scaling laws connects to maybe things you know about from machine learning 101. And then in two lectures, we're going to do the more advanced version of the scaling laws lecture, where we're going to go into a bunch of modern open-model tech reports — things like muP and other parameter initializations, and this year I'm going to throw in some more stuff on optimizers. So there will

**[0:51]** be an advanced version of this that will try to really get to the cutting edge. Today we're going to start with all the really basic stuff. And due to some scheduling constraints, next lecture will be inference. So you're going to go back and forth — you're going to do scaling laws, then inference, then scaling laws again. Scaling laws are really kind of the following scenario, or at least it's a scenario you run into very quickly. You have your very wealthy friend who has given you 10,000 B200s for a month, and he or she has asked you to build a very good open source language model. They would like to get something out of those B200s. So you've already put together your infra team — that's your assignment with your buddies — and you have, let's say, a good

**[1:37]** pre-training dataset — that's going to be the assignment after this, but let's say you already have that. And then you're going to go train a big model. But there are lots of choices in training a big model — that's the earlier assignments, the lecture two stuff we had earlier, where you have questions like: what architecture should I use? What should my hyperparameters be? And it's very scary to run these things on a run that is potentially millions of dollars in cost. So how do you really take this idea of scaling up seriously? How do you make sure that your big run is actually successful? Some of these many choices you're going to just copy out of the literature — the stuff I talked about earlier in the architecture lecture is really about adopted best practices, and you

**[2:23]** will probably just pick some of these out of a hat — that's fine. But if you're at the frontier and you'd like to build something that's actually better than the best available models today, you have to start really optimizing these. You can't just copy the choices of others and get something better than the state of the art. So how do we do that? Well, that's going to require us to really engage with this idea of scaling. Scaling laws have been one very powerful tool — you might also call it a paradigm. I think if you talk to some people who are really in these big labs doing scaling work, it's almost kind of a way of life — they really believe in the scaling laws. It's almost a belief, and you'll see why scaling laws can sometimes be quite tricky objects.

**[3:08]** Really, scaling laws are these simple predictive rules for how to go from small-scale model performance and behavior and extrapolate them up to large-scale behavior. The basic, naive way to approach model training might be to say: I have a big budget, I'm going to do multiple training runs at very big scale, and I'm going to tune hyperparameters on those big runs. But that's very wasteful. So instead, what I'd like to do is all of my optimization at small scale, and have some simple rule that allows me to extrapolate small-scale behaviors to large-scale behaviors. And if small scale and large scale are connected by some very simple, robust regularities of some kind, then you'd have confidence in this new scaling approach to

**[3:55]** optimizing your system, and that's really what today is going to be about — this engineering view of a scaling law. To start, I'm going to talk a little bit about background, because I think scaling laws are very interesting in that they're not only kind of the newest paradigm — that's kind of how a lot of these models are designed and thought of — but they also connect very closely to very classical ideas. So if you're a machine learning theorist, there are some things you'll really feel at home with, in some ways — they're kind of like empirical sample complexities. And then I'll talk through the history of scaling laws of one kind and show you that this isn't something that's really new — it's something with quite a bit of history and research

**[4:41]** done to it. I really had to put some math into my slides — I really felt the need to. But I think one of the things that's very interesting is that for the longest time, machine learning has thought about this question of how will my model perform — especially from a theory perspective. I have this model class; how good is my model going to be? A generalization bound is kind of the theorist's answer to this question. You have your error over some finite hypothesis class, and you say, my error is going to be at most this much worse than the model's performance on my training set — this is a classic generalization bound. You can write this down for more complex classes, like smooth densities, or whatever. But importantly, not only does this tell you some sort of

**[5:27]** performance — notice that these bounds are often dependent on the sample size. So this is telling you: this is a theoretical upper bound on my loss values as a function of how my training set grows. And in some ways that's very closely related to this idea of what happens as I scale up the amount of data I have — those are closely related questions, or you might just believe me that that's true. And if you try to figure out what the very first scaling law paper is, it turns out you can go back quite far — you can go back to Bell Labs, and Corinna Cortes and Vladimir Vapnik and these folks who were doing very serious theory work on machine learning, asking this question: training classifiers on a really large dataset is often very expensive. We

**[6:14]** would like to avoid doing that, and to do so, maybe we can just fit classifiers on a smaller sample, fit a curve to how their error rates decay, and use that as a way to estimate their performance. That's almost literally a data scaling law, back in 1993. So this idea of, at the very least, thinking about how your model's performance changes as a function of dataset size is just a very classical one, and there's a lot of work that supports this kind of idea. I just want to connect this to some of these older ideas so you don't think of scaling laws as this very new, purely neural thing. I think it's an idea that has a lot of roots in empirical machine learning, and also connections to theoretical machine learning. And if you look at the history of this, there's a lot

**[7:00]** of people who have said: isn't it the case that maybe, instead of spending money on algorithm development, we should just go collect more data? Because if we plot how the performance of our systems behaves as a function of the amount of data we have, it turns out they're all increasing, or improving, in predictable ways. Banko and Brill, I think, is one of the canonical NLP references for how thinking about scaling is a really valid way of improving system performance. There's lots of ways of thinking about this, and I think there's also work by other NLP folks, like Kolachina et al in 2012 — one of the earlier works on different kinds of functional forms for scaling laws, where once again they're

**[7:45]** increasing the amount of data they're using to train — in this case, machine translation systems, so they have BLEU scores. They're asking what the right functional form is for how a model's performance behaves as a function of increased data amount, and — maybe surprisingly, or not — they end up with the same power laws, power three and four, that we really use today. For the rest of the lecture, you're going to keep seeing these polynomial functions, power three and power four, as I talk about scaling laws. We kind of knew about a lot of this back in the 2010s — that these kinds of functional forms were very predictive of model behavior as we scaled them up. And then finally, I really

**[8:30]** like mentioning this paper because, for whatever reason, Hestness et al don't get cited quite as much as they should. I think the origins of neural scaling I'd point back to Hestness in 2017 — really a very forward-looking work in many ways, that built these scaling laws as a function of dataset size for a wide variety of systems, like different kinds of speech recognition systems, machine translation systems, language models, and showed that these things follow nice polynomial trends across a really large number of domains. It was really ahead of its time, because 2017 was long before some of the earlier OpenAI scaling laws and so on in the 2020s. And they talked about stuff we talk about today, like emergence.

**[9:15]** Like where suddenly models might show certain kinds of capabilities, because accuracy is a much more discontinuous measure than loss. And, really, scaling by compute — if we have systems that scale predictably with large training data, then compute is going to be really important. And finally, since we're scaling by compute, speed and systems optimization is going to turn into accuracy. So in some ways, a lot of the things we see today, like the phenomenon of system scaling and so on — we could have known, or I could have known, in 2017, had I read and thought about a lot of these scaling law papers back then and thought about them more seriously. So I like to mention that paper in every one of these scaling law lectures I give, just

**[10:01]** to say that a lot of these phenomena were known far before the modern era, or the modern language-modeling era, and that it's possible to have known the regime we're in even before seeing these large language models. I think that's very cool to know. Any questions about this semi-historical part? Yeah.

*[Question from the floor: It was — and I'm wondering if those are just found empirically, or is there some kind of way we can justify all of this?]*

You're asking about these scaling laws and where they come from — these are sort of pure curve-fitting exercises. I'll talk a little bit about where scaling laws can come from, and why polynomials are arguably a very natural class of scaling law, but I think

**[10:47]** there's no golden rule that these are the only ones. I will say, though, that in some places theory provides a very rich place where these scaling law functional forms can come from, because theory is often thinking a lot about how error rates decay as a function of various objects, and they give you candidate functional forms of various kinds. Physicists are also very good at this, because they like thinking about limits — how do the limits behave — and that gives you different scaling law functional forms. Any other questions? Good. So now I'm going to start talking about more modern neural language model scaling, and I'll start with data scaling laws, because to me they're the most simple and natural objects.

**[11:33]** Talking about data scaling laws also lets me walk you through, step by step, how we might guess what the functional form of a scaling law is, and why the polynomial form is a natural one. And then I'm going to start talking about hyperparameters and architectures and all sorts of other, more exotic, objects that are going to be harder to justify from intuition but still turn out to have very nice scaling law behaviors. I think one of the things that's remarkable about this scaling-law-style analysis is that these power law relationships seem to hold for a huge variety of different factors. The basic ones that we see in pre-training, and the ones I'm going to talk about today, are the usual ones

**[12:19]** that you see: compute on the x-axis — log compute, sorry, on the x-axis, log of test loss on the y-axis; or log of the dataset on the x-axis, log of loss on the y-axis; log of parameters on the x-axis, log of loss again on the y-axis. So we can get a variety of different x-axes that you might think of as resources, on the log axis, and all of them linearize log test loss. You can also see much more exotic scaling laws — scaling laws that are basically the equivalent of these but for downstream benchmarks. In those cases you expect to see a sigmoid functional form as a function of compute, or compute-like quantities. There's even been, more recently, a lot of work on the forecasting side, where people are doing

**[13:04]** things like putting dates on the x-axis, and strangely enough — at least for these plots — the upper envelope of these curves is often linear in various forms of capability. There's no reason that, on this bottom side, the more exotic scaling laws need to be true, but it turns out to be the case that language model performance is often much more regular as a function of scale and resources than it initially might appear. So, to begin with, I'm going to start with data scaling laws. Data scaling laws are a very simple, univariate relationship that we're going to think about — in this world we're going to fix our model training procedure, and in general, unless I say otherwise, the models are going to be bigger than the dataset

**[13:50]** size, and I'm going to increase my dataset size in a regular way and see how the error reduces. What we expect out of this relationship, in general, is that it's going to be monotone-ish, if we tune our hyperparameters well — because more data should help us do better at our task, the error should decrease. And if you're doing something like classification, or even next-token prediction, you should go from random guessing to some sort of entropy-like object, where you can't do any better than the irreducible error of your task — that's the noise floor. So we're going to have some sigmoidal-like object here. This is generally what we expect out of our system. So we're going to start with an empirical observation, and you

**[14:35]** can, and should, make this on your own — you'll make something that looks roughly like this in parts of assignment three — which is: if we just have a very big model, much bigger than our dataset, and we increase the dataset size, put that on the log x-axis, and on the y-axis have log test loss again, you'll find that the points you plot form a very clear linear trend. I've taken this from Kaplan in 2020 — I'm going to refer repeatedly to this paper, OpenAI's neural scaling laws paper. They have a lot of lovely ablations and a lot of lovely data. Now, this relationship, where I've plotted a line on a log-log plot — this is a scale-free, or power law, relationship.

**[15:22]** And what does that mean? If I have a line on a log-log plot — to many of you this is probably obvious, but just to emphasize it: if something's linear on a log-log plot, what does that mean? It means that the error I have is decaying polynomially. And it also usually means I'm very far away from my asymptote, because once I approach my asymptote I'm going to taper off rather than have a line. So let's go through an example, just to show you how a scaling law arises very naturally on a very simple estimation problem. Forget language modeling for the moment — this was a language modeling class, but now it's a statistics class, for the next one slide, or actually two slides — no, maybe three slides. In this case, what I want to do is estimate the mean

**[16:09]** from a bunch of data. I have an input, it's drawn from a Gaussian, and I'm going to estimate the mean of this object. I just write down the empirical mean, and then I can write down what the error is. What's the expected error? Well, each of these is Gaussian, and I know that mu-hat is Gaussian, so I can write down the error — it's going to be sigma squared over n. So, if you look at this, this is kind of a scaling law — I've written down the error, and the error, if I take the log of it, is a function of log n, log n being the log x-axis. In general, if you can come up with anything that's essentially 1 over n to the alpha, plus some constant, when you plot that on a log-log plot, subtracting out the constant term, you're going to get a scaling law. So anything of this form, where

**[16:54]** you have polynomial decay in error, is going to give you a scaling law. And of course, in parametric estimation, like mean estimation or regression, you'd expect a polynomial rate — you don't really expect anything else. And if you've taken your stats estimation courses, you know that essentially all the classical models are expected to have a 1-over-n scaling — if you fit a regression or so on you might get d over n, or whatever else. So if this is true, and this is the origins of scaling laws, if we plot our lines we should expect to see roughly a slope of minus one, because that's the exponent on the x — y equals minus x plus c. If we fit our neural scaling laws, we'll find that our exponents are quite different — these exponents

**[17:41]** I've taken these two on the left from Hestness, and the one on the right from Kaplan — they're about negative point one, negative point three, negative point one, roughly speaking. So what does that mean? It means this is much slower than estimating a mean or a linear regression — a much, much slower convergence rate. Still polynomial, but much slower. This is kind of interesting — it's saying we're getting these nice polynomial rates, but the exponents aren't so great, they're negative point one. Where might you get an estimation rate like this? Well, you'd get this if your model was more non-parametric. I promise this will be the last statistics slide of this lecture. Let's say that, instead of a mean, I want to estimate an arbitrary smooth function. So, one

**[18:28]** thing I could do — if I'm doing a regression problem, I have my inputs x1 through xn, let's say they're in the unit box, I have a little bit of Gaussian noise, and I have an unknown function f that I'd like to estimate. A trivial estimator for this is: I can take my space and cut it up into little 2D boxes of length n to the negative one-fourth. What's our estimation error? I'll have square-root-n boxes, each box gets square-root-n samples, and the error is going to be roughly one over the square root of n, or something. In general, if I have a D-dimensional function I'd like to estimate, in this very non-parametric, arbitrary way, my error is going to be roughly equal to n to the negative one over D. So my scaling is going to be y equals negative one over D times x. And now these

**[19:16]** kinds of more flexible functions — functions more flexible than the linear class — now have rates in their scaling laws that aren't just one, they're one over D. So one mental model you could have is that the neural networks I was showing you in these slides are behaving kind of like non-parametric regressors in 10 dimensions, like a nearest neighbor or other kinds of estimators in 10 dimensions — that's roughly the rate at which they learn from data. That's a cool thing to be able to know just by looking at some of these scaling laws. Some people have argued this more strongly — Bahri et al. and some other scaling law theory folks have argued that the scaling law exponents are actually literally telling us that the neural network is behaving like a non-parametric smoother, and so on and so

**[20:01]** forth. It's an interesting set of arguments, and you can buy that intuition to whatever extent you'd like — I don't quite know how much I truly buy this argument. Some of the evidence might be a little sketchy — it relies on estimators of intrinsic dimension. But it's an interesting thing to think about, that somehow these exponents are telling us how fast these neural networks are really learning. I'll pause for a moment here — that's really the end of the really basic data scaling law, of one distribution that you sample from and learn from. Is that all clear? The examples and so on? Good. Oh, yes, question.

*[Question from the floor: You said the model is always larger than the dataset size — do you mean, like, one-to-one, like number of parameters, number of tokens—

**[20:46]** or what do you—]*

Yeah, that's a good question. The question was something like: what do I mean when I say the model is larger than the dataset size? What I mean is, when the model gets small relative to the data — you have way more data than model size — you enter this irreducible error regime. You've basically fitted the data as well as you can for your model class, and you can't do any better no matter how much data you add. So when we're thinking about scaling laws, we usually want to think about this power law regime, which is the simpler case — either that, or you explicitly fit the asymptote and correct for it. Those are the two things. So when I say the model is larger than the data, it can be basically any amount larger than the data, as long as it's in this power law regime — usually, 10 times bigger than the data will do it.

**[21:33]** Now we're back to other data scaling laws. Data scaling laws by themselves aren't quite as useful as you'd like — they just tell you how fast your model learns, which is useful for forecasting but not for much else. If you want to do engineering, you're probably interested in other questions, like: what's my optimal data mixture? What's the best way for me to pick my training data? A very useful engineering question. Should I repeat my data, or not repeat my data and save the compute for something else? Or maybe I should repeat some high-quality stuff and not repeat the low-quality stuff. I could do various other things

**[22:19]** with our prediction — there are a lot of different engineering decisions I can make with data. So how can scaling laws help us make some of those decisions? I think one of the things that's interesting is if we think a little bit about how classical models behave. Data scaling laws, if we think about their origins as essentially empirical versions of generalization bounds, tell us that dataset composition, for many models, really affects the offset of these scaling laws, but their slopes are actually determined by the model class — they're not really determined by the distribution itself. So the slopes might remain the same, and only the intercepts might change. And the intercept might actually be really interestingly shaped. This is a toy model — a linear regression with two different things you can sample

**[23:05]** from — if you get only one kind of data, the errors are high no matter what. You actually want a mixture of both. So if you write down how this intercept behaves, you start to get some very interesting insights about how having more data diversity is very helpful. But what does this practically mean? Practically speaking, it means you can fit data scaling laws on mixtures, and that can help you do pre-training data optimization. So what does this mean? Let's say you have two data sources, for simplicity — you have news and you have Wikipedia, and you want to know how much news to use and how much Wikipedia to use. How can you determine that? Well, you can train really small models on really small

**[23:51]** amounts of data, and then fit a function of how different mixture levels affect your performance. You can do this while you scale up your models little by little, see how the trends change, and try to extrapolate that out the exact same way you'd extrapolate a data scaling law — and then ask which one will be optimal if I keep scaling it out to my full training run. This is a very simple idea: fit a functional form at a small amount of compute, find the minimum, and then scale that out. This is the data mixing laws paper. Unfortunately, if you talk to anyone who's done a lot of this data-mixture work, they'll tell you reality is a lot more noisy than this ideal world would suggest.

**[24:38]** And really, what has happened, as far as I know, in many cases, is you end up training a bunch of small models, picking the best data mix from the small models, and just scaling that up — no scaling law required. DataDecide, if you're interested in this kind of thing, is a nice, large-scale empirical study of these data mixture questions. What they found is that if you don't fit a scaling law and instead just pick the best data mix at small scale, that works well. For what it's worth, that's consistent with the argument that the intercepts differ but the slopes don't change — because if the slopes don't change, the best mixture at small scale is also the best mixture at large scale. Another really interesting set of

**[25:23]** questions I'll mention in the context of data is repetition. It's increasingly the case that compute is growing and the amount of data we have is not growing, so a lot of people are interested in what happens when you repeat data more and more times. There was a nice study a few years back called Scaling Data-Constrained Language Models, where they roughly show that up to about four epochs, with standard training recipes, you just don't get hurt at all. But if you go past that point, your realized scaling law — this dark curve — is much worse than the projected scaling law you'd get with fresh data, that's the dashed line here. And you can also write down a modified functional form that's generally predictive of the

**[26:09]** behavior of this scaling law under repetition. And we can take this idea even further. This was recent work with Perseus [Ed: name unclear; the cited paper's co-authors are Konwoo Kim and Suhas Kotha, and neither closely resembles "Perseus"], one of our co-advised students, where we were asking: what happens if you take this idea to the extreme, and just consider infinite amounts of compute? You're allowed to epoch as many times as you want, an infinite number of times — what's the best thing you can get out of this system? It turns out you can't just keep repeating passes over the data, and you can't keep making your models bigger — those have diminishing returns. So you end up reaching for other things, like ensembling your models, to try to squeeze more and more out of your data. But really, the one thing I'll point out that's interesting is: your standard data scaling law is here, in red — it's a very nice, predictable improvement in performance as you

**[26:55]** increase the amount of data. We do all sorts of interventions, like regularizing and adding ensembles — you get improvements in performance, but the slopes look surprisingly similar. This is a lesson you'll learn once you start fitting your own scaling laws: very often your slopes don't change, often the interventions you do just change the intercept of the scaling law. One last thing I'll mention, because I like this paper, in the context of scaling laws and scaling in general, is that a lot of the phenomena we care about in data are actually very scale-dependent. One example of this is data filtering. If you or I, tomorrow, decide to filter some data for the models we're training, we would probably filter very aggressively,

**[27:41]** because we would only keep the highest-quality stuff — neither of us has very much compute, so we can't train on all the internet anyway, and we might as well filter down to the highest-quality stuff. On the other hand, if you have a lot of compute, you want to train on more and more stuff, because you don't want to repeat this very high-quality data. So as you get more and more compute, your filters become looser and looser, and potentially you start training on stuff that's lower and lower quality. A lot of the things we often think about statically, like data filtering or the quality of data, are actually much more dynamic — as you increase scale, you have to think about where you're going to get the rest of the data. The filters can't stay fixed as a function of the data, and certainly the optimal filters turn out not to be fixed as a function of scale.

**[28:28]** So, that's it for data scaling laws — this is the part I think is most straightforward to understand. Even as a stats/ML-minded person, this stuff is all very simple and straightforward to me: you increase data, performance gets better in a very predictable way. I think this is uncontroversial — the fact that it does so polynomially for such a big model is still kind of cool, but nothing really crazy. So hopefully it's reasonable to all of you that there's so much predictability when you increase the amount of data. Oh, yes.

*[Question from the floor: So, on the slide for the pre-training under infinite compute — that graph on the right is linear in both the loss and

**[29:14]** dataset size, whereas the previous example I gave was log-log scale. I was wondering why it's linear, though.]*

Yeah, so this is — I mean, bad axis, Herman and I just talked about bad axes like this. This is actually doubling, so it looks linear but it's actually log scale — you fixed it now? You fixed it now? Okay. And the y-axis here, it doesn't look logged, but the range is so small that linear and log are basically the same. If you look at the fitted functional forms here, these are still polynomials — we're fitting the same functional forms, and those are the ones plotted here. This is another point I'll get to at the end of the lecture — if you only have a tiny slice of a compute range, it's very, very difficult to tell if something is scaling polynomially or if it's scaling

**[29:59]** exponentially — because Taylor approximations are a thing, everything looks linear if you zoom in enough. So whether this is polynomial or some other functional form, you always want to be a little careful and skeptical. All right, so that was scaling laws for data — in many ways straightforward. Now I want to talk about more exotic forms of scaling, scaling laws for model engineering of various kinds. What we're going to do now is try to design large language models. Maybe you're a radical, and you believe LSTMs are the future — why can't I use an LSTM instead of a transformer? I want to blow my B200 run on my LSTMs. Or,

**[30:45]** maybe you're a different kind of radical and you want to train with SGD — why can't I train with SGD? You might also wonder: if you have a limited amount of resources, you have different ways to spend it — maybe you should train models longer, maybe you should train bigger models, maybe you should go collect more data. There are all sorts of tradeoffs always at play, and scaling laws give us quantitative ways of making these tradeoffs, hopefully. I'm going to start off with some simple hyperparameter questions — things like, how do I choose my architecture or optimizer? Things I talked about in the second lecture, where I gave you the perspective of studying what other people do. Now I'm going to try to give you first-principles answers to those questions, with a bit of a different set of tradeoffs. So,

**[31:30]** you have to believe in scaling laws in this lecture, but as long as you do, you get to try things on your own and find out. The very first question we might start with: are transformers really better than LSTMs? LSTMs can work okay too — they'll fit distributions. With the advent of things like Mamba, we know SSMs can work. So are transformers better? The brute-force way to answer this is to train an LSTM at GPT-3 scale, or even bigger. The scaling law way is to train a bunch of smaller models — train my transformer across a variety of compute ranges, and train different LSTMs with different numbers of layers, again across a variety of different compute

**[32:16]** ranges. So what does this show? LSTMs have a definitely different intercept, maybe even a different slope, than the transformer, and because of this you probably don't want to pick an LSTM — this plot would justify scaling up the transformer instead of the LSTM. And if you look at a lot of the architecture papers today — go look at the Mamba paper, or the gated DeltaNet papers — they'll always have a plot that looks like this, with the vanilla transformer and "our really cool model," and our really cool model is usually either on top or below, because that's what would prove your model is doing better. And you certainly don't want something where your slopes are worse, because that means as you scale up larger and larger, your model will eventually do worse. So this is one way of reasoning about architectures

**[33:02]** without training gigantic models. There haven't been, I think, quite as many recent, good architecture scaling law papers. The one that's relatively recent, and that I like quite a lot, is by Tay and others at Google, back when they were publishing this kind of work — they did a whole bunch of scaling studies on T5-style models, training larger and larger variants of T5 on many different architectures. One thing that's really cool, and one of the reasons I still bring this paper up, is that they capture exactly the architecture changes we implement today in our frontier models. Things like,

**[33:48]** Performer, which is an efficient attention that doesn't scale very well — we don't implement those. The things we do implement are the gated linear unit — red line better than green line throughout the scaling trends. Switch Transformer — I don't know what happened with their biggest run, but generally speaking it's got good scaling trends. And — oh, I forgot — mixture of softmax. We don't do this anymore, but it's probably an effective intervention given the scaling trends. So you see, through the scaling trends, even though a lot of these papers were operating at much, much smaller compute scales than what we operate at today, they're able to see the kinds of architecture trends that were driving frontier model development back then. And I think this is why a lot of people use scaling laws as

**[34:35]** almost a paradigmatic way of saying, "if it doesn't show up in the scaling law, it's not a good intervention" — because, in some ways, a lot of the past work has captured what we see today. Going beyond architectures, we can extend this analysis to all sorts of other things we might want to understand. We might ask: is SGD better or worse than Adam? If you do the scaling law analysis — this one's by Hestness — you'll see very clear trends. This is, once again, the really mysterious thing: the intercepts are different, but the slopes are very similar. If you fix the data and fix the models, the slopes are often very, very similar — I'm surprised by this every time I see it, and yet it's very true. It's rare to get different slopes, even

**[35:21]** with an intervention as big as SGD versus Adam — if you were the one training with SGD or Adam, you'd think that's a huge change, and yet the scaling trends remain roughly the same. In this case, this is language modeling with a recurrent highway net. Similarly, in lecture two, I told you about aspect ratios — how do you pick your depth-to-width tradeoff? And I told you, well, you can just pick some roughly reasonable number, like four times, as a reasonable multiplier. But if you were doing this kind of scaling analysis, you could know much more precisely what's happening. So the first thing you could do is look at scaling trends as a function of layers, and you'd immediately see that having extremely few layers is very, very bad for your

**[36:07]** model — with one layer, you're not going anywhere, that's a terrible scaling trend. Beyond one layer, it's surprisingly more competitive, although you do see that, at every compute level, more layers, at least in this case, is better. And you can do a much more fine-grained study, which they do in Kaplan, where you try to identify what you might call scale-invariant quantities, like the aspect ratio. The number of layers is not a scale-invariant quantity — as you make your model bigger, you do want more layers. But as you make your model bigger, maybe the optimal aspect ratio should stay the same — that one is scale-invariant. So if we look at the aspect ratio, we see that, at different model sizes, we get basically the same minima, around 100 d-model per layer,

**[36:54]** or maybe a little less. And it does shift a little — there's a shift toward a smaller aspect ratio for deeper models, but for the most part the minima stay roughly similar. You can do similar analysis for things like attention head dimension and others, and if you look at the Kaplan paper, they do quite a good job at all of this. This is one way you can be confident about your scaling strategy — if your strategy is "I'm going to fix my aspect ratio and scale up," you can make plots like this and convince yourself you're probably good, because your optimum isn't shifting too much as you go to larger and larger models. One thing that's important — and this is a huge can of worms, a sufficiently big can of worms

**[37:40]** that I'll talk about it on more than one slide — it will come back. Not all parameters are created equal, and because not all parameters are created equal, your scaling laws may look good or bad depending on how you define what a parameter is. In the Kaplan paper, they made this observation: if they drew their scaling laws for depth with the embedding parameters included, they got these very funky-looking scaling laws, and they said, "oh, this is no good at all." Because it's no good at all, they decided to exclude all the embedding parameters and only count the non-embedding parameters, because you could justify it to yourself by saying "these are the parameters that are doing computation, or whatever." We're going to see later that this has non-trivial

**[38:26]** implications for the validity of some of the results. But the point here is, scaling laws aren't magic. Percy likes to make this point, that scaling laws — and this kind of predictability across scales — is engineered, it doesn't happen automatically. We need to pick the right x-axes to look at, we need to make sure the hyperparameters are set right, and only under those conditions does it become possible to get predictable scaling across many orders of magnitude of compute. This is kind of what a lot of scaling law researchers do — they think about the right way to get very predictable scaling. Related to this, I think, are very interesting recent papers on scaling for mixture of experts — now that mixture of experts is, you'd say, the

**[39:12]** dominant way of training large models, it's quite important to think about what the value of a parameter is in this new world, where the number of parameters and the number of active parameters are decoupled. There's a nice, clean analysis by some folks at Apple and MIT that shows the scaling trends as a function of both parameters and active parameters. The cool thing you see is, as you train bigger and bigger models — if you increase the total parameter count on the x-axis here and want to minimize your loss — you end up with sparser and sparser models. See how the color becomes darker and darker? Similarly, if you

**[39:57]** look at how the number of active parameters decreases as a function of MoE sparsity, you get a result that I think is somewhat intuitive: you can draw these surfaces for every compute amount, and ask, would I rather spend that compute on active parameters, or on more sparsity? I can vary my sparsity without really changing the amount of compute I'm spending. Given that, you can ask: what happens if I add more "empty" parameters — same active parameters, but more total parameters? As you move from the back of the plot to the front, you see the losses improving. In other words, this is saying that the parameters in

**[40:42]** an MoE that aren't active are still helping you reduce your loss, which is kind of cool, and you can write down the functional forms of this. I won't go into that in more detail, but the point here is that all of these kinds of quantities we care about in optimizing an MoE have predictable scaling and regularities. And the two things I want to talk about now — the two things I now want to talk about — when you're going to train a new big model, most of the things I've talked about are actually fixed. You're probably not radical enough to switch to an LSTM — you're probably going to pick, I don't know, a DeepSeek [Ed: "for" — possibly "V3," i.e. "DeepSeek-V3-inspired," not confirmable] inspired MoE or something. And you're probably going to pick architecture choices that are tried and

**[41:27]** tested — you're not going to throw everything out. But two things you really do have to reinvent, in some sense, are the batch size and the learning rate. If you train deep learning models, you know that you have to be very [Ed: a word, likely "careful," appears to be missing here] about the combination of your batch size and learning rate — you change one, you have to change the other. Now, batch size, we know from the previous lecture, is very tricky for another reason: we want the batch size to be as big as possible, because a big batch size gives us the opportunity for parallelization — remember, data parallel requires a large batch size. So, having said all that, what we really want to know is: how large can we make our batch size before we start to suffer? That's the relevant systems question. And you also want to know how that changes as a function

**[42:12]** of model size. So, given that preface, there's a very important idea, used in a variety of papers and talked about very frequently, called the critical batch size. The critical batch size is roughly this idea: up until a certain point, before you start to get diminishing returns, there are perfect returns to increasing batch size. I might call this the noise-limited regime. In the noise-limited regime, every additional element you throw in your batch reduces the gradient noise in your SGD step, and since you're variance-limited, that reduction in variance is very helpful — it gives you big returns. So this is the regime in which you have perfect scaling — you're doing about as well as you can, given the amount of extra

**[42:59]** examples you're processing. Once you get to a certain point, where you've reached the noise scale of your gradients, you're no longer variance-limited — you're now bias-limited. What does that mean? Well, remember, in gradient descent you're looking at the local structure of your objective — you don't have a global view of where the minimum is. So no matter how low-noise I am, I'm only going to point in this light-blue direction here, and there's always a disagreement between my local descent direction and where the global optimum is. Because of that, at a certain point I'm going to get diminishing returns, because now my limiting factor is no longer variance, it's this bias term. So now I'm bias-limited past this point. And so critical batch size is kind of a rule

**[43:46]** of thumb, you might call it, or a convenient trade-off point — this is the point at which we're starting to cross over from our perfect-scaling regime to our ineffective-scaling regime. It gives you a good rule of thumb for where to set your batch size, if you want it to be as big as possible without suffering huge efficiency losses. Hopefully that idea is clear. Now, critical batch size is a strange object — it's derived from a variety of complicated calculations about the local quadratic approximation to the objective. But let me give you the mechanical steps actually used to estimate this quantity.

**[44:32]** So, first, you pick a target loss — some number you want to hit, and your goal is to hit this target loss as quickly as possible. What I'm going to do is sweep over a number of different batch sizes, and write down the number of steps needed to reach my target loss, and the number of examples needed to get to that target loss. Of course, those two are related by a batch-size factor — the number of steps times my batch size equals the number of examples. Now, through some calculations, the OpenAI folks and others argue that this is the rough functional form we should follow, that the

**[45:17]** number of steps you need and the number of examples you need are inversely related, with S-min, the minimum number of possible steps, and E-min, the minimum number of examples, normalizing these two quantities. So there's a trade-off between the two: if you want to minimize the number of examples, you're going to need a lot of steps, and if you want to minimize the number of steps, you're going to need a lot of examples — that's a very natural trade-off. And if you increase or decrease your batch size, you get trade-offs that go one way or the other. Now, they argue the goal should be to balance these two sides — I want to make sure my number-of-steps term and my number-of-examples term are roughly balanced with each other, and if you solve for that, you get this particular solution: the critical batch size is the minimum

**[46:05]** number of examples I need, divided by the minimum number of steps I need — and both of these are estimated by fitting this to the observed data I have. This balances both sides of the equation: it gives me a little more steps than optimal, and requires a few more examples than optimal, but it balances the two sides, and you can think of this as improving the convergence rate without blowing up the number of steps you need. There's another point I won't belabor, because it gets even more into the weeds — you should really read the critical batch size paper if you're interested — one of their arguments is that estimating this is quite complicated; you can do it by just looking at the ratio of the

**[46:51]** gradient covariance and the squared norm of your gradient. So why is this even in the scaling law lecture? This isn't really related to scaling, so far. The reason it's in the scaling law lecture is: first of all, the critical batch size is a reasonable batch size to pick — it's a batch size that balances convergence rate and the size of the batch. So if I want to pick my batch size equal to my critical batch size, you might ask, how does this scale as a function of my target loss? Target loss can be thought of as compute — if I want a model that's better and better and better, what batch size should I use? And the reason this is in the scaling law lecture is:

**[47:37]** as your loss improves, the critical batch size increases, and it increases in this very predictable way, once again a power law. This is the relationship they argue for — roughly an inverse polynomial in batch size. This is actually quite nice, because it's saying that if you have a really large-scale training run, where you go all the way to the right of this plot, your batch sizes can be quite large. And that makes sense — as you get closer and closer to the minimum, the value of variance reduction increases, because you're trying to optimize finer- and finer-grained objects, and the noise matters a lot more once you're minimizing really tiny differences.

**[48:23]** So that's the batch size story. The other side — remember, the two quantities you're always going to have to tune are batch size and learning rate. The learning rate side of the story is also a little complicated — I'll go into much more detail in the advanced scaling lecture — but learning rates generally shift. I think this is a good mental picture for how to think about learning rates, in the standard setting you start with: let's say you have just an MLP, a standard neural network of some kind. I'm not going to do depth scaling, only width scaling. If I'm doing width scaling, the bigger my model, the smaller my learning rate should be, and the reason my learning rate should be smaller is because I have

**[49:08]** bigger, more parameters, and I'm changing more things at once — maybe I should move less. That kind of makes sense. And in fact there's a rule of thumb that you want to scale by one over the width — a very commonly known rule of thumb, that you want to decrease the learning rate regularly as a function of your width, and that gives you a rule of thumb to scale your learning rate. There's another school of thought, or another set of tricks people use, which is to rescale the network in various ways — change the initialization sizes, and change the step sizes of the optimizer for various parts of the network, in order to force the model to have the same

**[49:54]** learning rate minimum across scales. This is an idea called muP, and others like it. Some people have reported great success with these approaches, others have reported less success. The two basic ways of setting a learning rate for large-scale runs are captured in this thought: one way, you try to estimate what these minima are and predict what your minimum will be — the way the minimum changes is pretty predictable, so this isn't a crazy idea. Or, you can reparameterize your model to try to keep the learning rate minimum the same, and then just pick your best learning rate and go. Those are the two philosophies for picking your learning rate. Both have been applied successfully at scale — there are large-scale training runs that have used both approaches.

**[50:41]** I think, anecdotally, it does seem like more people are favoring the scaling law approach, but both are certainly viable. I'll talk in detail about both of these two lectures from now. Before I get into the last part of this lecture, where I want to talk about not quite pitfalls, but various details of scaling law things — one thing I want to end this section with is the idea that upstream and downstream performance can be quite different. If you look at something like log likelihood — this is from the Tay et al architectures paper — you find that perplexity, negative perplexity in this case,

**[51:27]** and parameters are very correlated — the more parameters you have, the better your model, very predictably. This is a very beautiful linear trend. Now you're like, "okay, this is great, we're going to ship our best model, NL12." It turns out NL12 is not your best model — it was actually NL32-XL, which was a much worse model in perplexity. This is a very common thing that does often correlated. [Ed: likely missing "-n't" — "doesn't often correlate" — the surrounding argument and slide 41's own framing ("downstream scaling can often be much less predictable") both support a negation here, but the word is left exactly as captioned.] This is probably one of the worst correlations I've seen from upstream to downstream, but I think it's very important to remember that scaling laws, generally speaking, are objects you want to apply on the perplexity side — they're very clean, very regular, very predictable. But transfer from perplexity to downstream is a lot less certain than it might initially seem. This is a place where you want to be very cautious.

**[52:13]** As an anecdote, I have former students who've gone and done post-training at various places, and they always complain — they say, "oh, those pre-training people, they hand you this model and say, 'the perplexity is good, it's all your problem now,'" but often the problems started on the pre-training side. So you don't want to be one of those people — you want to really think about transfer as well, not just focus on perplexities. Maybe this is surprising, maybe it's not, but the point I want to drill in here is that before you do your big training run, you should have a rough idea of what you're going to see — not just whether it's going to train at all. In fact, if you do scaling laws, you should be able to predict fairly precisely, almost the numerical values, of how good your run is going to be.

**[52:59]** If I choose one optimizer versus the other, what will the gain be? You should be able to predict that. The scaling-law-based procedure is: I'm going to train a bunch of smaller models, establish some sort of scaling law by fitting these lines onto log-log plots, and then, if the fit is good enough, have some confidence that the gap will persist, and deploy that onto my large-scale training run. This is the naive version of a scaling law procedure, but I think a reasonably accurate one for how you might think about these things in practice — what else are you going to do, just deploy a run out of nowhere? Probably not. I can pause here and take some questions before I get into the last part of the lecture. Yes.

*[Question from the floor: For each of the data points on the

**[53:44]** scaling law, like how do you think about variance? Like how many runs do you do? And is this aggregation like average or do you take minimum or maximum, like that?]*

Yeah, I think in most cases, if not all the cases I've seen, those are singletons. And the reason it's a singleton is because when you're looking at perplexities, they're very, very clean — if you rerun them, the variance is probably in the second decimal place or something, because the training data is very homogeneous, you've got a lot of it, your eval sets are very big — you really push the variance very low on these things. But if you're doing something like, say, learning rate or critical batch size scaling laws, you

**[54:30]** will see some truly horrendous stuff. I do think people do some variance reduction, but it's not actually that common — not as common as it should be, maybe. Yes.

*[Question from the floor: Do most people use downstream metrics directly, instead of using perplexity?]*

Yeah, people do run scaling laws directly on downstream metrics, but that has the same problem as this. So the way to think about scaling laws is to reliably establish at least one thing. Let's say I see something like this — I have strong belief that this transformer line is going to continue, this is a very regular trend, it might curve a little, but we think there's probably some regularity here.

**[55:15]** But instead, imagine this was a very noisy, jagged line of this form — you wouldn't really know what's going on. You'd say, "maybe I believe it will go on, but maybe it's a line, maybe it's a curve." So, if I'm trying to explain the philosophy of this stuff, it's: let's start with a low-variance measurement, establish scaling regularity there, and then rely on some belief about transfer — or establish transfer elsewhere — to show that it would transfer over to downstream. Oh, yeah.

*[Question from the floor: So, some of these are for the test loss and some are for the train loss — when do you do either?]*

I see. Yes. You fit the test loss when you're doing good scientific practice, like Kaplan 2021,

**[56:01]** but realistically, all the models — except for a very few, like the infinite-compute one, and the repetition stuff, unless you're doing some of that — you're doing one-pass SGD. So the generalization gap for all of these models is very small — train and val are very similar. I've seen some pre-training codebases that don't even have a validation loss, because if you're doing one pass, most likely your train and val are very close. That's why a lot of these plots exchange those two, because they're all in the one-pass regime. Good question, though. All right, we will continue to move on — sorry, I moved back too far here. So, now I'm going to talk about one of

**[56:47]** the most well-known uses of a scaling law — even if you might not personally be using this yourself, you've certainly used the rule of thumb at some point. And also, I think this set of things will tell us something really interesting about the actual execution of a scaling law. So — the motivating question is: do I want more data, or bigger models? The resource you have to spend is compute — someone's going to hand you, metaphorically, a certain amount of flops, and you're going to spend them somehow. If you remember Percy's very first lecture, it's roughly: data times parameters is your flops, linear in that. So, do I want more data, or more model?

**[57:32]** We know that if we take a teeny-tiny model and dump tons of data into it, that will just be wasted — that's the dark purple line at the very top here. I've taken a teeny-tiny language model and passed a huge amount of data into it, and this thing has been flat for a very long time — this is a complete waste of compute. You would have much rather trained this big yellow model to the same number of tokens, or even earlier, for the same amount of compute. Now, if you could understand the interaction of how data amount and model size come together to give you performance, you could optimize this very precisely. And so, there are scaling laws that kind

**[58:20]** of are more advanced, and relate the joint behavior of both objects. Almost simultaneously, Kaplan and Rosenfeld both proposed these kinds of functional forms, roughly equivalent, describing how the size of the model and the amount of data relate to the error. Rosenfeld's is very simple — it's really just the sum of two inverse terms, two scaling laws added together. Kaplan's is a little more complicated, but roughly the same idea. If you think about the limits, this is very intuitive: if I have an infinite amount of data, what happens? This term goes to zero — you're basically model-size-limited, you become a pure model-size scaling law. If I send my model size to infinity, then I'm, you

**[59:06]** know, I'm going to be bounded by data — I'll have a pure data scaling law. So it makes sense, if you take the limits of both of these. It's often a good idea, if someone tells you a scaling law, to take the limits of all the variables, to understand the behavior of the system. Now, the thing that's kind of cool is that Rosenfeld and many others who've proposed these kinds of joint scaling laws show that these things fit quite well, even if you extrapolate. What you're going to do is fit on these green points — a low-compute, low-data regime, you never see things with either high data or high model size — and then you can extrapolate, on the basis of these tiny points in this corner, out to the high-compute-or-high-data, high-model-size regime, and you can very accurately

**[59:53]** predict the error — great predictions. And once you have that, you can say: I have a fixed amount of flops, subject to my flops constraint, minimize this. That's a simple non-linear optimization problem, and you can solve for this. Now, both Kaplan and Rosenfeld provide equations of this form — what do they say? Well, if you look at Kaplan, and solve for this equation, they say something like this — well, the amount of — they always flip this on me, it confuses me — the amount of data should be C to the 0.73, and the amount of model size should be C to the 0.27. All right, is that

**[1:00:40]** reversed? I think it's reversed, sorry — N should be the amount of parameters, and D should be the amount of data. So tokens per parameter decreases with C. As you increase the compute amount, this prescription by Kaplan tells you to train bigger and bigger and bigger models. If you were around in the days of GPT-3 — it kind of shocks me to think there are people who weren't — you'd know there was a period where everyone was training these gigantic models, hundreds of billions of parameters, trillion-parameter dense models, what have you. Part of that was driven by this: they saw GPT-3, they saw these Kaplan predictions, and said, wow, I'm going to do my big training runs at these gigantic model sizes. But, a few years later, in

**[1:01:26]** 2022, some folks at DeepMind — Hoffmann and all — came in and said, well, actually, these predictions are all terribly off. You've been training these giant models, these three stars here, but actually those are way too big. What we should be training instead is a very different trend, this line in blue here, and we should be training this teal star, which is a much better model. So they argued we should be training much smaller models, relative to this. And I think, of course, many of you, or all of you, probably know the Chinchilla paper, and what the token multipliers are — it's 20 tokens to every parameter. But I think it's useful to go into this in detail, because the Chinchilla-Kaplan disagreement, and all of this, tells us something important about how we should be thinking about scaling laws,

**[1:02:12]** it's not just something where you turn the crank and get the answer — it's something careful and sensitive, and we have to respect the process. So I'm going to walk through the Chinchilla paper, and after that, talk about why the Kaplan and Chinchilla papers disagree, and what that means for you. The Chinchilla authors suggest three different ways of fitting scaling laws — I really like this, because it's a way of robustifying yourself against modeling assumptions you may have made, and each of these is actually quite a different approach to estimating the trade-off between model size and data. Minus method three, which does differ a little, they mostly agree — methods one and two both agree that the coefficients for data and for

**[1:02:59]** model size are roughly the same — 0.5, 0.5; 0.49, 0.51. Method three does differ, 0.46 and 0.54, but we'll talk about why later. Kaplan et al., of course, have a very big difference between the two, which is why they were training such big models at such big compute sizes. Method number one, from Chinchilla, is what I'd call the lower-envelope method — this is the method that was also being used, partially, by Kaplan, to try to figure out what the right scaling was. What you do is take all of your training runs — each of these colored lines is a training curve — and you take the lower envelope of these training curves, because a lower-envelope point on this training curve means that, for a particular

**[1:03:44]** flop count, this is the best loss I ever achieved for that flop count, on any training run. So I can take all of the lower-envelope points, at the very bottom here, in the high-compute regime, and plot those points to see how big the models were that each of these flop points intersected — because each of these points is a training run with a model size. If I scatter-plot that, I get a nice linear trend, and that gives me one way of estimating the trip [Ed: "trip" does not parse; possibly a mis-hearing of "compute," i.e. "the compute-optimal trade-off," the standard term used throughout this lecture, but not confirmable] optimal trade-off between flops and parameters. That gives one answer: for their compute budget, they should train a 67-billion-parameter model. This is a nice, simple way, but tricky to actually find things like the lower envelope — there are definitely some tricky parts. Method two is IsoFLOPs. This is now very

**[1:04:31]** popular, and also very easy and very robust — this is probably my personal favorite method. What you do is pick a bunch of different flops budgets — a range of flops budgets, going in factors of three in this case, one, three, six. For each flops budget, you sweep the space — here I have one variable, the parameter-to-data trade-off. For each flops budget, I sweep over the parameter-to-data trade-off, fixing my flops — I double my data size, halve my model size, and so forth. That generates a sequence of training runs whose terminal loss sweeps out some sort of curve. Each one of these colored sets of points is a fixed-flops run. Each point is sweeping across

**[1:05:16]** different parameters, and of course it's implicitly sweeping over data too. Now, the nice thing is, I can take the minimum over each of these runs — or even fit a quadratic and take the minimum over the quadratics — and draw a line through the bottoms of the quadratics, plotting them as a function of flops to parameters, and that gives me another prediction, which agrees very closely: 63 billion parameters for my compute budget. Now, the final approach we can take is actually maybe the most natural one, as suggested by the Rosenfeld paper as well as the Kaplan paper, because both of them propose a hypothesized functional form for how model size and

**[1:06:04]** data relate to loss. If you have such a joint functional form, you can take a whole bunch of training runs you've done and fit your loss landscape — your hypothesized functional form — doing curve fitting to estimate the coefficients in your scaling law. This is the most brute-force way: you have a hypothesized relationship between loss, data size, and model size, you train a bunch of models, and do curve fitting — hopefully fairly straightforward, how you do that. Of course, if you're doing this method, how you do the curve fitting is very important — you're fitting these surfaces, there are many variables, it's kind of tricky. This has some trickiness too, just like method one — it's not a straightforward way,

**[1:06:50]** the same way that maybe IsoFLOPs is. Now that we've gone through what Chinchilla does, it doesn't seem materially different from what Kaplan was doing — it's not like Kaplan was doing one crazy thing and the Chinchilla authors did something way smarter and you'd say, wow, why didn't we do that from the start. They both did pretty reasonable stuff. But if we look at their scaling predictions, they're very different. In hindsight, we generally tend to agree that the Chinchilla authors had the more correct scaling — if you do your own Chinchilla-style analysis, you'll find your scaling is probably much closer to Chinchilla. So why is there such a big difference, when they're both fitting these joint scaling laws?

**[1:07:36]** This, I think, is where we get into the messy realities of how scaling laws are made. Scaling laws can really change depending on the precise ways you implement them, the hyperparameter settings you use, and even what the x-axis is. There's a really nice paper — oh, no, I covered the authors, okay, I can paste this in the lecture Slack later — called Resolving Discrepancies in Compute-Optimal Scaling of Language Models. Yair is the last author — he was a former student here. They show, basically: first, they replicate the Kaplan result, using roughly the Kaplan settings. Then they say, okay, let's change how we count parameters — that shifts the curve a little.

**[1:08:22]** And then they say, let's change how learning-rate warm-up is done — that shifts the curve a little more. And let's tune the optimizer a bit more, and then they get exactly the Chinchilla result. So their argument is that there's a sequence of what seem like very minor decisions that, in the end, gives you this big gap between the Kaplan scaling law and the Chinchilla scaling law. I'll explain what each of these steps is — not that any one is the most important single thing, but to emphasize how important the details are here. The very first arrow, where we go from Kaplan to the blue line, is how we count parameters. Remember, in Kaplan, I said they saw weird stuff if they included embedding parameters, so they excluded embedding parameters — that's generally an okay

**[1:09:08]** thing to do. But one thing they also did that really messed them up was excluding the last-layer parameter counts too — the softmax ones — because, in many models, the embedding and the last-layer linear are dual to each other, they have the same shape: the input embedding is vocab size by hidden dim, the output matrix is hidden dim by vocab size. Because they're the same shape, they said, well, let's just exclude both of those. Turns out whether you include or exclude those has a big material impact on the shape of the scaling law. They also found — this is much more of an oversight than a genuine difference of opinion — that a lot of the models being trained in Kaplan were very small; in fact, they were so small that they were not

**[1:09:54]** actually converging by the time the learning-rate warm-up was done. So their learning rates were set very suboptimally, and because of that their models weren't fully converged. And finally, they found that Kaplan et al. was fixing one big batch size, and those big batch sizes were suboptimal for the smaller models — so if you tune that correctly and use varying batch sizes, you get results that agree exactly with Chinchilla. So I think each of these might seem like a very minor difference, but changing up these minor things can produce big shifts in the scaling law. One thing I like to emphasize is that scaling laws are kind of lower bounds, in some sense — they're saying, "if I continue this recipe and scale it up, this is what I'll get."

**[1:10:41]** But if you're scaling up a recipe you don't actually want to scale up — your warm-up is crazy, or your batch sizes are crazy — you're going to get bad scaling laws. So you want to be as close to a proper, full run as possible. There's another paper that does another very interesting thing, trying to study the difference between Kaplan and Chinchilla scaling laws — it's by Pearce and Song. And what they do, which I think is very clever, is they don't train any models — they just take the Chinchilla training curves, or the implied functional form of the Chinchilla training curves, and plot them out, and say: based on these implied training curves, simulated purely from Chinchilla, here's what the implied training curves would be for Kaplan, if you ignore the embedding parameters and scale way down, and what

**[1:11:27]** happens if you use the total parameter count instead. What they find is really two effects. One is that Kaplan is operating at a much lower compute scale, so it's very sensitive to small changes — and, actually, ignoring the non-embedding parameters is a sufficiently big change to mess up the result relative to Chinchilla. So their argument is slightly different from Yair and others — it's that, here, it's the size of the compute scale, plus the slight nonlinearity induced by non-embedding parameters, that's causing this disagreement. This is really starting to get into the weeds, but I hope you appreciate the importance of paying attention to detail — a lot of the work that goes into establishing reliable scaling laws is about figuring out the right regime, so that you

**[1:12:13]** can get reliable scaling across compute scales. The final thing — this is really just something I find amusing more than anything else — is that Chinchilla is this paper that kind of takes down the Kaplan paper, "oh, you guys were very wrong." But there was one remaining mystery: Chinchilla's method three never agreed with methods one and two. The numbers look small, and the authors are pretty unperturbed about this, but I think the implications are quite nontrivial. Methods one and two basically say the amount of parameters and tokens should scale together — there should be a fixed ratio between the two, which is where the Chinchilla factor of 20 comes from. If you look at method three, the exponents are different — this means that if you scale up your compute, eventually, in this case,

**[1:13:00]** you'll have way more tokens than parameters — a very different scaling conclusion, asymptotically, than the other two. It turns out I wasn't the only one perturbed by this — some folks at Epoch AI did something about being perturbed. They couldn't get either the raw data or the code, so what they did was extract the data from the plots in the paper and refit the method-three regressions. They extracted the data from the paper, and given that data, fitted the 3D surface that was proposed. And what they showed is that the original paper must have underfit the original data — and if they redid the curve fitting, they could

**[1:13:46]** get much lower losses relative to the proposed scaling law in the Chinchilla paper. And in fact, if you follow the refitted model, you get almost exactly the D-over-20, the "20" rule of thumb, from the Chinchilla paper — which is really a funny conclusion, I think, to the Chinchilla paper: in the end, it turns out the authors were more right than they knew. They'd made a mistake, and that was the only reason method three disagreed with methods one and two. So it's a nice conclusion, to know the method was robust after all. That's the end of the Chinchilla saga, in some ways, but I'll end this section by saying: you probably don't want the Chinchilla factor. At this point, I think this is a

**[1:14:32]** pretty common knowledge at this point. If you're training a production model, you don't really care much about saving training compute. You can look at all sorts of external surveys people have done of how frontier labs are spending their compute — most of the compute is not going into training runs, most of it is going into R&D and serving. And given that, especially for serving, what you want is small models that are capable — you don't want big, bloated models that cost a lot to serve, even if that minimizes training cost. So what you really want is something you'd call "overtrained" — I put that in quotes, because really, overtraining is what we want, that's the right amount of training. And if you look at all the models that were released, in the early

**[1:15:17]** days, like GPT-3, this was undertrained for sure — three tokens per parameter. Chinchilla gets to the modern ratio of 20 per parameter, and for a while people stayed at 20 per parameter. I think this is the era where there wasn't that much serving of these models — the models were cool, but they weren't being served at scale. Then, as you move into eras where serving becomes very real, you start overtraining these models, and you've moved into MoEs, and you're making all these tradeoffs to optimize inference serving. So I think Chinchilla is important, not because I think the 20:1 ratio is the one golden ratio — if you're doing research, it might be — but because it tells us quite a bit about how we should fit scaling laws, and how to think about scaling laws.

**[1:16:03]** One final thing I'll end with, Chinchilla-wise and otherwise for scaling, is that IsoFLOPs have been one of the things that, as a research tool, have endured. IsoFLOPs are very easy to execute — you fix a flops budget, and sweep over all the other degrees of freedom to see what the surface looks like. This gives you, in general, fairly reliable readings of how different parameters vary as a function of your free parameters. People have done this — actually, one of my students did this for diffusion models. The MoE study I talked about was an IsoFLOP-style design. If you're ever in a situation where you're thinking, "how am I going to decide all these tradeoffs?", IsoFLOPs is always a good default.

**[1:16:48]** So, to wrap up everything for today: there's this idea that we have a log-linear regularity between the amount of resources we put in and the performance we get out. It extends to a wide variety of things — model parameters, compute, the sparsity level of your MoE. And that lets you do a lot of the things I was talking about earlier as arbitrary choices, but in a much more evidence-driven way, hopefully. This allows us to have engineering at scale without explicitly relying on doing big model training runs. Hopefully you remember all the different components here — data scaling is this very natural object, just the exponent is a little strange; model scaling lets you do all these cool engineering things; and finally, scaling is this very cool,

**[1:17:35]** robust predictor, let's call it, for how to do engineering at large scale. Thanks. Next lecture, I think, will be Percy on inference, but after that we'll return to more advanced, niche scaling topics.
