---
title: "Lecture 5: GPUs, TPUs"
lecture: 5
video: https://www.youtube.com/watch?v=izZba4UA7iY
source: copy-edited from the YouTube auto-captions
verbatim_original: original/05-gpus-tpus.md
material: ../slides/05-gpus-tpus.md
---

# Lecture 5: GPUs, TPUs — transcript

**This is the edited transcript.** The auto-captions have been repunctuated,
segmented into sentences, stripped of filler, and had mis-heard technical terms
restored. No content was added, removed or reordered, and every `[MM:SS]` /
`[H:MM:SS]` marker is preserved in its original position — content that was under
a marker is still under that marker. That was checked mechanically against the
verbatim captions the same three ways as lecture 4: the 103 timestamp markers
match exactly in order and form; the inventory of numbers in the two bodies was
diffed, turning up the deliberate 50257/50304 restoration (below) plus a couple
of incidental differences that don't change any figure — one repeated "108 SMs"
trimmed once as redundant at 1:08:17, and "float 32" written as "float32" at
35:24; and every paragraph's word count was compared with its original to catch
content drifting across a marker boundary — all 103 paragraphs landed inside the
0.72–1.10 ratio band. This speaker leans just as heavily on "you know," "sort
of," "kind of" and "right?" as filler here as he does in lecture 4, so this
transcript retains about 84% of the caption word count (13,445 of 16,022 words),
essentially the same share as lecture 4's 84%. The verbatim captions are kept at
[`original/05-gpus-tpus.md`](original/05-gpus-tpus.md); that file is the record
of what was actually said, and this one is the readable version of it.

Terminology was cross-checked against
[the lecture's slide deck](../slides/05-gpus-tpus.md) (`lecture_05.pdf`),
transcribed page by page, which is the authority for hardware terms, part
numbers, number formats, and the equations and benchmark figures this lecture
draws from.

**Restorations made.** These are places where the captions produced a *wrong
word* or dropped a word, and the text now reads differently. Each was confirmed
against [the slide deck](../slides/05-gpus-tpus.md) unless the note says
otherwise. This is the list to grep the deck against.

| Caption | Restored | Confirmed by |
| --- | --- | --- |
| "your map moles are much much slower" / "on something like a map mole" / "this also happens \[clears throat\] with map mole" | matmuls / matmul | context — the word only ever appears where "matrix multiply" already appears alongside it in the same sentence (e.g. "reduce your global memory access on something like a matmul"); no deck string, but unambiguous from usage |
| "structured sparsity and FPA and all these sort of lower number formats" / "with FPA, there's going to be E4M3" / "scale factors are themselves FPA" / "MXFPA is going to use more mantissa" / "once you start getting to these FPA FP4 formats" / "train a model FPA" / "that scaling factor will itself be uh FPA" (7 instances) | FP8 | deck (slide 27, "Very low precision (FP8)... FP8 E4M3", "MXFP8... Scale factors themselves are FP8 (E8M0), 1 per 32"; slide 24, "Number Representation: FP32, FP16, Int8" gains list) |
| "you can also understand how this thing works" — preceding word "MXFPA" (2 instances: "what happens internally when you actually use MXFPA" and "now that you understand MXFPA") | MXFP8 | deck (slide 27 header "Multiple scaling factors MXFP8 (Blackwell)"; slide 28 title "MXFP8 training in practice") |
| "an even faster local memory called S-man or shared memory" | SMEM | deck (slide 13 figure: the TPU TensorCore diagram labels this block "Smem", paired with the Scalar Unit) |
| "I think uh Grok, right? Which was recently acquired by Nvidia, was a design where you just had these giant amounts of SRAM" / "very fancy alternative accelerator like Rock or something which is all SRAM" (2 instances) | Groq | context **plus outside knowledge** — the description (a physical accelerator built entirely of SRAM, useful for inference) rules out the unrelated xAI model "Grok" that this course's lecture 4 deck names in its MoE table, since that is software. But the *spelling* Groq comes from knowledge of the company, not from anything in this lecture: the deck never names it. Treat the identification as an editorial judgement. |
| "as long as it divides by your birth size then you're good to go" | burst size | context — matches the "burst section" / "burst mode" terminology established earlier in the same paragraph and on deck slides 37–39 |
| "when I increase my vocab size to 5257 to 5304... I got a 25% speed up" | 50257 to 50304 | deck (slide 45, pasted Karpathy tweet: "increase vocab size from 50257 to 50304 (nearest multiple of 64)... (~25% speedup)") |
| "if we're getting into really needy-greedy details" | nitty-gritty | context — common idiom, mis-heard by the captions |
| "Chris Ray here has done a lot with, you know, lots of different kinds of structured matrices" | Chris Ré | context **plus outside knowledge** — "here" places the person in the room, and the topic matches Christopher Ré's structured-matrix work. But the deck never names him, and nothing in the transcript gives an affiliation or full name beyond "Chris Ray". Treat the identification as an editorial judgement. |

**Capitalization and formatting only.** The following were already the correct
words in the captions and were only cased or joined to match the deck's own
usage — they are not restorations: "the Jax book" / "this is from the Jax
book" → "the JAX book", "which is, you know, JAX's compiler... integrated into
Jax" → "JAX's compiler... integrated into JAX" (JAX is a proper-noun acronym;
deck slide 3 credits the "How to Scale Your Model" book to Google DeepMind, and
the deck's own URLs, e.g. `jax-ml.github.io`, cap it the same way), "max auto
tune" → "max-autotune" (PyTorch's actual compiler-mode flag name), "Torch
Compile" kept as spoken (PyTorch's compiler entry point, referred to by name
rather than as `torch.compile`), "flops" → FLOPs where used as the unit (deck:
capitalized throughout, e.g. slide 24's "Model efficiency... overall gain >
1000x").

**Four spots marked ambiguous rather than guessed**, per this KB's policy of
not reaching for outside knowledge the deck and context don't supply:

- *At 8:31*, "I don't want you to think of it as like a... set of pieces of
  text on Nvidia as am I" — likely means Nvidia's documentation or website, but
  "as am I" does not resolve to any confirmable phrase; left as heard with an
  inline `[Ed:]` note.
- *At 29:14*, "I think this was uh step one three, uh which is a Chinese open
  source model" — likely a specific named model, but this lecture's deck does
  not mention it and the captions don't resolve the spelling; left as heard
  with an inline `[Ed:]` note.
- *At 46:07*, a student's comment — "A lot of like things like squeeze that we
  didn't talk about on that slide" — names a technique as "squeeze" that isn't
  confirmable from the deck or surrounding context; left as heard with an
  inline `[Ed:]` note.
- *At 1:10:35*, a student asks "Why 108 \[snorts\] birth?" and Tatsunori
  answers "That's just the number of SMs" — the question plainly concerns the
  A100's 108-SM count discussed a few paragraphs earlier, but "birth" does not
  parse as any recognizable word in that slot; left as heard with an inline
  `[Ed:]` note.

**Two exchanges left close to verbatim** rather than reconstructed, because it
is not clear where one speaker's turn ends and the other's begins:

- *At 2:22*, the captions garble a crosstalk moment about the CUDA MODE
  community's rename to GPU MODE — "Um and there's a TPU and now what? >>
  They're GPU mode now. They changed their name." It is not clear what
  Tatsunori was about to say before the interruption (very likely trailing off
  while trying to recall the group's new name).
- *At 54:35–55:20*, a back-and-forth about the DRAM row/column diagram is
  heavily garbled, including a repeated word the captions render as "key" that
  does not clearly refer to anything in the diagram being discussed.

Both carry an inline `[Ed:]` note at the point they start.

**False starts are preserved, not completed.** Where the speaker abandons a
word mid-utterance the transcript keeps the break rather than finishing the
thought for him, because guessing the ending authors content he did not say.
Two such completions were caught in adjudication and reverted at 1:06:45: the
draft had rendered "They're not even divi- They're not even even" as "they're
not even divisible by two", and "no sort of diminishing or sorry, no uh
penalties" as "no diminishing returns — no penalties". The words "two" and
"returns" appear nowhere in the captions; both are now back to the aborted form
the recording actually contains. Neither was needed — the paragraph sits inside
the expected word-ratio band on legitimate restorations alone.

**Caption artifacts dropped.** Four non-verbal sound annotations —
`[clears throat]` (×2, at 24:37 and 1:00:39) and `[snorts]` (×2, at 1:10:35 and
1:13:42) — are caption machinery rather than anything said, and have been
removed. These are the only things removed from the body that were not filler.

**Speakers.** Lecture 5 is taught by Tatsunori Hashimoto. Student questions are
run together with his speech in the auto-captions with no speaker labels; where
the recording makes clear that someone from the floor is asking something, the
text carries a `*[Question from the floor: …]*` marker (or
`*[Question from the floor, inaudible]*` where it cannot be recovered),
following the convention used in this KB's other transcripts.

---

**[0:05]** Okay, we'll get started. Now we're going to start the systems portion of the class. We've been doing a bunch of neural network stuff for the last two lectures, and now we're going to start getting into GPUs, parallelization and inference. Systems, I think, is part of the stack that is most reasonable, in the sense that you can reason through all the pieces and there are logical steps you can go through to get the result. So it's very different and satisfying. But when you first start interacting with GPUs, it will feel a little bit like both magic and like a very strange device you're interacting with. I don't really know, as a kind of class survey, how many of

**[0:51]** you have taken a GPU programming class of any kind? Okay, so this is pretty good — like a fourth, maybe a little bit less. But I think when you first interact with these things, it's actually a little bit of magic, so it's good to understand. And I think, you know, deep neural networks feel like magic, but GPUs can too. Sometimes people will show you plots like this. Part of my promise to you for this lecture is that by the end you will understand what this plot is, or why this plot looks the way it does. This is how much throughput you're getting on a matrix multiply, where the x-axis is your dimension. You might think, well, maybe bigger matrix multiplies are always more throughput, because you have more work to do. Well, you've got all these kind of crazy patterns happening

**[1:37]** where at certain sizes your matmuls are much, much slower than others. Why? You'll need to understand that. You also need to understand some basics of how to mentally think about a GPU in order to build fast algorithms. And even if you're not a systems person — I think probably not all of you, or even a majority of you, are systems people — but if you're thinking about architectures, or just generally reasoning about these things, you need to understand the hardware model of how your model is going to execute in order to make an effective model, right? Thinking back to Percy's initial lecture: a big part of scaling is making sure you use your resources effectively, and without understanding your systems, you will never be efficient at using your resources. And, you know,

**[2:22]** I am not a systems person — surprise. So before I start, I want to make sure to point you towards resources that I think are very good and very useful, and that I used in making this lecture. Horace He has really incredible blogs and explainers of how GPUs work. There's an enthusiast group, CUDA mode, that loves to talk about GPUs and CUDA kernels and so on. And there's — *[Ed: captions have "there's a TPU and now what?" here; this reads as garbled crosstalk, most likely Tatsunori trailing off while trying to recall the group's current name]* *[Question from the floor: They're GPU mode now. They changed their name.]* Okay, fine, GPU mode. And the TPU book — but now it's also a GPU book — by the Google folks. This is a really incredible resource. So if you're really excited or interested by any of the stuff I talk about today, I would strongly encourage you to look at that book. They also have exercises that you can work through

**[3:09]** and your assignment will have things that look a little bit like those exercises, so you can get a preview of what we'll send you later. Great. Okay, so this lecture is going to be in three parts. The first part is really just broad introductory material on GPUs — the hardware model, how the programming of the GPU works. A GPU is kind of a foreign device, and I want you to understand philosophically how it's different from a CPU. Once you do that, we're going to talk about six different tricks — important tricks — to get a GPU to go fast. These are the building blocks for how you can start to manipulate your code in order to make it run efficiently on a GPU. And then in the last part

**[3:55]** where we put all of this together, we're going to walk through flash attention, and this will be kind of our victory lap. We'll say, actually, at this point we understand everything we need in order to reinvent our own flash attention and understand all the different components, right? That's the goal we'll be building towards by the end of the lecture. Okay, so we haven't gone to scaling yet, but from Percy's introductory lecture, or just generally following language modeling, we kind of know that if we have more compute on hand, we can get better language models, right? This is the currency by which we operate. So if we have faster hardware, or better utilization, or more chips, and improved parallelization — all of these can drive progress, right?

**[4:40]** And so in some sense, the game for the last few years has been how can we bring more compute to bear in training models. And I guess, in the last year or so, how can we bring more compute to bear in the inference of these models. So compute is extremely important, and systems is part of that. If we were talking about this in the '90s, the things we'd be focusing on are clock speed — we'd be saying, well, how we make computers go fast is we have a CPU that executes instructions serially, and we make those instructions go faster and faster. This type of scaling, where transistors got smaller and clock speeds got faster — called Dennard scaling — held quite well for a while; really, the 2000s was when this kind of scaling tapped out. And one interesting side note,

**[5:26]** right — the number of transistors, of course, has continued to go up, but making transistors smaller does not necessarily mean your transistors go faster. They don't move at a faster clock speed. There are fundamental physical reasons why you don't actually continue to get benefits from smaller and smaller transistors past a certain point. So this old approach of making your clocks faster, making instructions execute faster, is not going to work. So how do we get LLMs to continue to scale in a world where we can't just make things faster in this frequency sense? That's where the GPU shows up, in parallel scaling in general. Percy's going to talk about more advanced topics next lecture, but after that's going to be parallelism. Parallelism and GPUs are both kind of part of the same

**[6:12]** paradigm. Instead of making things go faster serially, with your clock going faster and faster, instead you're going to scale horizontally — have more things executing your instructions in parallel. This is kind of the story of GPUs. This one slide, or one picture, really summarizes the state of parallel scaling: in the early days of K20s and M40s, you had respectable, but somewhat pitiful by today's standards, flops. And all of a sudden, around P100s and V100s, this curve takes off, and you get super-exponential scaling in the amount of floating-point operations you can do, year by year. This is a remarkable plot, in the amount of compute we can bring to bear. GPU scaling has really driven almost all of the compute

**[6:59]** scaling that we've seen. We'll talk about all of the different components we have here — I'll highlight them just to put them into your head. At the V100 era, in 2017, we start getting Tensor Cores — that's one of the big hardware innovations. And then in the next few years, we're seeing structured sparsity and FP8 and all these lower number formats also driving our flops forward — we're going to see what that means in detail in a little bit. So, how is a GPU different from a CPU? Those of you who have interacted with a GPU already know this — we'll move on to more advanced things in a minute. But for the rest of you, it's very important to understand the model — the philosophy, in some sense — of how they're different. So, a

**[7:45]** CPU is designed for quick serial execution, where you have complex branching logic, complex conditionals, complex control flow. Because of this, a CPU generally has really big control units, and a few ALUs that can do mathematical or other operations. But this is meant to run very quickly — you want to go as fast as possible, and have low latency. The time between when you get an instruction and when you complete it should be very, very short. That's the general CPU design philosophy. In contrast, the GPU is really about throughput. And the plot on the right, I think, is a great pictorial illustration of what you end up getting when you change these philosophies. In a GPU, maybe you dispatch a task, and it takes a long time to finish. It might be

**[8:31]** that you process a task, then stop, then go work on another task, and then go back and work on another task. But in general, even though your tasks don't finish quickly, you're getting large aggregate throughput across all of your tasks. And what enables this is having tons and tons of lightweight cores. A GPU has hundreds and hundreds of compute units packed into a chip, and all of these can execute in parallel. This is a very different mode of programming and thinking compared to a CPU. Okay? I want to demystify what a GPU is — I don't want you to think of it as just a set of pieces of text on Nvidia — *[Ed: captions have "on Nvidia as am I"; likely means Nvidia's documentation, but the exact words are not recoverable]* — I want you to be able to visualize what the hardware of a GPU actually is. I think this

**[9:16]** picture is usually used as a pictorial illustration of what a GPU is, and I think it's a great logical picture of what's happening inside of a GPU. So the basic unit of a GPU is an SM, a streaming multiprocessor. A streaming multiprocessor is kind of like a core — its own independent compute unit that has sub-components it can use to accelerate its computation, and access to certain kinds of memory it can connect to. Each SM, building on this idea of parallelism — it's not a single compute unit, it has streaming processors inside of it that can execute different threads in parallel. But an SM is the discrete compute unit. We'll see why later, when I get to the programming

**[10:02]** model. Each SM you can think of as kind of a core, almost. GPUs have tons of SMs — an A100 will have 128 SMs. Each of these can be independently programmed, and they can independently execute different jobs, and they have access to global memory outside of it. That's kind of the compute story of a GPU. But we'll see throughout this lecture that a really important part of this story is not just the compute part, but the memory part. Modern hardware and LM optimization is really defined by the memory. So we need to understand what the memories are and where they live. When we talk about memory, there are different kinds.

**[10:47]** There are different levels of caches connected to a GPU. There are registers, which are even more limited and even faster. And then you have global, or high bandwidth, memory. As you can see from this table — this is a benchmarking some folks did of, I think, A100 latencies — the L1 and shared memory are very fast: something like 20 to 30 cycles to read and get data from them. If you're hitting the L2 cache, much slower. And if you're hitting global memory, that's 10 times slower than the L1 cache. This is a big difference in speed. This global memory is what you normally think of, as a high-level programmer, as GPU memory. When an H200 tells you it's got 144 gigs of memory, you're

**[11:33]** talking about global memory. You're not talking about shared or L1 cache. Now, physically, where do these things live? Global memory is usually these memory chips that live outside of your compute unit, whereas L2 and L1 cache live physically in the chip itself. So this is the picture of a GPU chip — you see all these SMs, these green blocks are SMs, and you see L2 right here, and the L1 is inside each of the SMs. They're physically much closer to the compute units, which is why their latency is so much better. You might say, okay, if shared memory is so nice, why don't you build a whole chip out of shared memory? It's much more expensive — hundreds of

**[12:18]** times. It's also much more power hungry, from the way these memories are built. So this is the reason why, if you're building a practical accelerator, you often don't want to just have a giant amount of shared memory — you want the hierarchy of memory, and to use it effectively. I think Groq, which was recently acquired by Nvidia, was a design where you just had these giant amounts of SRAM. For certain workloads, like inference, this is very helpful. But still, for most accelerators, you're going to have this hierarchy that you really have to respect in order to make anything run fast. Yes. *[Question from the floor: What's the difference between the shared memory and the L1 cache?]* So, the shared memory and the L1 cache kind of operate differently. The cache

**[13:05]** is really just a cache — it's storing recently accessed data elements. Shared memory is a programmable element that you actually interact with, that you can put things in and out of. Okay, cool. So that's the memory story — keep that in mind, because it's going to keep coming back, the memory hierarchy. It's an important thing. Now, we have the hardware mental model in place, and we're going to start talking about the software and programming model of a GPU. There are three important players in the execution model of a GPU. There's a thread — if you're familiar with parallel programming of any kind, you know what a thread is. A thread is a lightweight thing that's running in parallel and doing some work,

**[13:50]** right? In a GPU, an important thing about threads is they follow the SIMT model, which means all of the threads you have are executing the exact same instructions. They're going to have different inputs, which means the threads can do separate useful work, but the instructions they execute have to all be the same. This has important implications for both the efficiency of your code and the programming model — if the threads could do all sorts of different stuff, it would become very hard to program. GPUs are this trade-off between programmability and efficiency. Now, a block is a group of threads, and the reason a block is a relevant object is that a block is guaranteed to run on a single SM. And remember, an SM

**[14:36]** has shared memory, so you can think of a thread as doing work in parallel, and blocks as having access to a shared pool of memory. That will become very important later when we talk about tiling, where these blocks are going to be reusing the same pieces of memory. Finally, a warp is essentially the scheduling unit of what's happening inside the GPU. Threads execute in groups of 32 consecutively numbered threads — that's a warp. This decreases the overhead of a scheduler deciding which threads to run. But essentially, these are the three important terminology items for a GPU. If I say something like, "we're going to have different warps executing," that's what I mean — a basic unit of 32 threads that execute together.

**[15:21]** Yes. *[Question from the floor: You mentioned all threads execute the same instructions — is that all threads in a block, or all threads in a warp?]* All threads in a warp. Cool. *[Question from the floor: So then the scheduler decides which warp the next instruction goes to?]* That's right — or, sorry, the scheduler decides which warp is going to be executing next, in terms of warp assignment and so on. Cool. Okay. So in the memory model of a GPU, device code can read from registers. Registers are some of the fastest and most local items — this might be storing things like memory addresses, where your arrays start and end. That's a place you might store things in registers. Then you've got

**[16:08]** local memory. This is local to within a single SM. You've got shared memory, accessed within your block — if you have something you want to transfer across threads, or information that's going to be reused across threads, that lives in shared memory. You can, of course, read from global memory — the DRAM that's far away — as long as you're willing to pay the latency hit of waiting for data to come from global memory. There's also constant memory, which I don't think I've seen used very much in practice, but you can use it as well. You can even transfer to and from host memory — host memory means CPU memory that lives on the machine. You can do this if you need to offload

**[16:53]** beyond the size of your GPU. This is the set of things you can do with your memory. And really, the most important part of this memory model is that as soon as you go outside your shared memory, things are going to be slow. So grouping blocks to decrease the amount of global memory reads is going to be the name of the game throughout this lecture. Okay, now I'm going to talk for two slides about TPUs. The majority of this lecture is about GPUs, mainly because if you go out and write your code and build your models, it's probably going to be on GPUs for most of you. But I think it's very important that we talk about TPUs, both because I think TPUs are cool

**[17:40]** and becoming more popular, but also because TPUs are kind of the alternative evolution of GPUs, and you can see what's the same and what's not. It's very interesting to see how TPUs work as well. Actually, what's interesting is that TPUs are very similar to GPUs at a high level — this is convergent evolution. If you want to build an energy-efficient accelerator that does machine learning, you kind of end up at the same place. So the core mental difference between a TPU and a GPU to keep in mind is that TPUs are, in some sense, simpler — they're more optimized for the machine learning workload. They have lighter-weight control units, and much bigger matrix multiply units. They have the same basic structure that I'll talk about for

**[18:27]** for GPUs. They will have a specialized circuit to multiply matrices. They will have components that can do parallel vector operations, and they will have some control system. They have a very similar-looking memory structure, where they have slow memory, called high bandwidth memory, and an even faster local memory called SMEM, or shared memory. So this looks very, very similar to the architecture of a GPU. Really, the difference is in how these things are sized, and the flexibility of the operations. And I would say the main difference that I won't get into today is the networking for these accelerators. So if you're thinking about the big difference between a TPU and a GPU, actually the biggest differences come in networking, not in

**[19:14]** the individual chips, because really the chips just live to multiply matrices. This is from the JAX book, which is a really lovely resource — they have these two tables. They have a section on GPUs where they describe how to map GPU ideas to TPU ideas. Whatever I say today about what's on the left here, you can map directly to the TPU. But the important thing to notice at this point is that for every concept in the GPU, there's a corresponding concept in the TPU, and the mapping is actually pretty precise. A tensor core in a GPU, which is the matrix multiply unit, corresponds almost exactly to an MXU, which is the matrix multiply unit inside a TPU's tensor core. And if we're getting into really nitty-gritty details, they actually use the same

**[20:00]** underlying circuitry — they're both what's called a systolic array, which streams data in and out to do a matrix multiply. So the architecture is very similar. Now, how are they — *[Question from the floor: Do you know what a typical systolic array size is, on a GPU versus a TPU?]* I do not know that off the top of my head — I'm sorry, I'll have to look that up. Yes. Okay, so the main difference between these two is the number of different units we have. So if we look at — actually, I'll mention one very confusing thing before I move on: TPUs call their streaming multiprocessors tensor cores. GPUs call their matrix multiply units tensor cores. They're named exactly the same thing, so you will have to disambiguate them based on context. If someone

**[20:46]** says tensor core for a TPU, that means a processor. If someone says tensor core for a GPU, they mean a matrix multiply unit. So if you look at a GPU — a typical GPU, like an H100, might have something like 132 streaming multiprocessors. That's a lot of processors. A TPU will only have two units — there are only two separate units running in parallel. Now, if we just look at the matrix multiply units, a GPU will have 528, whereas a TPU will have just eight. These are roughly scaled to be comparable-looking objects. Why is it this way? Well, the TPU is relying on a smaller number of much bigger matrix multiply units, whereas the GPU is relying on smaller,

**[21:32]** but much more numerous, matrix multiply units. You can imagine that if you have a large number of small matrix multiply units, it's going to give you more flexibility — you can program them to do this and that. Whereas with a TPU, you're locked into big, big matrix multiplies. There was actually a funny situation in a recent paper I wrote, where we did a batch size sweep, and it goes all the way down to 64 and stops at 64. Why is that? Because the tensor core refuses to accept anything smaller than a 64-dimensional input — it has to be big matrix multiplies. So this is the main difference, but as you can see, GPUs and TPUs share a lot of similarities. They rely on a memory hierarchy between fast and slow memory, and they have a matrix multiply unit. Because of that, the core concepts I'm teaching you today are going to transfer over between GPU and

**[22:18]** TPU. The basic ideas are always going to be the same, for that reason — and this is really convergent evolution. There are only so many ways to cost-effectively allocate memory, and only so many ways to multiply matrices very fast. There are some design decisions that differ, but otherwise you end up at the same place. Okay, cool — so that's TPUs. And GPUs, I think, have been enormously successful, because it allows for easy scalability for things like matrix multiplies. If you want more throughput, you just keep adding SMs — as long as your memory bandwidth can handle the SMs, you get more and more throughput. The programming is something we don't think about, but it's deceptively easy. If you were to try to write lower-level, machine

**[23:05]** code-looking stuff like PTX, it's actually doable, because it's all SIMD — it's not like you're programming every single thread, you're saying, "here are my instructions, here are my inputs, now run them on all of my inputs." It's something you're maybe already familiar with, from things like applying, or using, functional programming. Finally, I haven't talked much about threads, but whenever you're doing GPU programming, these threads are always very lightweight — they can be stopped and started at any time. The scheduler can decide, "okay, I have another warp that's better to run, so I'm going to stop you and start this one." It's very quick to swap in these jobs, which allows for much higher throughput if certain jobs are stalled, or have some property that's lowering throughput.

**[23:51]** Okay, so that's the benefit of GPUs as a hardware model — it's been enormously successful. I think even in the early days, before Tensor Cores were implemented, people realized that commodity graphics hardware, with this massive parallelism, would be very effective for scientific computing. This is one of the original papers on using graphics hardware for fast matrix multiplies — really, really cool hacker work. People figured out, well, we have these shaders that are implemented, but we can program specific shaders that will actually give us matrix multiplies, and different rendering settings give us faster matrix multiplies. That's just really cool computer science you can do. But nowadays, you do not have to

**[24:37]** manually program your GPUs to do matrix multiplies, because as of the V100, Nvidia decided, here is a piece of hardware that will do your matrix multiplies for you. And once that hardware existed, matmuls became the one privileged operation in machine learning. At this point, there's a gigantic gap between the throughput you'll get on parallelizable but non-matrix-multiply operations and the amount of operations you can do in a matrix multiply. So this is one of the reasons why any near-future machine learning architecture that scales with compute is going to have a matrix multiply in it — because this is the one way you can really effectively get a lot of compute throughput,

**[25:23]** more than 10 times faster than any other floating point operation you can do on a GPU. Okay, and then the last hardware thing I'll end with — I think this is the last slide of this section — is that different components of the GPU are scaling at different rates over time. We know GPUs are getting faster and faster, that's just a thing, as well as CPUs — overall compute throughput is getting faster and faster. That's the gray line — pretty fast, we're getting faster and faster improvements in total floating point operations. Green is memory bandwidth, how fast we can transfer data — that's growing comparatively slowly. Blue is parallelism, how fast we can connect different GPU devices. That's

**[26:09]** also going very slowly. What does this mean? It means that if you're out here, in the early days of GPU programming, you probably weren't thinking much about memory, because your compute wasn't that much faster than your memory transfer. But now, as we go further to the right of this plot, we're going to see a bigger and bigger gap between compute and memory — that's why a lot of the optimization I'm talking about today is memory optimization. As we go further right, we'll see memory and communication bottlenecks more and more, and it's going to take more and more work to fully utilize the hardware we have. Okay, so that was our very basic overview of GPUs. I can't possibly give you a 20-minute lecture that fully helps you understand what the GPU hardware is, but hopefully you have a

**[26:55]** flavor, right — the mental model should be: GPUs are massively, massively parallel, they execute instructions all at the same time; compute scales really, really fast, even faster than memory, which means memory is really important; and because memory is important, everything we do has to respect the memory hierarchy — as much as possible goes into shared memory and not global memory. So hopefully that's your understanding of GPUs, and if you have that, you'll understand all the basics necessary to internalize the next part of the lecture. Okay, I can pause for a moment here in case people have hardware questions before I move on. Yes. *[Question from the floor: Before the V100s, was there no matrix multiply unit like]*

**[27:43]** Yeah, yeah — there was no tensor core specialized to a matmul. The tensor core is a V100 addition. Before that, you'd be programming manually — of course, you have so many SMs and so many ALUs that you can still get quite good throughput, but a specialized circuit for matrix multiply really changes the game. Oh yeah, back there. *[Question from the floor: What is the difference between the shared memory and the L1 cache?]* The cache just operates on its own — you don't get to control the L1 cache. *[Question from the floor: Is L2 slower than L1 just because of the physical distance, or does it also differ some other way?]* Physical distance is a big part of it. If I remember right, they're both SRAM — that's one of the reasons why global memory is much slower. But the physical distance and the interconnects

**[28:28]** that are used are the reasons it's slower. This isn't inherent, by the way — one additional side thing is that the equivalent of L2 cache in TPUs is much faster, because they make different design tradeoffs on silicon to enable that. That's one of their selling points. Yes. *[Question from the floor: If this compute-memory gap keeps growing, do you think that affects the way future chips will be laid out?]* Yeah, or maybe the future chip budget splits. One of the things I really didn't want to talk about, but now you've forced me to, is that the inference landscape for hardware is actually even more crazy. One of the things people started doing, I think last year, was something called prefill/decode disaggregation, where the prefill part, which is very heavy matrix

**[29:14]** multiplies, is one chip, and the decode part of your inference, which is more memory-bandwidth limited, is another chip. And some of the models — I think this was, uh, step one three *[Ed: captions have "step one three"; likely a specific Chinese open-source model name, but this lecture's deck does not mention it and the exact name is not recoverable from the captions]*, a Chinese open source model — even have a thing where they try to disaggregate different layers, so attention goes to one accelerator optimized for one thing, and MLPs go to a different accelerator. So you're starting to see that if this gap grows, any clever trick to utilize your precious fast memory becomes very valuable, and you see this very heavily in inference, which is even more memory-bound than training. Yeah, cool. Oh, yeah. *[Question from the floor: How many warps are in a block?]*

**[30:00]** Do I know that? I don't think I know that off the top of my head — I think it's also hardware-dependent. Okay, good. So the second part of this lecture — now that you all have a general sense of what a GPU is and how it works — is going to talk about all the basic tricks to make workloads fast on a GPU. These are basic components you can put together to make things fast. And to keep us motivated, one of my goals by the end of this section is to explain this plot to you. This plot, as I said before, has a very simple picture: it's the size of a square matrix, and I'm multiplying two square matrices. The y-axis is how many items I'm processing per second — so this is a throughput.

**[30:47]** As I go to the right, in general, throughput is going up, because I have bigger matrices — more work to do — and if I have a lot more work, I can keep my hardware more utilized. But there are all sorts of strange patterns here, and we want to understand why. How can we be at the very top of that curve rather than the bottom? That's what I'll be talking about in the second part. Okay, and Percy already mentioned this — this is the roofline model. The plot I showed before looks a little bit like this classic roofline model: a diagonal part and then a flat part. This is what we expect. The roofline model says, up until a certain point, we'll be memory limited — no matter how much compute

**[31:32]** you give me, my throughput is not going to go up. At some point, you have enough work per unit of memory movement that I can keep my compute units completely saturated, and past that point, bigger and bigger units of work don't help anymore — no matter how much extra work you give me, that doesn't help, because I've fully saturated my matrix multiply units. So this is what we expect from the classic roofline model, where, as we get faster memory, we can push this upward, but there's a memory-bound region, which is diagonal, and a compute-bound region, which is flat. This is what we expect. Now, if you want to make sure your code runs efficiently on a GPU, what you need to do is be on

**[32:18]** this flat part. As soon as we're on the flat part, there's nothing else to do — we're at maximum throughput. So we want to avoid being in the diagonal part, which means we want to increase the operational intensity — the amount of computation we do per memory read needs to be high enough. That's what we need to do. There are six different tricks we'll talk about — some of these aren't memory tricks, just the first one. There are six tricks that are necessary to understand to make GPUs go fast. The first one is a little different in flavor, but the rest will have this flavor of trying to minimize memory movement as much as possible. The first one is a GPU-specific thing, and it relates to something I said before — the SIMD model.

**[33:05]** In a GPU, every thread is executing the same thing. Now, what happens if you write an if statement in code that executes on a GPU? On a CPU, it's quite clear — you pick one branch or the other, execute it, and move on. In a GPU, you do something actually quite different, and slightly counterintuitive: every thread has to execute the same instruction, so every thread will execute both the if and the else, but if you're not part of the if branch, you just mask out that computation and sit idle. Whenever you have a conditional, your mental model for the execution looks like this: part of your threads will diverge based on the if statement — some go to the bottom branch, some

**[33:50]** go to the top branch. The bottom-branch threads execute first while the top-branch ones wait, and then the top-branch ones execute while the bottom-branch ones wait. So to execute an if statement, you're actually clock-walking through both branches, and some threads are just waiting, doing nothing, if they're on the wrong side of the branch. This is called control divergence. It's nice in some ways, because it simplifies programming, but it's not nice in that you now have these big compute gaps where your GPU is doing nothing. This is one of the reasons you really don't want to write if statements. If you look at GPU code — say, a ReLU — you'll often find you're multiplying by zero, or multiplying with masks, instead of an if statement that updates half, because a multiplication is going

**[34:37]** to all happen at once, whereas an if statement might have to wait two clock cycles, or two operations. Okay, so that's the first thing. But that's a separate story from all the memory stuff, which is really the main narrative here. The next one, which you don't really have to do too much for, but which I think is one of the most important, and hardware-wise where Nvidia is investing the most effort, is low precision computation. If you look at this plot — I've brought Bill Dally back to the slide — this super-exponential growth, where does it really come from? A pretty non-trivial part of it is number representations: you start at FP32, go to BF16, and then to INT8. You just keep decreasing

**[35:24]** the precision of your numbers, and of course, if you have half the bits, you have half the bits to move — so you've solved the memory bottleneck, at least for a little bit. This is a dumb example, but I'm going to drill it in: say I have a value in a vector of size N. If I'm at float32, I need to do one read to read my number, and one write after I've computed my value — that's 4 bytes per operation, 8 bytes total for my memory access. I only have one flop worth of operation, so that's 8 bytes per flop. If I halve the precision, of course, I've halved the number of bytes per op — I've halved the number of bytes that I

**[36:10]** need to move per flop. Sorry — this is the inverse of arithmetic intensity, arithmetic intensity should be the reverse of this. Now, that's a very simple example, but how low precision actually works in a modern GPU is very complicated, and either very cool or very crazy, depending on how you think about how cool things are. When you do a matrix multiply — say you have a tensor core operating in lower precision — how does that actually work in practice? You're not actually going to keep everything in low precision. You're going to downcast before you take your product, and when you sum up the partial sums in your matrix multiply, you're going to do that in full precision, and then you might emit your outputs, or your results, in FP32.

**[36:57]** Part of the reason low precision is kind of a black art is not because you can just decrease precision — anyone can do that. You have to figure out which of your operations has to be in which number format. For matrix multiplies, maybe both your weights and activations can be low precision. If I'm doing a softmax, maybe I need to do that in FP32. Or if I'm doing an exponential, maybe that also needs to be FP32, or maybe I can get away with BF16. It took several years to get low precision training to the state it's in today, and a lot of that work was slow, incremental, empirical work — figuring out which operations we can downcast, how to downcast them, and how to preserve stability during

**[37:43]** training, while making as much of our training and inference low precision as possible. People have been pushing this further and further — once you think about how hard it is to pay for fast memory, you realize, if only I can cut my precision by another factor of two — people really, really want to do that. So FP8 was the next frontier, I'd say, of low precision. Once you get to FP8, there's no longer a standard canonical format everyone uses — if you're using 16 bits, you're probably using BF16, for a lot of what you're doing. But with FP8, there's going to be E4M3, which stands for four bits of exponent and three of

**[38:30]** mantissa, or E5M2, which is more exponent, fewer mantissa. Because these are used in different places, there's no one-size-fits-all once you get to so few bits. But I think the really interesting thing is the more advanced low-precision number formats coming out with new hardware. I'll talk briefly about MXFP8, because it's both cool and what it does to training is strange, to say the least. In FP8, the traditional version of training, all of your activations — let's say — are in eight bits. That's up here. And then you have a scaling factor in FP32 that lets you scale this up and down. Why do you have a scaling factor? The scaling factor is necessary because you

**[39:15]** only have, say, four bits of exponent — that's so few bits of exponent, you're going to quickly over- or underflow. So you need the scaling factor to keep you in the right range, so you can keep some information. Well, people realized this is not optimal, because a single matrix might have very different magnitudes — one part of my sequence might have much bigger activations than another. So instead of a single scaling factor, why not have many scaling factors? MXFP8, and these multi-scaling-factor formats that are more advanced now, have many scaling factors — each corresponds to a relevant colored submatrix. So the first four elements in this matrix correspond to

**[40:01]** this first scaling factor, and so on. Now I have more scaling factors, and these scaling factors are themselves in lower precision — eight bits as well. They're E8M0, so they only have exponent bits — they're all powers of two, in eight bits. So you have to make these tradeoffs. So MXFP8 uses more mantissa in the elements, the scale factors are themselves FP8, and you have one scale factor every 32 elements in your matrix. Okay, I'm going to pause here — don't read the third thing. Think for a moment: what is the problem with this design? What is the big issue if you do something as clever as this?

**[40:47]** So the big issue is: say I want to transpose this matrix. The transpose does not have the same scaling pattern as the original matrix. It used to be that transposing a matrix was simple — I just transpose it, I don't have to re-quantize, I don't have to do anything. Now, transposing this matrix is very complicated, because I now have to potentially re-quantize the whole matrix to fit this one-out-of-32 pattern. What happens internally when you actually use MXFP8 — and I found this to be crazy, but also kind of cool — is that when you're training your model with this, it actually creates two copies of every quantized matrix: one for your original matrix, one for the transpose. So if you ever want to

**[41:32]** transpose, you have the transposed version waiting for you. That's how you get around this, which I think is a crazy and cool thing. The point here isn't to emphasize the GPU trivia — it's that once you start getting to these FP8, FP4 formats, things get pretty exotic, because the amount of exponent and mantissa loss you have is pretty extreme. So you have to be very clever about how you do this. And, as I said, if you were to go and really train a model in FP8, which I think a lot of frontier labs are doing for the compute benefits, you have to be very careful. This plot shows what I'm saying: every time you quantize, you quantize one way, and you transpose and quantize, so you have both copies. And the other part, which I told you

**[42:18]** about a few slides ago, is that you're only going to quantize certain layers you think are safe to quantize — another trial-and-error thing you have to figure out. Once you have all this together, you get the big benefit of training in FP8: when you're doing those matrix multiplies, you're probably going to get 20 to 30% savings, maybe more, depending on the size of your matrix. Unfortunately, it's not going to be a 2x speedup, because you have to do all these quantization operations — there's a big overhead, so it's not free. Yes. *[Question from the floor: Going back to the thing you mentioned about the first and last layers being hard to quantize — do you have any intuition for that?]* That's right, that's right. I don't know that I have an intuition for the first layer. The last layer, I think, is hard to quantize because it's kind of a first-order

**[43:04]** factor — it derives a ton of the loss. So I think you end up getting both instability and big loss increases if you attempt to quantize the very last layer. Okay, I put this up here almost because I think it's so ridiculous — the next frontier, already implemented, is MXFP4. The entirety of MXFP4 can be shown to you in this slide: it's negative six to six, you have all the possible values. But now that you understand MXFP8, you can also understand how this works. You have a structured block, where each block is one of these four bits. Every 16 of these numbers, you have one scaling factor, and that scaling factor is itself FP8 — an E4M3 scaling factor.

**[43:50]** There has been a paper that trained with FP4. I don't think I've heard of anyone successfully training real, big, serious models with FP4 in the wild, but I think this is coming — the next generation of models will probably be FP4. Okay, so that was low precision. I think it's really cool that you can push the limits so far that you end up with just plus six to minus six, and yet do interesting things with matrices at that precision. Now — oh yeah, is there a question? *[Question from the floor: Is this only for matrices — can you apply this, or not to single scalar values?]* Yeah, you can quantize anything. You can quantize the

**[44:36]** activations after a ReLU, for example, but there just isn't as much need to. Something like a matmul — if you quantize that, you get really big throughput improvements, and that's why, if you look at the MXFP8 training case, these are all matmuls getting FP8-quantized. The overhead probably isn't worth it most of the time elsewhere. *[Question from the floor: Does it have to do with the hardware — is it that the matmul hardware part only does this?]* It's not exclusive to that — you can always do lower-precision stuff, and it decreases your memory movement, that's true. But for the others, it doesn't quite make sense. *[Question from the floor: So this isn't a hardware limitation?]* It's not a limitation, but there is hardware support for all of this — a lot of it is baked into the hardware to accelerate it. Yeah.

**[45:21]** *[Question from the floor: You've framed this as an improvement for memory bandwidth, or requiring less memory bandwidth — but is there a bandwidth-to-compute benefit as well?]* Yes, for sure — for compute, you get basically linear improvements just from multiplying the quantized numbers. But the fact that you have to quantize and dequantize means the benefits are more diluted. *[Question from the floor: Do you have to train the scaling factor?]* The scaling factor is an implementation-dependent choice — depending on the library, you might get different ways of quantizing and dequantizing. "Training" is a strong word, but you might have quantization factors that scan through and pick the max and min, or scaling factors that look at historical running statistics and use those. I wouldn't really call that training, per se. At inference, you might fit them

**[46:07]** more. *[Question from the floor: So there are no gradients?]* At inference time you might have some; at training time you would not. *[Question from the floor: Just to clarify, has anything been done with structured sparsity? We didn't cover it, but —]* Yeah, there's a lot that's been done with structured sparsity — MoEs are, I think, one successful example of a structured sparse operation people use. *[Question from the floor: What about sparse ones?]* Yeah, right — a lot has been done. Chris Ré here has done a lot with lots of different kinds of structured matrices. I think the benefits there, in terms of compute benefits versus representation losses, have kind of washed out — I don't think it's a bad idea, but empirically it hasn't necessarily panned out. Oh, yeah. *[Question from the floor: There are a lot of things, like squeeze — *[Ed: captions have "squeeze"; unclear which technique this refers to]* — that we didn't talk about on that slide.]*

**[46:55]** *[Question from the floor: What is the current state-of-the-art approach — is it training in full precision or half precision and then distilling into a quantized model you can run on your Apple computer, or things like that? What's state of the art?]* Yeah, there's a combination — you probably want to train a bigger model and then quantize down. There are various quantization scaling laws, and papers precisely characterizing these trade-offs. I think the state of the art involves combining quantization-aware training, to some extent, with model scaling and post-training quantization of various kinds. There's a lot that I don't think is fully well understood — even industry teams I've talked to are still talking about doing the science of

**[47:40]** quantization. Okay, so the next trick — this one is much more of a conceptual programming thing — is operator fusion. It's a fairly simple idea, but you'll be surprised how much it doesn't already happen. Think of a GPU as a factory: you've got your memory warehouse, your little compute factory, and a little conveyor belt going back and forth. If you scale up your compute, you're going to be bottlenecked by that conveyor belt, and this gets worse if you have many operations — imagine I've got lots of different operations to do, and I'm shipping raw materials to my factory and back, over and over. That's a lot of memory bottleneck you're paying for, and lots of duplex, bidirectional memory bandwidth. Now, wouldn't it be better if I just had

**[48:26]** one giant factory that took all the raw materials and shipped back my finished products? This way I only have to pay for memory bandwidth twice. Very simple idea — this is operator fusion. It appears very often. If you write a simple computation of sine squared plus cosine squared, what does the PyTorch computation graph look like? It looks like this: you have your X, you do sine, you do cosine, square both, add them, and get your output. Naively, if you don't do anything smart, each of these will be its own factory, so to speak — you'll read and write from global memory every time one of these is called, and incur quite a bit of memory cost each time. This is pretty painful.

**[49:14]** And you can imagine, instead of doing that, why don't I just write GPU code that reads once from global memory, and does the whole thing inside the SM, as I talked about before, and then, after it's done inside the SM, writes it back to global memory? A single kernel that does all of these operations — this is called fusion. And anything that's easy like this — this is a very easy fusion — is like a graph that I can just squish down into a single unit; a compiler can automatically do this for you. So Torch Compile, which is a compiler for PyTorch, or JAX Compile, which is JAX's compiler, more deeply integrated into JAX — both of these will fuse these kinds of basic things into a single CUDA call.

**[50:00]** I'll talk a little more about fusion later — the advanced version sometimes requires manual intervention, but for the simple versions, you can just think of compilation as solving your problems. Okay, the third idea is recomputation. In backpropagation, what do we do? We go forward, up the tree — this is from Percy's 221 class — you go up the tree and keep track of activations, and then when I go backward, I use the activations I stored to compute the backward signal down my tree. We all know backprop to some extent, so we know what this is. But now, instead of looking at this from the perspective of mathematics — how do we compute a gradient — we're thinking about it from

**[50:46]** the perspective of systems: how do we minimize our memory? If we think about what's happening — say I have something simple, X, sigmoid, sigmoid, sigmoid, out. Each sigmoid generates some residual, or activation, in the computation graph that I need to track. So after my first operation I have S2, after the second S1, and when I do my backward pass, it consumes S2, S1, and the gradient at my output, and pushes that backward toward the derivative of my input. If I count the memory reads and writes: one memory read for X, three memory writes for S2, S1, out, and on the backward pass this is flipped. So that's a total of eight memory reads and writes — very

**[51:31]** low arithmetic intensity, because I'm just stacking a bunch of sigmoids. Now, what can I do instead? I can just throw away all of the activations. Normally you wouldn't do this — why would you want to recompute something? But imagine you're in a world where computation is super cheap — you have abundant computation, you're not using all of your units, but your memory is very expensive, any read and write carries a big penalty. Then you might prefer this computation graph: I read X, then do sigmoid, sigmoid, sigmoid, output — I store this output, because I have to write it, that's the output of my graph. But now, in the backward pass, my backward graph gets D-out, and on the fly, I re-forward the X and compute the activations

**[52:17]** on the fly, right where I need them. So this is two memory reads, for D-out and X, and one memory write for DX. I've gone from eight total memory accesses to just five — 5/8 of the total memory accesses. Same computation, but I've spent a little extra compute and saved a little extra memory. This is another very important trick — we don't normally think about it, because we're usually just concerned with computing the gradient, but once you have a systems mindset, you start to think about recomputation. Cool. Okay, trick number four — this is another memory-trivia thing, but it surprisingly becomes important, and it's also why it's sometimes important to understand the hardware model of what you're interacting with.

**[53:04]** Global memory, the big memory that lives outside the chip, is DRAM, and because of how it's structured, it actually comes in bursts. What that means is: when I access, say, the very first position, I don't just get that first value — I can read off all four of the first values for free. That's because the way the memory cells are lined up, there are these blocks of memory units, and when I activate an amplifier, I can read out all of them almost for free. The way it's structured in parallel lets me read blocks of memory — that makes sense as a hardware model. This is called a burst section, and whenever a location in a section is accessed, all the other locations can

**[53:49]** also get delivered for free, so to speak — you won't pay for them. You might have something like 128 bytes to a burst, meaning a single read can return up to 128 bytes of data, as long as it lives in the same contiguous block. This means that if you have your reads arranged contiguously, where they're all together, it's going to go much faster. And to use a term — you say that a memory access is *coalesced*. That word means all of the threads in a warp fall in the same burst. So say I have three threads — sorry, four threads — and my four threads live inside a single burst section,

**[54:35]** or they're all reading from this burst section — then this is coalesced, because they're using up the entire burst section, and I'm fully utilizing this nice property of DRAM memory. If I have multiple warps, I'll have multiple different burst sections, but you can read out of memory efficiently by grouping your memory reads together. Why don't — oh, yeah. *[Question from the floor: This sort of just explained it — why does this balance out like this? This kind of makes sense —]* Which one, like this? *[Question from the floor: Like this one, yeah.]* *[Ed: from here through the next paragraph, a back-and-forth about the DRAM row/column diagram is heavily garbled in the captions, and it is not always clear where the question ends and the answer begins; left close to verbatim rather than reconstructed.]* So, as far as I understand, the memory cells are arranged in a grid, and the amplifiers deciding which cells to read operate at a column-wise level, and activating

**[55:20]** the voltage for this often takes a significant amount of time, so once you've selected the column, reading out multiple elements within that column is comparatively much cheaper. *[Question from the floor: Is this to select — to read that upper-left cell, or —]* Or, yeah, right — if I want to read this, for example — maybe it's the columns that are cheap in this diagram, at least — but if I'm trying to read this, I might get this whole row for free, because I've addressed the whole row. *[Question from the floor: Okay, so I don't get a "key" for free, I just —]* You don't get a "key" for free, yeah. Okay, good. Okay, so you're saying, Tatsu — why are you telling me DRAM trivia? The reason I'm telling you DRAM trivia is because — think

**[56:05]** about matrices — you often want to read them in big blocks, because you're going to do matmuls, and certain kinds of memory accesses on matmuls are privileged: they're much more nicely structured than others, and sometimes you can take advantage of that to get very fast memory reads. So the mnemonic I remember is: if you have threads, and a row-major matrix — sorted so each row is how your indices increase — threads that move along the major axis are not coalesced. To make this visual, I like this diagram on the right: imagine a 4x4 matrix, row-major, so it's sorted in the order where the yellow block comes

**[56:51]** first, the red block comes next, and so on — that's the linear ordering in memory address, the matrix I'm trying to store. Now imagine I have threads trying to read each column, each clock cycle — I read a column, move to the right, read a column, move to the right. At the very first iteration, I hit this element, this element, this element, this element. Notice each one of these hits a different burst window, so I read the entire matrix, so to speak, even though all I wanted was a single column. Now imagine if it were reversed — if I were reading down the matrix instead — I could read the entire block: every thread would get its read in a single read of memory.

**[57:37]** So coalescing can give you significant improvements in reads from large, contiguous structures like matrices. This is one reason you might want to think a little about row versus column versus other kinds of ordering. Okay, the last one I want to talk about, and this is the big one — maybe the most important idea, I've saved it for last — is tiling. I think this one probably has the biggest impact on performance. Tiling, more than any other idea, is about trying to do as much as you can with the memory hierarchy: you want to group your memory accesses as much as possible, and shared memory accesses should be done together, on shared

**[58:22]** memory. The reason it's called tiling is that I'm going to cut up the things I need — tile my space — and each tile gets loaded into shared memory. Once a tile is loaded, I repeatedly read from it and do as much as I can. So think about the matrix multiply case: I want to multiply this matrix and this matrix, and this matrix on the bottom right is my output — that's where I'm writing the result. This is pretty standard notation. Now, if I do this naively, as I read through the threads, the same entry gets read multiple times — to be precise, each entry gets read N times in an N-by-N matrix multiply, naively. So you might think, if I'm

**[59:08]** reading this entry — say M00 — multiple times, instead of hitting global memory each time, why can't I group all of those memory accesses together? And the nice thing about matrices is there's a very natural grouping: cut the matrix up into submatrices, load them, and operate on them. So here's a very simple algorithm: cut my matrices into 2-by-2 submatrices, then load different tiles. First, load the M00 tile, the top left here; load the N00 tile, the top left of my other matrix; and load those into shared memory. I pay the cost of transferring them slowly from global memory once, but once they're in shared

**[59:53]** memory, I can read and write very fast with those tiles. This output tile will also, for now, be in shared memory, and I'll operate entirely on these submatrices — multiply the two submatrices, and put the partial sum into my output submatrix. That lets me do a bunch of reads and writes all in fast shared memory, and then write it back out when I'm done. What does the math look like? In the non-tiled case, I already told you: each element of my input has to be read N times when I do this big N-by-N matrix multiply. If I tile with a tile size of T, each input gets read N/T times from global memory, and T times within each tile. This makes sense at the extreme —

**[1:00:39]** if T equals N, I read it once from global memory and access it N times in shared memory — that's a T-times reduction in global memory access. If you have big shared-memory tiles, you can dramatically reduce your global memory access on something like a matmul. Cool. Tiling, okay. Yes. *[Question from the floor: Are you doing this because you can't fit the entire matrix?]* Yes, yes, yes, yes, that's right. Although, if you were building one of these very fancy alternative accelerators like Groq, which is all SRAM, then you'd just put the whole matrix in there and be very fast with the whole thing — but very, very expensive. Yeah. *[Question from the floor: And this also happens with matmul — there are a couple of tiles]*

**[1:01:25]** that are basically useless — you're not doing any real work, you just keep adding that cell over and over. Is that kind of how it — *[Ed: this next exchange is confusingly phrased in the captions; kept close to verbatim.]* You mean, are you asking whether this kind of splitting happens at the accelerator level? *[Question from the floor: Yeah.]* That's right, that's right. So data parallelism you can think of this way: if I have data on this axis and weights here, and I cut on one of the axes, that's data parallelism — tensor parallelism could be another kind. All of these forms of parallelism look a little bit like tiling, in some ways. Yeah, cool. Okay. Oh yeah. *[Question from the floor: That was super descriptive on the previous one — can I assume a library like PyTorch will just do that automatically?]* Tiling, yes, yes, yeah. So any

**[1:02:10]** matmul kernel you call will do tiling underneath. That's right. If you're doing some exotic operation, you might want to think about tiling yourself — you want to be able to locally reuse your reads. But I'll say even PyTorch isn't perfect, and we'll talk about some of the imperfections that come up. Tiling is very powerful as an idea, but it's also complex — you can end up with some really subtle things happening with tiles. Imagine I have a tile size of 128 by 128. If my matrices are 256 by 256, life is good — I've got four tiles, perfectly cut. Now imagine I increase one coordinate by one. Now I've generated two very skinny tiles with

**[1:02:56]** basically nothing inside them. So now you realize this tile-sizing thing is actually pretty complex — depending on my matrices, I might need to adjust my tile sizes to trade off my memory bandwidth against this gigantic empty space where I'm doing nothing. So tile size is actually something you optimize — coalesced memory accesses, the size of your shared memory on the accelerator, and the sizes of your matrices all affect which tiles are optimal. And if you know about different options in PyTorch, there's a compiler option called max-autotune. I don't know how many of you have used this or know about it — you turn on that flag, and for the next 15 minutes PyTorch is going to

**[1:03:43]** run an endless series of benchmarking tests. What it's actually doing is trying all sorts of different tile sizes on your matrices, and figuring out which one is actually faster. You'll find you get non-trivial benefits from optimizing these things. The other thing you'll find, related to coalescing, is that if your tiles — or your matrices — are the wrong size, your attempt to read a tile can trigger a lot of extra memory reads. Imagine this nice world where, say, this blue one is the row I want to read as a tile, and my burst sections are the width of the tile. In this case, if I do four reads, I'm good to go, because of the burst sections, I read this

**[1:04:28]** entire submatrix — good memory read, thanks to coalescing. Now imagine I shift the matrix size by one. I've bumped this first row over, so this blue box now lives over here, and there's no way to coalesce my reads to read a single tile in one go, because now everything is ragged and misaligned. So I need at least two tiles' worth of reads to read out this one tile. There's actually a lot of complexity involved in optimizing tile reads once you start thinking about coalescing and everything else. In this case, what you have to do is pad, in order to line up the boundaries of the burst sections with the tile sizes you have. And this is why you end up getting stuff like this, which seems very

**[1:05:14]** mysterious. This is one of my favorite weird ones: Andrej Karpathy, when he was doing the nanoGPT speed run, said, well, when I increase my vocab size from 50257 to 50304, I got a 25% speedup. It's kind of weird that padding gets you a speedup, but it makes sense once you realize things like this — padding can shift the rows and columns so that you actually get very efficient reads, whereas otherwise you might not. Yes, go ahead. *[Question from the floor: Is there a — do you have, like, tile sizes you should be taking into account?]* You don't want to think about tile sizes per se, because tiles are normally a lower-level systems thing, unless you're writing your own

**[1:05:59]** kernels. But you do want to think about the size of your matrices. *[Question from the floor: Okay, but you want the size of your matrices to be divisible by the tile size?]* You want — right, I'll talk about that in a moment, but, right, generally powers of two, ideally also divisible by something like 32. Okay, so now we can talk about this matrix mystery. We essentially have all the pieces we need to understand this, so we'll walk through it one by one. We've already talked about compute intensity — the roofline plot: you need enough work per memory read to saturate your compute unit. That's what drives the diagonal upward trend, so that one's done. Now let's talk about the others. First is tiling — if we look at each of these lines and color them by

**[1:06:45]** the divisibility of the matrices — you'll find that the blue ones are only divisible by one, they're not even divi— they're not even even. The orange ones are divisible by two. Green is divisible by eight. Red is 16, and purple is 32. And you see that the ones that are not divisible by, say, one or just two actually have very bad throughput. But once you get to higher multiples, you essentially get no diminishing— sorry, no penalties, right — 16 or 32 perform equally well. And this isn't because powers of two are somehow magic, or something like that — it's because 16 and 32 are sufficiently large that they have

**[1:07:32]** the right burst-window property — the tile fits the whole burst window, so you get coalesced reads into your entire tile. It has a major impact on alignment. Now, if we look at what's remaining — this orange line — we sometimes see this weird periodic behavior, where performance suddenly drops. This is very strange, because I'm just multiplying matrices — why does adding a single extra dimension suddenly kill my throughput? Using everything we've learned to get to the bottom of this is kind of fun — Percy calls this GPU trivia, and I think he hates it, but I love it. So why is it that going from here to here, I get such a big drop in throughput? Well, it happens between 1792 and 1793.

**[1:08:17]** And why does this happen? I'm using a tile size of 256 by 128 — so how many tiles are there? There are 98 at 1792. But once I bump up to 1793, I've increased both dimensions by one, so now I have 120 tiles. Okay, 98 tiles before, 120 now — that's bad, but not this bad, right? This drop is gigantic. It turns out the reason it's so dramatic is that the A100 has 108 SMs. 98 tiles fits in 108 SMs, but 120 tiles does not. So you finish 108 SMs' worth, and you've got 12 more of these to

**[1:09:03]** process, and most of your GPU sits idle. So you can end up with these very weird effects, where bumping up your tile count by a little suddenly means waiting a whole extra cycle to process a bunch of tiles. That's called wave quantization. Okay, so that was all for part two. How do we make machine learning workloads go fast on a GPU? A lot of the story is really just about memory, memory, memory, memory. How do we reduce the amount of memory access we have to do? Coalesce our memory reads together. Fuse our operations together, so we don't have to read as much. And for the things we have to read no matter what, reuse the memory — move things to shared memory and reuse it as much as possible, which

**[1:09:49]** is tiling. And finally, maybe you can trade other resources you have for memory — that's quantization and recomputation. So all of these ideas relate to this idea that memory is our bottleneck, and how do we deal with that. Cool. Okay, any questions before we finish this part? Yeah. *[Question from the floor: I feel like bumping up the SRAM is very expensive — is that why —]* Very expensive, very energy-hungry. There are two reasons: one, it's just expensive to build SRAM; also, it's very difficult, because it has to be physically close — signal propagation is hard. And I think a third reason is that SRAM and DRAM are just different storage mechanisms — SRAM needs to be powered the whole time to hold a value, which is very energy hungry. So if you're thinking

**[1:10:35]** about total cost of ownership, energy consumption — maybe you don't want a giant SRAM chip, you want to use it where you need it. Yes. *[Question from the floor: Does this scale-with-SRAM thing hold past 32, or if you operate in chunks of 64?]* You mean, does it help if I go higher? No, because as long as it divides your burst size, you're good to go — it's a divisor property. There are other phenomena where you might get benefits from larger divisors, but not here. Yes. *[Question from the floor: Why 108?]* *[Ed: captions have "Why 108 birth?" here; the question plainly concerns the A100's 108-SM count discussed above, but "birth" does not parse as any recognizable word]* That's just the number of SMs — you'll have to ask Jensen for that, I don't have an answer for you. Yes. *[Question from the floor: Going back to the hardware side, sorry — not related, but it]*

**[1:11:23]** just occurred to me — have you studied GPUs versus, say, wafer-scale engines? Yeah, I think the person sitting over there knows much more about wafer-scale stuff than I do. We were talking, after last lecture, about how difficult the compilation is, because if you have something wafer-scale, you get wave-interference effects and all sorts of horrible things happening. That's a world I'm not familiar with, but I think it's much more complicated than what we have here. Okay, cool. All right, so now I think we can talk about, in some sense, the finale — we've learned a bunch of things about GPUs, and it would be nice to tie that all together to reproduce, or understand, something I think is really important: flash attention. Flash attention is one of the big

**[1:12:09]** improvements in attention capabilities in a while, and it's all systems — it's going from PyTorch's naive implementation of attention to a single, very cleverly fused kernel. This is from the original paper, but you get this dramatic improvement in the latency of serving these things. And as I showed you, I think two lectures ago, flash attention is a nice, memory-efficient way of computing this, so you can do bigger and bigger attention. A lot of the gains come from improvements in the amount of memory transferred from HBM, the global memory. So what do they actually do? If you read the paper, they say, okay, we just do two things — we apply tiling and recomputation — and we do it in a way that lets

**[1:12:56]** us do things sub-quadratically in the number of memory accesses. I think we understand all of those components now, so let's see what's actually happening. Remember, attention computation has a couple of things: K, Q, V — we do a matrix multiply with the K's and Q's, then a matrix multiply with V at the end, with a softmax in the middle. So those are the operations we need to deal with: three matrix multiplies and one softmax. We know how to do matrix multiplies now — we're going to do a tiled matrix multiply. Figure one from the paper is literally just tiled matrix multiplies — we've seen this before. We take our inputs, our K's and Q's, cut them up, and multiply them chunk by chunk on a tile, and then after we do the

**[1:13:42]** computation, we write them out. Nothing fancy or special here. The tricky thing about flash attention, which you'll see in the assignment, is that there's a big global softmax over the whole thing — we can't do this naively, because the softmax is a global operation that ties everyone together. It connects all of the different tiles together. It might not seem obvious how to tile attention. The key trick — and once you see it, this is really the core idea — is that, normally, to compute a softmax, you'd do it the way shown in this plot: exponentiate all of your elements, subtract the maximum for numerical stability, and then normalize. Standard softmax. Now, there's a

**[1:14:28]** different way of computing this online, and once you realize it, it's kind of simple: as you go, you compute the normalized softmax, and every time you encounter a bigger number than what you've seen before, you swap out the maximum. So you have an online way of computing the softmax as you go — the only tricky part, of course, is the max. We deal with it by keeping track of the max, and swapping it out and correcting for it every time we see a new maximum. Otherwise, this is really just an online running sum of an exponential over everything we've seen. At the end, you divide everything by the accumulator you've computed online. Now, the important thing about the online softmax is that, because it's online and goes block by block, or piece

**[1:15:14]** by piece, we can compute it tile by tile — I don't need to see the rest of the tiles to compute this. I can compute the softmax for my tile, save the partial results, maybe write them back to global memory, and continue on with my day. This lets us break up all the interacting pieces into something simpler. I think this is from flash attention two, or maybe three — I think it's two. I like this visualization, which shows which parts are being tiled: the dashed blocks are tiled in SRAM, and the blue boxes are in high bandwidth memory, the global memory. What you see is, say we do the K-Q inner product — these are tiled matrix multiplies, and they're stored in

**[1:15:59]** SRAM, and we keep them in SRAM and compute this exponential. Similarly, in this tile, we compute this running softmax, then move to the next tile and do the same thing — but now I keep the partial running sums of my softmax as I go, in shared memory or registers or elsewhere. That's a very small number of things to keep track of. At the end, after I've computed all these exponentiated quantities and my normalizers, it's very simple to multiply by V, again in tiled form, and divide at the very end. This lets me take the attention operation, cut it up into small tiled chunks, and go. Finally, you're going to do both of

**[1:16:44]** these together, as much as possible without going back and forth. I'm not going to cover this in detail, but what you can also do, in the backward pass, is recompute everything you need. So the last piece of flash attention is: if you saved your activations, you'd still have this N-squared-sized activation you'd need to save for attention. Instead, throw it all away and recompute everything tile by tile on the backward pass — so it's got the recomputation piece as well. Okay, that gives you flash attention. We've now seen all of the different tricks in action, and that's essentially all there is — a lot of the tricks for efficient implementation, and using GPUs well, boil down to those kinds of ideas. So, to put it all back together:

**[1:17:30]** it's important to understand the hardware. The hardware, in our case the GPU, is powering scale and compute, and we need to understand things all the way down to the low-level details to fully grasp why we make certain decisions. I don't want you to be the kind of people who cargo-cult, say, multiples of 32 for your matrices — I want you to really understand why we do these things, and that goes, in my opinion, all the way down to the hardware level. The other thing is that the way compute is currently growing means we really want to think about matmuls — those are the core operations that are very arithmetically dense. And because of the gap between compute and memory, we need to think very carefully about data movement. And finally, things like flash attention, I think, are

**[1:18:16]** really cool — there's also architecture work that's very hardware-aware. Thinking carefully about the hardware when we do things like architecture design is critical to good performance for future systems. Thanks.
