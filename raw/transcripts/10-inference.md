---
title: "Lecture 10: Inference"
lecture: 10
video: https://www.youtube.com/watch?v=EfM546A79aM
source: copy-edited from the YouTube auto-captions
verbatim_original: not committed - regenerate from the video (see below)
material: ../slides/10-inference.md
---

# Lecture 10: Inference — transcript

**This is the edited transcript.** The auto-captions have been repunctuated,
segmented into sentences, stripped of filler ("um," "uh," "you know," "sort
of," "kind of," "right?") and false starts, and had mis-heard technical terms
restored. No content was added, removed, or reordered, and every `[MM:SS]`
marker is preserved in its original position — content that was under a
marker is still under that marker. The verbatim captions are **not committed
to this repo** — they are a complete reproduction of the lecture and none of
that text is this KB's own work. They stay reproducible on demand: the
`cairn-kb` skill's `fetch_transcript.py` run on the video id in this file's
front matter (`EfM546A79aM`), piped through `transcript_to_md.py`, reproduces
them exactly.

Terminology was cross-checked against
[the lecture's source program](../slides/10-inference.md) (`lecture_10.py`),
which is the authoritative written transcription of this lecture's content —
model/package names, symbols (B, S, T, D, F, N, K, H, L, V), acronyms, and
numbers.

**Restorations made.** These are places where the captions produced a
*wrong word* or dropped/mangled a word, and the text now reads differently.
Each is confirmed against [the slide file](../slides/10-inference.md) unless
the note says otherwise. Grouped by kind:

*The one specific case flagged for this edit, worked through in full:*

| Caption | Restored | Confirmed by |
| --- | --- | --- |
| "GPT-4, which came out earlier this year, was trained on 32 trillion tokens" (1:37) | DeepSeek v4 | slide's "The inference landscape" section: "OpenAI processes ~8.6T tokens per day" / "For reference, DeepSeek v4 was trained on 32T tokens" — the 8.6T figure matches the sentence right before it, so "GPT-4" can only be the caption's mis-hearing of "DeepSeek v4." GPT-4 (2023) also could not plausibly be described as having "come out earlier this year" in a 2026 lecture (the slide dates DeepSeek v4 to 2026), which corroborates the restoration rather than contradicting it. |
| "um you know, at least the GPT-4." (2:23) | at least DeepSeek v4. | same restoration, continuing the sentence begun at 1:37 across the marker boundary — this is the "consequence" instance the case note asked to check for. |

*Inference-serving package/system names (evidence: slide's "Who does inference" and PagedAttention sections):*

| Caption | Restored |
| --- | --- |
| "v l m" | vLLM |
| "s g lang" | SGLang |
| "tensor r t" | TensorRT-LLM |
| "llama c p p" | llama.cpp |
| "This was introduced in the VLM paper... VLM has many other bells and whistles" (~1:19:29) | vLLM paper... vLLM has many other bells and whistles |
| "final idea is page attention" (~1:19:29) | PagedAttention — slide section is literally titled "PagedAttention," and its reference line reads "paper that introduced vLLM in addition to PagedAttention," matching this exact pairing of claims |

*Metrics and memory acronyms:*

| Caption | Restored | Confirmed by |
| --- | --- | --- |
| "t t f t" | TTFT | slide: "Time-to-first-token (TTFT)" |
| "HPM" (6 instances, e.g. 17:02, 26:22 x2, 29:29 x2, 38:47) | HBM | slide uses "HBM" (high-bandwidth memory) throughout and never uses "HPM"; "HPM" is not a real term in this domain |
| "compute bound if... the batch size is greater than 295 for each 100" (31:00) | ...for H100 | slide: "accelerator_intensity = flops_per_second / memory_bandwidth ... Conclusion: compute-bound iff B > 295" is computed specifically for an H100; "each 100" only parses as a mis-hearing of "H100" |
| "this particular mammal operation" (17:48) | matmul operation | the whole passage is deriving arithmetic intensity for a matrix multiply (X·W), and "mammal" has no sensible reading here |
| "bottom neck" (3x, 31:00 x2, 32:32 x1) | bottleneck | plain mis-hearing; "bottleneck" is used correctly elsewhere in this same transcript (e.g. 12:23, 34:03) |

*Quantization terms (evidence: slide's "Activation-aware quantization (AWQ)" section, "fp16 → int3 produces 4x lower memory, 3.2x speedup"):*

| Caption | Restored |
| --- | --- |
| "if you take uh FE16 um weight matrix" (1:06:31) | FP16 |
| "you quantize it down to N3" / "you keep everything else as N3" (1:06:31, 1:07:17) | int3 (both instances) |

*Attention-architecture terms:*

| Caption | Restored | Confirmed by |
| --- | --- | --- |
| "this paper shows that for a JQ JQA" (50:20) | GQA | GQA is spelled correctly everywhere else in this same transcript (e.g. 46:33, 48:05); "JQ JQA" is a stray mis-hearing/stutter of the same acronym |
| "rope" (52:38, 53:24) | RoPE | slide: "MLA is not compatible with RoPE" — casing only |
| "deep seek" (51:06 x2) | DeepSeek | slide and rest of this transcript always render this "DeepSeek" — casing/joining only |
| "the projections of the KQV" (37:15) | QKV | this transcript itself uses "QKV" consistently elsewhere (e.g. 29:29); letter-order transposition |
| "Nvidia" (3:57, 1:07:17) | NVIDIA | slide casing throughout |
| "Let's say you do student naively" (20:06) | Let's say you do this naively | the sentence that follows is a description of "the most naive thing" (repeated auto-regressive decoding), so "student" cannot be the intended word; "this" is the only reading that parses |
| "the n n equals k" (35:43) | N equals K | casing + stutter cleanup; matches slide's Llama-2-13B config, "$K = N = 40$ is multi-head attention" |
| "next class uh Tats will return" (1:24:51) | Tatsu | this transcript's own opening line ("last time Tatsu talked about scaling laws") and lecture 6's transcript both use "Tatsu" for the co-instructor; "Tats" is an isolated caption truncation of the same nickname |
| "multi-latent attention" (51:51, 1:01:49) / "MLA, multi-layered attention" (54:11) | multi-head latent attention (3 instances) | slide section is titled "Multi-head latent attention (MLA)"; "multi-latent" and "multi-layered" are both mis-hearings of the same three-word name, and MLA's own acronym expansion ("M"-"L"-"A") only matches "multi-head latent attention" |
| "if you launch five M copies of the model" (44:10) | if you launch M copies of the model | slide: "Easy parallelism: if you launch M copies of the model, latency is the same, throughput increases by M!" — M is the variable name used throughout this passage (M copies → throughput × M), so "five" is a stray insertion, not a literal count |

*Formatting/casing only (already the right words, not restorations):* "K V cache" → "KV cache" throughout; "int four" → "int4".

*One stray token dropped as noise, not restored:* at 26:22, "the number of flops is uh, you know, depends on P, B, and T, and D and F" — the letter "P" does not correspond to any symbol in this lecture's notation (B, S, T, D, F, N, K, H, L, V per the slide's symbol table), and the lecturer immediately re-lists the same quantity in words right after ("batch size, sequence length, model dimension, and the feed-forward dimension") without a fifth item. Treated as a disfluency and dropped, the same as "um"/"uh", rather than guessed at.

**`[Ed:]` notes inserted, and why:**

- *At ~18:35–20:06*, a student's question about the transpose/heads notation is badly garbled by a bad connection ("I'm having trouble with the connection," plus non-verbal captions like "[clears throat]" and "[snorts]," which are dropped as non-content) and heavy crosstalk with Percy. It is genuinely unclear at several points where the student's words end and Percy's resume — in the same spirit as lecture 6's precedent for garbled crosstalk, it is left close to verbatim inside a single `*[Question from the floor, crosstalk / connection trouble: ...]*` marker rather than split and attributed line-by-line.
- *At 20:06*, within that same exchange, "That means we have two tables" is marked `*[Ed: "tables" as heard — possibly "variables" (they're discussing the symbols K and G), not confirmed by the slide]*`. The slide does not describe this classroom back-and-forth at all (it isn't lecture content, just notation bookkeeping), so there's nothing to check the word against.
- *At 59:31*, "There's like GateNet, DeltaNet, and Mamba" is restored to "There's like GatedDeltaNet and Mamba," flagged inline as `*[Ed: "GateNet, DeltaNet" — likely one mis-split term, GatedDeltaNet]*`, on the strength of the slide's own list two sentences later in the source outline: "Other ideas: linear attention / state-space-models (Mamba 2, GatedDeltaNet)." It remains possible the lecturer separately named "DeltaNet" (a real, distinct architecture) as a third item; the slide does not confirm or rule that out.
- *At 1:08:03*, "this is in some sense a training uh purse weighted um to you know, reduce the KV fit cache" does not parse and is not resolved by the slide (whose corresponding "Model pruning" section ends in a bare `# TODO` and does not narrate this sentence). Left close to verbatim with an inline `*[Ed: unclear]*` flag rather than guessed at.

**Student questions.** Following lecture 6's convention, wherever the recording makes clear someone from the floor is speaking, the transcript marks it `*[Question from the floor: ...]*`, using the captions' own words with only light punctuation. 11 such markers are used (9 openers plus 2 `*[continued from the floor: ...]*` tags), covering **8 distinct exchanges**: two of them — 18:35–20:06 (the transpose/heads exchange) and 1:10:22–1:11:07 (the "neuron always 100" exchange) — are cut across a `[MM:SS]` marker boundary by the recording itself, so each is split into two (or, for the first, three) markers at the same point, exactly as the marker boundary requires.

---

**[0:05]** All right, let's get started. So, last time Tatsu talked about scaling laws, and we're going to take a little bit of a break from that and talk about inference. So, the problem of inference is very simple. You've trained a model, and you're given a prompt, and you want to produce the response, usually as accurately and as quickly as you can. So, inference turns out to be only one lecture, but it's actually of growing importance. And it shows up in many places. So, clearly once you've trained a model, you don't just sit there — if you're a researcher, you put a plot in your paper of here's how my model works,

**[0:50]** but for everyone else, you actually want to use this model. So, what does use look like? It could be chatting with an AI assistant or chatbot. It could be doing code completion. These days, agents are very popular, and that requires inference. Batch data processing as well. It's also used for evaluation — evaluation that requires generation in particular. And it's also used inside training. If you're doing reinforcement learning, you need to have a model, you generate rollouts, you score them, and you update the weights appropriately. So, inference shows up all over the place. And efficiency, as the theme of this

**[1:37]** class, really matters more than ever here. And even if you just look at the actual use — kind of the app vantage point — training is a one-time cost. It could be very expensive. It is very expensive, but once you're done with that, that's it. But inference is a repeated cost. You incur it every single day. So, OpenAI is estimated to produce 8.6 trillion tokens a day. And just for reference, DeepSeek v4, which came out earlier this year, was trained on 32 trillion tokens. So, in less than 4 days, the number of tokens that OpenAI has to produce, and therefore the compute, is

**[2:23]** at least DeepSeek v4. And of course, even the frontier models might be trained on more tokens, but still inference — the number of tokens generated is going to be higher. So, that's just some perspective. And moreover, I think the importance of inference has grown in the last year, because once upon a time we thought of language models primarily as chatbots or assistants, where you put in a prompt, you get back a response, and presumably the goal of the human is to read the response. But now, as we move more into an agentic world, what it looks like is that a query goes in, and the agent is going to do a bunch

**[3:10]** of stuff. It's going to think, it's going to reason, it's going to call some tools, it's going to introspect, and at the end of the day it will produce some output for a human to read. So, most of the tokens that the agent produces are actually not for reading. And so you should really think about the number of tokens generated as really the compute spent. And there's no sort of limit — if you have an ambitious enough problem, you're going to need much more compute and a lot of tokens. Right? So, if you were in the chatbot world, maybe after a certain point inference is fast enough, because humans can only read so fast. But for agents, there's no kind of limit to how much value you can get out

**[3:57]** of squeezing more out of inference. So, a lot of people are doing inference on the commercial side. Of course, all the closed API providers have to serve their models, so inference is a big deal for them. And there is also a bunch of providers serving open-weight models and providing inference as well. And in the open-source community, there's a bunch of packages. So, vLLM is probably a popular one, kind of a go-to. SGLang is another one that's particularly good for agentic workloads, but maybe not as popular yet. And then there's TensorRT-LLM from NVIDIA, which is really fast, but it's more narrow. And then llama.cpp, if you want to

**[4:43]** run inference on your CPU — this is a popular package. Okay. So, hopefully I've argued that inference is huge. If you can make inference twice as fast, or even 10% faster, that is a big deal. So, now the question is what does fast mean? So, there's a few metrics that capture the notion of fast, and they're going to be applicable in different settings and have different trade-offs. So, one metric of fast is time to first token, or TTFT. This is essentially how long a user waits before any generation happens. So, you put a query into ChatGPT, and

**[5:30]** some number of milliseconds passes by, and then, from the point the first token comes in, that is the time. And this is generally useful for interactive applications, because that latency — where you're just waiting and doing nothing — the longer that is, the worse the user experience. And as soon as tokens come in, it doesn't necessarily have to be that fast, because if you're going to read it anyway, you can't read that fast. So, the second metric is latency. And this is from the standpoint of an individual user — how fast are tokens appearing for one query? This is also important for interactive applications. So, basically, this is how fast the

**[6:15]** tokens are streaming through. And then the second, related concept is throughput, measuring tokens per second. This is how fast tokens are appearing for many queries. So, latency — or one over latency — and throughput are clearly very related. Right? In general, many interventions will make both latency and throughput better. But, as we'll see later, there is actually a trade-off here. And throughput is useful if you're doing a bunch of batch processing. Right? You just have, say, a petabyte of data, and you want to process it with a language model. You just want that job to get done. It doesn't matter if one query is coming

**[7:01]** in faster, sooner, or later. Okay. So, what determines the efficiency, by either of these metrics, of inference? So, the high-level bit is as follows. And this is kind of the one high-level bit to remember from the lecture: when you're doing training, you see all the tokens at once, because in supervised fine-tuning you see all the tokens, and you can parallelize over the sequence. Remember, think about in the Transformer, the calculation for attention and MLP — the sequence is just a dimension, so a big tensor that gets multiplied. And so you essentially process all the tokens at once.

**[7:47]** In inference, you can't do that. You have to generate tokens sequentially, one at a time, because of the autoregressive nature of inference. So this is the fundamental problem of why inference is a very different workload than training — because you can't parallelize across the sequence dimension. And therefore, as we'll see later, it's going to be harder to have high arithmetic intensity, or fully utilize the compute. Okay. All right, so let's go on. First, I'm going to just give a preview of what's in this lecture. First, I'm going to do some math to understand the arithmetic intensity and throughput

**[8:34]** and latency — how to think about that for a transformer. And then I'm going to talk about various techniques to reduce the cost, by making the KV cache smaller, quantizing, pruning the model, and then finally I'll talk about speculative decoding, and then some practical concerns. All right, so this is going to be a bit of a review, mostly swapping in notation and making sure we're on the same page. Much of this lecture is based on the scaling book from Google on transformers and inference, so I do recommend that people go check it out — it's very nicely written, and some of the figures are generously

**[9:19]** lifted from that book. Okay. So just to establish a bit of notation to understand this diagram — we're going to use symbols, think about them as knobs, to denote dimensions, but also their length. So, B is going to be the batch dimension and also the number of sequences. T is going to be the sequence dimension, which is also the number of tokens. D is the model dimension. H is the head dimension. Okay, so if I write this — this is taking a product between a tensor with dimensions B, T, and D, and another matrix of D and H, and I produce B, T, H. So, what is going on

**[10:05]** here is that there are some dimensions marked red, which are going to be contracting. These dimensions appear in both operands and disappear from the result. And then there's going to be other regular dimensions, in black, that appear only in one operand and stay in the result. Okay, so this is kind of the general form. Another case is if you have blue dimensions — that means they're batching dimensions, and they appear in both, but stay in the result. They do not get contracted or reduced. Okay, so the interpretation here, as we'll see later, is that we have this tensor for every sequence and token position — you have a vector, and you want to multiply that by

**[10:50]** a matrix. And then here you're basically taking a bunch of dot products between two tensors. Okay, so with that in mind, this, in all its full glory, is the description of a transformer block. It looks a little bit like a circuit diagram. There's a lot of detail here, but I actually find this probably the most crisp definition of what a transformer is. You see a lot of descriptions of transformers, and frankly a lot of them — it's hard to understand what is exactly happening. This one basically tells you exactly the shapes of the tensors and allows you to reason about the dependency. So, for a transformer block, you take the

**[11:36]** X, which is the activations of one layer — you feed it through attention, and then you feed it through the MLP. And just as a quick refresher, the X, you project it using a query matrix, a key matrix, and a value matrix. You'll see here that we have N, which is the number of heads, and H is the dimension of each head. So, the query matrix is batch times sequence times number of heads times the head dimension. And K and V — instead of having N, we have K, which we'll see later, which is a potentially smaller number of key/value heads.

**[12:23]** And then this is the attention operation, where notice that there's batching dimensions — B appears in both, and we're contracting over the head dimension. And we'll come back to why this B being in both makes inference hard, why attention is kind of a bottleneck there. Okay, so just hold on to that thought. And the MLP is very simple and straightforward — you just do some matmuls. This is the gating matrix. This is the up-projection and the down-projection. So, I think you guys have implemented this, so I don't want to belabor it, but

**[13:08]** just to swap in the notation — by convention, we're going to assume that in the MLP, the MLP up-projects the D-dimensional model dimension into four times that, so F is always — think about it as 4D whenever you see F. And then the model dimension gets split across the N heads, so the model dimension is always equal to number of heads times the head dimension. And then the number of heads gets split, in the case of grouped-query attention, into the number of groups and the number of heads per group. And finally, we have two variables, S

**[13:53]** and T, which both represent the sequence dimension. And the difference is that S is going to represent the number of input tokens, and T is going to represent the number of output tokens. So, at training time, these two are the same, because we're predicting all the same inputs as outputs. At inference time, T is going to be one, and S is going to be the input. Okay. So, another review for arithmetic intensity — I talked about this in the second lecture. So, just as a warm-up, suppose I'm multiplying two matrices, a B-by-D matrix and a D-by-F matrix. So intuitively, B is the batch dimension, D is a hidden dimension or the model dimension, and F is the up

**[14:40]** projection dimension in the MLP. So, remember, what is the arithmetic intensity? I have to count flops, and I have to count the amount of memory moved. Okay. So, we can do this as follows. So, what do you have to do to multiply these matrices? Now, remember the systems lectures — you have to read X from HBM. And if you're storing everything in BF16, which is the case for inference, we're going to have 2 * B * D. You're going to read W — that's going to be another 2 * D * F. You're going to do the matmul, so that's — the number of flops is 2 * B * D * F.

**[15:28]** And then you're going to write it back. Okay? So, every time you read and write, it's basically the number of entries — it's kind of a quadratic-like term. And matmuls are cubic. And remember, this is going to be important, because that's how you're going to get high arithmetic intensity. So, the total number of flops is the cubic term, and the bytes transferred is the sum of the reads and writes. Okay. So, remember, arithmetic intensity is how much compute we do per byte transferred, and we want this to be high. So, the intensity is going to be equal to flops over bytes transferred. And

**[16:15]** I'm representing all these things symbolically, because it'll be easier to see and work with rather than numbers. One simplification we can make is to suppose that the batch dimension here is much less than D and F. Then we can simplify this. So, in particular, what this is doing is saying D equals c * B and F equals c * B, and let c go to infinity, so both D and F are really large and B is smaller. And this just reduces to the intensity being B. Okay. So, the upshot of that is — remember, when we did arithmetic intensity for a matmul, it was something like N over

**[17:02]** N over 3. And this is kind of the analog, where instead of just having one variable for dimension, having square matrices, we have non-square matrices. Okay. So, just to compare — the accelerator intensity of the hardware: you look at the flops per second in the spec sheet, you look at how much memory bandwidth, how fast memory gets moved to and from HBM, you divide, and that is the accelerator intensity. And now you compare your computation's intensity with your accelerator intensity. And if it's greater, then you're compute-bound, which is good. If it's less, then you're memory

**[17:48]** bound, and that's bad. So, in this case, for an H100, for this particular matmul operation, you're compute-bound if the batch size is greater than 295. So, here's an extreme case — suppose you just have one example. Okay? What happens — you have one example, then your arithmetic intensity is going to be one. And this is going to be memory-bound. And you can kind of see what's going on here — you're reading this D-by-F matrix, but B is only one, so you're effectively doing only two times D times F flops. So, you know, that's — and this is
**[18:35]** basically kind of the workload that you'll see in inference. You don't get these full matrices — you get these very thin matrices, or tensors. Let me stop there in case there's any questions. Yeah.

*[Question from the floor, crosstalk / connection trouble: In the transpose slide — I just want to confirm, let me say it into the mic. Is that the transpose? Can I ask about the transpose across the board? Should that be across tables instead of heads?]*

I'm having trouble with the connection.

**[19:20]** Let's see. So, the number of groups here — wait, I'm just making sure. So, K is the number of key/value heads, and so you have — sorry, G — G per KV head. So — my mistake —

*[Question from the floor: You treat each key head as having a few queries. Of course, that's why you only have — but when you compute attention, that means G is the number of query heads per key.]*

**[20:06]** *[continued from the floor: That means we have two tables. That's my point.]* *[Ed: "tables" as heard — possibly "variables" — not confirmed by the slide]*

Yeah, okay — I think you're right. So, K should be the number of groups, and G should be the number of heads per one of those groups. Thanks for that, I'll fix that later. Okay. So, let's talk a little bit more about the arithmetic intensity of inference. Okay. So, here's how to think about what's happening in inference. Let's say you do this naively. So, here is a prompt, "Never going to give you." I'm going to feed it to the transformer. And the transformer

**[20:53]** generates keys and values and activations. And at the end of the day, it generates logits over the output vocabulary. And you sample a token. And that token gets concatenated with the prompt, and then you just do this again. You generate "never," you attach that to the prompt, and so on and so forth. Okay, so this is the most naive thing. If you have a black box that takes a sequence and outputs a distribution over tokens, which is exactly what a transformer does, you can just apply this repeatedly. Okay, so this works, but this is really bad, because each time you generate one token you actually take

**[21:40]** order T-squared time, where T is the number of tokens you've generated so far. So generating T tokens is actually T-cubed. Okay? Because the attention is already T-squared, and you have to do that once for every token. So this is pretty bad. But the observation is that you don't actually have to do this. A lot of the work can actually be shared across prefixes. So, for example, if you're generating "never" versus generating "up," you're actually computing a lot of the same key values for "never going to give." So these tokens — they shouldn't change.

**[22:26]** Okay? And this is because it's a causal transformer. If it were bidirectional, then if you attach a token, everything changes. But if it's causal, then the activations here don't change based on any tokens you append. So with that observation, the first obvious thing is to store a KV cache in HBM, so that between successive token generations you can just reuse the KV cache. So that means when you're trying to generate "going to," you don't actually have to recompute all the keys and activations of these previous tokens. So, this is what it looks like with a KV cache. Okay, so

**[23:13]** there's going to be two stages: prefill — you get the prompt and you populate the KV cache, which is the set of key and value pairs that the transformer computes — and then you generate the logits, just the same computations we did before. And now you have the KV cache here, and then this distribution, which you sample a token from. And now you feed that through the transformer, which uses this cache and then produces both the distribution of the next tokens, and a new KV — the activations corresponding to the token "up." And then this gets fed in.

**[23:59]** So, now you have the KV cache, you have these logits, you sample, and then that feeds through the transformer — you output the distribution of the next token as well as the new token, which you add to the KV cache, and so on and so forth. Okay, so the KV cache, formally, is: for every sequence — so there's B of them — for every token — there's S of them — and for every layer and every head, you store an H-dimensional vector. Okay, and just to reiterate, for inference there's prefill — you're given a prompt, you encode the prompt into

**[24:46]** these vectors, and this is parallelizable, just like in training, because you see the entire prompt — you can compute the KV cache. And then in generation, you generate the new response tokens sequentially. But at least you don't have to pay for generating the KV cache of the tokens you already looked at. Okay. So, is everyone comfortable with the KV cache? Any questions about this? All right. So, let's try to figure out the flops and memory IO for both the MLP and attention layers. So, remember, S is the number of

**[25:33]** tokens we're conditioning on, and T is the number of tokens we're generating. So, we're going to do this sort of abstractly, and then later we'll specialize to prefill, where T equals S, and generation, where T equals 1. So, for the MLP layers — and through all of this, I'm just going to look at the matmuls, because the matmuls are the thing that actually requires a lot of work. Everything else isn't that many flops, and can potentially be fused into the matmul too. Okay. So, let's do this calculation. This is the same as the matrix calculations — I'm not going to belabor every single detail here, but the form is: you read X from

**[26:22]** high-bandwidth memory. You read all the parameters. You compute the upward projection, and then you write it to HBM. You compute the gate, and you write it to HBM, and then you compute the down projection of that, and you write it to HBM. Okay, so the number of flops depends on B, T, D, and F — batch size, sequence length, model dimension, and the feed-forward dimension, the MLP dimension. And the bytes

**[27:09]** transferred is this expression. Okay, so now you can compute the arithmetic intensity, which again is flops divided by bytes transferred. So this is some expression. We're going to assume that, just like before, B * T is much smaller than D and F. And with that, we see that the intensity is B * T. Okay, so this is analogous to just the matmul case — there's more matrices and more dimensions, but fundamentally it's the same thing. And that makes sense — an MLP is basically a big matmul. And it also makes sense, because the batch dimension and

**[27:56]** the sequence-length dimension are sort of independent, all the way through the MLP — they don't interact. Attention's a different story. So, B * T — as long as you have, in the prefill, B * T large enough — large batches, long sequences — you'll be fine. Now, let's look at generation. So, remember, generation — there's two problems here. One is that in generation T equals 1. Okay, you're only generating one token at a time. And so that means your arithmetic intensity is going to be B. But B, in generation, is the number of concurrent requests.

**[28:43]** And so, if you're in a batch setting, you can kind of control that. But if you're — let's say, serving a chatbot — the number of concurrent requests is essentially how many concurrent users there are, which can be high or low, so it's a bit unpredictable, it can change over time. That's something we're going to address when we talk about continuous batching. But overall, this is not bad, right? Because as long as you have large batches — the sequence length isn't going to really help you, but if you have large batches you should be good. So, now let's look at the attention layer. So, again, S is the previous tokens already generated, T is the number of tokens you want to generate logits for.

**[29:29]** So, in attention, you read the QKV matrices from HBM. You compute the attention, you compute the softmax, which doesn't really matter, and you compute the value matrix. And then you write the result to HBM. Okay, so the flops is B * S * T * D, and the bytes transferred is this expression. So, everything here is a matmul, right? So this should always be one degree higher of a polynomial than the bytes

**[30:14]** transferred, because it's a matmul. The only question is what does that factor look like. So, that factor for attention is S * T over S + T. Okay? So, let's look at the prefill. So, prefill — what this is saying is that the prefill intensity is S over 2, when T equals S. So this is good, right? Because as long as you have long sequence lengths, for attention, you're going to maintain high arithmetic intensity. Notice that the batching dimension doesn't show up here. I'll explain a bit why

**[31:00]** later. But for generation, this is bad news. Right? So, the generation intensity is S over S plus 1, and that's less than one — or even let's just call it one. And arithmetic intensity, remember, one is bad. We want it to be something like 295, for an H100, to saturate the compute. And so, this is really the bottleneck. So, we did all this analysis for MLP, attention, prefill, generation, and we found this to be a bottleneck. So, let's try to contemplate why this is a bottleneck.
**[31:46]** Unlike MLPs — MLPs for generation was actually okay, as long as your batches are large enough. So, the problem with attention is: let's look at MLPs. Every sequence hits the same MLP weights. Okay? So, these don't depend on B. Whereas in the attention layer, each sequence has its own KV cache. So, these all depend on B. And so, you can think about this as

**[32:32]** what's going on here is that, in the MLP case, having B be big is actually helpful, because you get to load these MLP weights once, and you can use them for all your sequences. Right? That's how you get higher arithmetic intensity — to simplify a bit, you load it once, you do all your batch processing, and that's good. Whereas in attention, you can't — all of these depend on B. So, increasing B doesn't help. It's like, for every sequence, you're basically doing a matmul. So, they're all independent, so

**[33:17]** doing more matmuls isn't helpful. Just like I showed you this example in the very beginning — this also has pretty bad arithmetic intensity. It's not a matmul, because we're essentially batching by a coordinate, and this is basically the same as doing a dot product, which has horrible arithmetic intensity. And remember, in attention, this blue B is the cause of why attention's arithmetic intensity doesn't scale with B, and why that is a bottleneck. Okay, to just summarize here: prefill is compute-bound, generation is

**[34:03]** memory-bound. So, if you look at the MLP intensity for prefill, it's B * S — great. Prefill attention intensity is S over 2 — not as good, but workable. Generation MLP intensity is also workable, requires many concurrent requests. But it's really the generation attention intensity which is a fundamental bottleneck, and that's it — if you're sticking with a transformer, you can't really improve this. Okay, I'll pause there for questions.

**[34:57]** So, now whenever you hear people say "inference is memory-bound," you know why. Okay. So, let's now use these intuitions and calculations to think about our inference metrics — throughput, latency, and also TTFT. All right, so inference is memory-bound. Now, in some ways this simplifies a lot of things, because when we think about how long things take, you just look at how much memory needs to be transferred. Okay? Because, assuming you overlap communication and

**[35:43]** computation, the bottleneck is going to be just the amount of memory that you have to deal with. Okay? So, in some ways it's nice because it's simpler, but in other ways it's frustrating that your accelerators are sitting there not doing anything. Okay. So, let's walk through an example. So, for Llama 2 13B, on an H100 — what is the latency and the throughput? Okay. So, remember, Llama 13B has a particular shape — so, oops — this is a sequence length, the model dimension, feedforward dimension, number of query heads, number of key/value heads. There's no GQA here, so N equals K, head

**[36:28]** dimension, number of layers, vocab size, and the memory bandwidth for the H100. Okay. So, now, using this config, let's compute the transformer performance statistics. Okay, so these are our inputs here. And the statistics I'm going to compute are going to be number of parameters, the memory usage, latency, and throughput. Okay. So, first of all, what costs — what takes

**[37:15]** memory. So, the parameters take memory — we compute the number of parameters. And so, you look at the embeddings, the MLP layers, the projections of the QKV. At the end of the day, you get some number of parameters. Assuming we're doing BF16, which is always the case for inference, then the parameters take up this many bytes. Also in memory is the KV cache. And the KV cache is the number of tokens in your sequence, times the number of

**[38:01]** your heads — KV heads — times the head dimension, times the number of layers. You have one for the key and one for the value, and then you multiply by two again for BF16. Okay, so that's the size of the KV cache. And the total memory usage — this is for each sequence, and you have B sequences, so that's B times that, plus the parameter size, and that's the amount of memory you need. And now, what's the latency? The latency is determined by the memory IO. Okay, because inference is memory-bound — assuming you overlap communication

**[38:47]** and compute — all the memory is going to be shuffling parameters back and forth between HBM and SRAM. So that's how long it takes to move memory around. Throughput is the inverse of latency, but we're generally processing B tokens in parallel, so this is — latency is seconds per token, and throughput is tokens per second, with an additional B, because you're processing a batch of B. Okay, so now let's compute, for this config, what these actual values are.

**[39:33]** Okay, so num_params is 13 billion — good, that's a sanity check, since it's advertised as a 13-billion-parameter model. Memory is this term, which is about 838 million times B — so it's basically a linear function of B, plus some other term. This is the KV cache, which grows with B; this is the parameters, which is double the number of params. Now, latency is just this scaled by the memory bandwidth, so it has the same form as the memory. And throughput is B over that.

**[40:18]** Okay? So, notice that latency grows as you increase B, because the KV cache grows, and in order to process stuff you have to copy the KV cache back and forth. And now, throughput is more interesting, because as you increase B, throughput does improve, but up to a limit. Right? Throughput improves because you're now amortizing the parameter-loading cost over a larger batch — but also, the amount you're processing at once obviously goes up too. So, this sort of asymptotes — throughput can't possibly go to infinity.

**[41:06]** Okay, any questions about this so far? So, basically, you just have to remember: latency is a linear function in B, where the constant is the size of the KV cache, plus the number of parameters. And throughput is proportional to B over B plus something. Okay, so now let's instantiate this for a bunch of situations. So, if you have batch size one, this is what you get — you get a latency of 0.0 — 0.008

**[41:52]** seconds per token, and the throughput is 124 tokens per second. So, what happens if you increase the batch size — you'll see that the latency goes up, but the throughput also goes up. So, latency gets worse, but the throughput improves. So, this is kind of interesting, because we think, oh, we just want to make it go fast — but "fast" actually has two meanings here, and depending on which one you care about, they're completely opposite. Tuning your batch size is really going to determine whether you get latency or throughput. And what happens if we increase it further — let's say we really want high throughput, because we're

**[42:38]** processing lots of documents — let's increase the batch size even more. Okay, so the latency gets even worse, and the throughput gets even better. But the main problem here is that you run out of memory. Okay, because the memory for storing this KV cache is going to exceed your H100 memory. And if you have B200s, you can increase the batch size more, but eventually you'll hit some sort of limit. So, there are limitations to how much you can improve your throughput — you'll never get to the asymptote, because you'll hit the memory limit first. And also, your throughput gains are

**[43:25]** diminishing as well. So, increasing batch size worsens the latency, because now you have a larger KV cache to read and write. And remember, it's batched, so if you're one individual query, you have to wait for everyone else to finish. It's basically like waiting for a bus — the latency is pretty high, you wait, and then you go. Whereas the throughput of a bus is pretty good, because you can move everyone at once. So, throughput improves as batch size increases, because the parameters are shared, and you load them once

**[44:10]** into memory, and you can process a lot of sequences. Okay, so, trade-off between latency and throughput — just to make sure everyone's aligned: smaller batch sizes give better latency but worse throughput; large batch sizes yield better throughput but worse latency. Okay, I'm not going to talk about parallelism too much — there's also another dimension of inference, which is that you can shard your model across multiple devices. You can look at the scaling book chapter on inference if you want to know more. Just as a very trivial example: if you launch M copies of the model, the latency is the same, but the throughput increases by M.

**[44:57]** And then, the other metric we didn't talk about is time to first token. And this is essentially the time it takes to do prefill, because after you finish prefill you can start generating. Okay, so, if you want faster TTFT, you should use smaller batch sizes, and you want larger batch sizes to improve throughput. Okay, hopefully that's clear. Any questions about throughput and latency, and how they're in tension with each other?
**[45:47]** Okay. So, now that we have a conceptual framework for thinking about how efficient inference is, in terms of arithmetic intensity, throughput, and latency — now let's try to make it faster. How do we make inference faster? There's a bunch of different techniques, which are quite varied, ranging from changing the model architecture to doing systems optimization and everything in between. So inference, in some sense, is a fairly rich, cross-cutting topic. Okay, so the most — maybe, now in hindsight, obvious — thing you can think about, and hopefully I've beaten it into your head by now, is that memory

**[46:33]** is the bottleneck for inference, and the KV cache takes up a lot of memory — it could even be larger than the number of parameters, for a large enough batch size. So let's just try to reduce the size of the KV cache. Now, you have to be careful about how you do this, because you want to make sure you don't lose too much accuracy in the process. So, here is one thing you can do, which we already talked about — grouped-query attention. And just as a reminder here: in multi-headed attention, for every token you have a key, a value, and a query. And if you do grouped-query attention, you compute the same number

**[47:19]** of queries, but you have only a smaller number of groups, and you compute key and value for each group. Okay, so K is the number of groups here. And so, in multi-headed attention, K is N — no reduction. There's something called multi-query attention, which no one uses because it's really bad — K equals 1. And somewhere in between is hopefully where we'll find a balance between accuracy and speed. So there's a paper that introduces GQA, from 2023. And they show that if you look at time per sample,

**[48:05]** which is related to both latency and throughput, you see that MHA — multi-headed attention, full attention — has this high time. Whereas, if you start at K equals 1, it's much faster. And then, you can actually keep increasing K, up to K equals 8, and it's still pretty good. And eventually, your time goes up quite a bit. Okay, so why does GQA improve the latency and throughput? Well, it reduces the KV cache by a factor of N over K.

**[48:50]** And, just as a friendly reminder, reducing memory usage leads to speedup, because we're memory-bound. Okay, so let's revisit our friendly Llama model here. Remember, in the initial configuration we're just using K equals N — there's multi-headed attention, no reduction in the number of keys and values. And so, for this one, using a batch size of 64, we get this throughput and latency. Now, if you do GQA, we're going to put in, let's say, a sparsity ratio of 1 to 5.

**[49:35]** That reduces the memory quite a bit, which in turn improves the latency and also improves the throughput. So, it's not that latency and throughput are always at odds — if you reduce the amount of memory, it improves both. It's mainly the batch dimension that is the point of tension. Okay, so this is great. Let's actually increase the batch size even more and see what happens. So, before, if we had a batch size of 256,

**[50:20]** we ran out of memory. But now it fits in memory, and we see that the latency suffers a bit, because we're increasing the batch size, but the throughput goes up proportionally. Okay, so sometimes you play with these parameters jointly — you can reduce the KV cache, but that allows you to increase the batch size, and you can make other trade-offs. So, the final thing you have to do, whenever you make some lossy change, is make sure the accuracy doesn't drop. And this paper shows that, for GQA,

**[51:06]** the time is better, but also, across a bunch of evals, it basically works well. So, now, with these accuracy evals, I think you always have to take it with a grain of salt, because — this is like predicting a model later — the DeepSeek paper would just show that it actually does hurt. So, I guess, take everything that's not just math with a grain of salt here. Okay, so, speaking of DeepSeek, here's another idea to reduce the KV cache. Okay, so the theme is: reduce the KV cache, and latency and throughput improve. So, this is multi-headed attention — same number of queries, keys, and

**[51:51]** values for every token. And GQA, remember, reduced the number of keys and values — sorry, not for every token; we reduced the number of key and value heads. And now, multi-head latent attention, for DeepSeek, says: we're actually going to leave the number of keys and values the same — one for every token, essentially — but I'm going to compress these. So, normally, how do you compute your keys and values? You have your activations, and you multiply them by some matrix to get K, and some other matrix to get V. And these are generally

**[52:38]** N*H dimensions, like your model dim, which is big. So, MLA says: I'm going to project these activations down into C dimensions. So, DeepSeek V2 reduced it from 16,000 to 512 — this is quite aggressive compression. And then, I'm going to compute the K and the V from this compressed representation. So, now I can just store C — this is much smaller. And then, when I need my keys and values, I can just materialize them. So, there's one wrinkle here, which is that MLA is not compatible with RoPE, which operates directly on the keys and values. So, what they do is add additional

**[53:24]** dimensions for handling the RoPE. But more or less, it's still a pretty big reduction. So, the latency and throughput improvements follow by just simple math — the smaller the KV cache, the faster you go. Okay, it's almost linear scaling up until some point. And then, remember, you need to check whether your model is accurate. So, first of all, this is sort of a result that contradicts, or is in tension with, the GQA paper — they show that GQA actually isn't that great.

**[54:11]** It's — it's much — So, this is MHA, this is GQA — these numbers are smaller than these numbers. But they show that their method, MLA, multi-head latent attention, works even a little bit better than MHA. But, let's just say it's about the same. So, this column and this column are much better — I guess there's no GQA on this table, it's — yeah, compare over here. Okay, so I'll show you a— Yeah, question.

*[Question from the floor: How does this compare to reducing the number of dimensions of the model?]*

So, the question is, how does this

**[54:57]** compare with reducing the dimension of the model? That's a good question — these ablations don't show that. My guess is that reducing the model dimension just makes things worse, because you're indiscriminately reducing everything. I think the trick in all of this is to find places in the model where you can squeeze, and a priori I don't think you can necessarily know for sure — you just have to do a bunch of experimentation to see what works. So, here's another idea to reduce your KV cache — this is called cross-layer attention. So, the idea is that normally every layer has its own K's and V's.

**[55:43]** But instead of doing that, we're just going to compute KVs for a subset of the layers, and for the other layers I'm just going to use the previous layer's KV cache. Okay? So, this is another way of sharing — just like GQA shares KVs across heads, now I'm sharing KVs across layers. And empirically, this paper shows that doing this improves the Pareto frontier. So, each of these models is better than — given a method, you can

**[56:29]** always sweep the size of the KV cache by changing K and the head dimension — which, I guess, relates to your point about changing the model dimension. But if you do CLA, cross-layer attention, it's better. Okay, so, moving on — let me give you a whirlwind tour of different techniques for reducing the KV cache. There's local, or sliding-window, attention. This is a fairly old idea, and a very natural one. So, if you look at the full attention matrix, it's N-squared.

**[57:14]** And, instead of doing that, if you're going to generate a token, you just look at the last K tokens. Okay? So, you essentially have a sliding window — for every token you generate, you just depend on the last K. And now, if you do that, the effective — the KV cache is now independent of the sequence length. It's just the batch, times the other variables — which is great, and this is especially great for long context. Now, because of the number of layers, the effective context length is actually larger than the stated context length, because information can

**[58:00]** propagate farther as you go down the layers. Now, you can do fancier things — you don't have to do a dense selection of layers, you can space it out. You can also do this global-plus-sliding-window approach, where you have attention to a fixed grid of token positions, plus a local sliding window. So, you can do various things. Now, the problem with this is that it actually still hurts accuracy — it reduces expressivity. There's no free lunch here — or, at least, this was an expensive lunch. So, the solution people have come up with is to interleave local attention

**[58:46]** with global attention. So, these hybrid models have full attention for some of the layers, and local attention for some of the other layers. And you're always trying to — this allows you to reduce the KV cache a little bit, while balancing accuracy with speed. Yeah?

*[Question from the floor: What's the difference in the trade-off between a linear-attention variant and a sliding window?]*

Yeah, so the question is, what about linear attention versus sliding-window attention. So, I'm not going to talk about linear attention in depth, but maybe very quickly — there's a bunch of

**[59:31]** methods where, instead of storing a KV cache, you compute some sort of compressed representation of all the history. So, the most naive linear attention is: you just sum the KV values up into a single vector. That's definitely independent of the sequence length. You can do fancier things — there's like GatedDeltaNet *[Ed: captions read "GateNet, DeltaNet" — likely one mis-split term for GatedDeltaNet]* and Mamba, which let you try to compress, but not forget, as much. Now, the question is, how do those compare? Those have also been used in place of sliding-window attention, and people have gotten good results with them. You can also use a combination of full attention, sliding

**[1:00:17]** window attention, and linear attention, because they capture different aspects. If you care about local, high-resolution stuff, sliding-window attention is better. If you just want broad summaries of the past, linear attention might be better.

*[Question from the floor: So, for a long context, is that when linear attention would be a better setting?]*

So, the question is: for long context, would linear attention be better? Let me — so, there's no free lunch, right? Let's say you have a very long context, and you're solving a needle-in-a-haystack problem, right?
**[1:01:03]** If you have to compress your entire history into a small context, you're just going to lose information, and you might not be able to retrieve it.

*[Question from the floor: I feel like what it seems like people are going with is hierarchies — they always need some sort of longer attention, but I'm just trying to understand, in my head, what the trade-off between using a sliding window versus some sort of Mamba or DeltaNet layer would be. Is using a DeltaNet layer better than using sliding window consistently, or what are the actual representative trade-offs?]*

Yeah, I guess maybe I'll say that Mamba and DeltaNet are more powerful than sliding-window attention. Maybe you can think about the Mambas —

**[1:01:49]** probably can — it certainly can represent some of the aspects of sliding-window attention, because, as you're doing the recurrence, it can just look at the last state. So, maybe you can think of linear attention, or its extensions, as being better — at least, they have more room. Once you do sliding-window attention, you're done, there's nothing else you can — Okay, so let me just quickly highlight this: DeepSeek. DeepSeek continues to innovate different types of attention mechanisms. So, remember, they came up with multi-head latent attention, which compresses the key values.

**[1:02:36]** There is this thing called — now they have Compressed Sparse Attention, DeepSeek Sparse Attention, and Heavily Compressed Attention. I never quite remember all these acronyms and what they mean, but let's look at this diagram. So, normally you have your KV tokens and your query token. Compressed attention is going to compress every M tokens into one token. And then there's this thing called DeepSeek Sparse Attention, which basically selects a subset of those to keep. And the way you select a subset is that you compute some lighter-weight queries and keys, and then

**[1:03:22]** you do a small attention to get these index scores, so you know what to keep — a lightning-fast way to figure out what tokens you need to keep, and then you use those. And then there's more compression that happens on top. So, in the interest of time, I'll move on. The goal of this section is to reduce the KV cache, because the KV cache is related to memory, and we saw that inference is memory-bound — so that directly translates to improvements in throughput and latency. And the key is to do this without hurting accuracy. So, you can do lower-dimensional KV caches — across layers, across heads, and across

**[1:04:11]** the head dimension. You can do local attention. You can do linear attention, which was discussed earlier. And there's much more — there's also diffusion models, which are a non-autoregressive way to generate, and can be much faster. Okay, so let's talk about a few other ideas, which are important. Quantization is, I'd say, less of an architecture thing and much more of a systems perspective on how to make things smaller. So, the key idea here is just: reduce the precision of numbers. Less memory means better latency and throughput. And, obviously, you have to worry about accuracy. So, quantization —

**[1:04:58]** there are many options here, all the way from BF16 down to int4. So, one thing you can do, if you're worried that quantization is going to mess you up, is train a model with quantization in mind — this is called quantization-aware training. During the forward pass, during training, you quantize and dequantize, and you're basically simulating these quantization errors as you train. So, the weights are adapted toward the quantization, and things work better — but the con is that it requires expensive, large-scale training. And

**[1:05:45]** typically, what people do instead is train models and then quantize after the fact — this is post-training quantization. It's often much cheaper. There's a naive way to do it, where, for every layer or tensor, you determine the scale and the zero point, and then you quantize that separately — this generally doesn't work as well. You can use this idea called GPTQ, which uses Hessian information to quantize layer by layer, and keeps track of the errors, which get

**[1:06:31]** propagated into the non-quantized weights, which lets you correct for the errors. And now, activation-aware quantization is a more sophisticated way, where the observation is that some activation channels are large, and the weights that interact with those matter more — so let's allocate more precision to those weights. So, let's look at this picture. Normally, if you take an FP16 weight matrix and you quantize it, let's say you quantize it down to int3. But what you're going to do is figure out which of these — these are maybe

**[1:07:17]** activation channels — and some of these activation channels are large. So, if they're large in general, you basically allocate, let's say, FP16 for that channel. You keep everything else as int3, and for a few important channels you use higher precision. Okay, so another idea here is model pruning. Here, you just take a large model, rip out pieces of it, and fix it up. It's a very crude way, but it turns out to work. So, there's this paper from NVIDIA, where — essentially, let me actually —

**[1:08:03]** let's see. So, you first have to estimate the importance of the different parts of the model, and choose the most important parts, and then you remove different hidden units, and even different layers, and now we have a model. It's not going to be very good. And so, what you do is post-train it — you train it some more, on the data or the task you care about, to heal it. Okay? So, this is, in some sense, a training *[Ed: unclear — captions read "purse weighted... reduce the KV fit cache"; not confirmable against the slide, whose corresponding section ends in a bare TODO]* process to reduce the model, where you initialize it with parts of a good

**[1:08:50]** model. Okay, so this seems to work pretty well. They were able to take a 15B model and reduce it to an 8B model, and it doesn't really hurt accuracy by too much, and the amount you use to train the model through this process is much less. Okay, so, just to summarize here: the game is to reduce the inference complexity without hurting accuracy. You can think of this as mostly reducing either the number of parameters or the KV cache. You can define a faster model architecture and train it, or you can

**[1:09:36]** define a faster model architecture, initialize the weights from the original model — which might have a different architecture, so you kind of just make this Frankenstein thing — and then repair the faster model with distillation. Okay. Yeah?

*[Question from the floor: How do you distinguish the important layers from the unimportant layers?]*

So, how do you distinguish the important layers from the unimportant layers? In general, you have a calibration set, and you pass the inputs through the model, and you're basically looking at the magnitude of the activations, and the ones that are — some of

**[1:10:22]** them — especially if they're dead units, they'll be close to zero — and the ones that are large, you want to keep. So, that's the high-level idea.

*[Question from the floor: Why does it matter if the activations are high? What if it's just always high for — is that]*

So, in general — the question is, what if all the activations are high? In general, this is an empirical observation that some of the channels will be much higher than others. If this weren't true, these techniques wouldn't necessarily work. But it happens to be true, because that's how these models ended up being trained, and you can exploit that.

*[Question from the floor: Well, I just made up, like, this —]*

**[1:11:07]** *[continued from the floor: say a neuron which is always, like, value 100 for a prompt, across all the samples. Would that mean it's necessarily meaningful, or maybe it's just an artifact of training that I'm — going into?]*

Yeah, I see. So, the question is, what if a neuron is always 100? I mean, if that's the case, you can also look at variance-related questions — if it's 100, you can't just remove it, because then everything's going to be broken. But if it's high mean and low variance, maybe there's another way to just incorporate it as a bias, essentially. Okay, let me quickly go through this other idea. So far, we've looked at lossy methods, which basically really crunch down the KV cache,

**[1:11:52]** but it could hurt accuracy. There's a very elegant way of doing this in a lossless way — this is called speculative sampling, or speculative decoding. So, remember that if you're doing prefill, you can encode all the tokens in parallel, and this also gives you probabilities. This is fast, this is compute-bound, all nice things. And in generation, it's one at a time. Okay, so checking is faster than generation. If I give you a sequence, it's fast to tell you how good it is — much faster than it is to generate one token at a time. Okay, so you can exploit this asymmetry using the following idea. So, what we're going to do is use a cheap draft model to generate from — you guess a few tokens, let's say
**[1:12:39]** four tokens — and then we're going to use the model we actually care about, the target model Q, to review these tokens and accept or reject them. Okay? So, things are chosen to be balanced. The draft model is smaller and cheaper, so even though it's memory-bound and has to generate one at a time, it's not too bad. Whereas the target model is big and expensive, but what we're asking it to do is process a batch of tokens in parallel, so that's not too bad either. Okay, so here's a video that shows how things work. So, if you use a big model token by token, it's going to be pretty slow. But if you're

**[1:13:24]** doing speculative decoding, then you can see the small model generating a bunch of tokens, and then the large model critiquing them — so you get this sort of burst of tokens, and then maybe another burst of tokens, and so on and so forth. Okay, so here's the algorithm — there's a few papers that came out with speculative decoding around the same time; this is one of them. And so, the idea here is that, in order to generate, we're going to

**[1:14:09]** generate K tokens, and we're just going to sample from the draft model — so, Q is a draft, sorry, P is a draft model. We're going to sample K tokens, and then, in parallel, compute the logits of these draft tokens using Q. And now, we have to determine whether we accept them or not, and this is where you do a bit of math, probability and statistics. You're basically going to accept with probability equal to the minimum of one and this ratio of Q over P. So, if Q is much larger than P, the larger Q is, the more likely we are to accept it.

**[1:14:54]** And otherwise, you sample from this residual distribution and exit. Okay? So, this is basically rejection sampling — except, with rejection sampling, sometimes when you reject you get nothing, but here we're always guaranteed to get an exact sample from the target model. I'm going to skip this simple proof — it's basically the same argument as rejection sampling, to show that it gives you exactly the target model's probabilities.

**[1:15:39]** And the initial paper shows that this is fast. And, in general, if you have too few draft tokens, you're not really leveraging the batching on the target-model side. And if you have too many, then you're going to reject more often. So, there's a sweet spot — in this case, around three or four. Okay. And, in general, the draft model is much smaller than the target model. And ideally, you want the draft model to be as close to the target as possible, which means you want to distill it. Which means that, actually, a lot of the same ideas we just talked about are applicable to speculative decoding as well. So,

**[1:16:25]** basically, the idea is: let's try to reduce your KV cache via all the different shenanigans, and if you end up with a model you're happy with, just serve that. If you're not happy with it, then at least it can be a draft model, and you can use your main model to fix things up. There's a whole literature on speculative decoding right now that improves on the original, which I'll skip for now. Okay, so, very quickly now — dynamic workloads. Okay, so the use case here is: you're serving a live website, and users come and chat with your model. The requests arrive at different times.

**[1:17:11]** They have different shared prefixes, and they have different lengths. So, it's pretty messy — it's far from this very simple training setting, where you have these blocks of the same number of tokens all at once. So, what do you do in this case? There's this system called Orca that was built — this is actually very early on — which introduces the idea of continuous batching. So, the idea is that you get a bunch of requests that look like this: here's a prefix of the first request, and you're generating this token; here's the second one; here's the third and fourth. It's jagged, because every prefix has a different length. And what we're going

**[1:17:57]** to do is decode step by step: every step, you decode one token for all the sequences; next step, you decode another token for all the sequences. And when a sequence ends, you just eject it. And as new requests arrive, you put them in the batch and continue. So, that's why it's called continuous batching — your batch is dynamically being updated, with old, finished sequences being evicted and new ones coming in. So, now, one problem here is that everything we've seen so far — batching works when all the sequences have the same dimensionality, right? You have tensors,

**[1:18:43]** and every slice has the same dimensionality. But, if each request here has a different length, what do you do about that? So, there's this idea called selective batching, where — let's say you have a sequence of length three, length nine, and length five. In the attention computation, you can't really do anything about this, because attention depends on the length of your sequence — if you have a 3x3 computation and a 9x9 computation, you can't really share the tensor effectively. But for the non-attention, the MLP layers, which take up a lot of the flops, you can actually just concatenate all the

**[1:19:29]** sequences together to form a mega-sequence, and process that. Okay. So, the final idea is PagedAttention. This was introduced in the vLLM paper — of course, vLLM has many other bells and whistles now, but this was the core idea at the time. So, the question is: how is the KV cache stored? Right? If you think about requests coming in, you have to put them in memory somewhere. And, in general, there's this problem where you get fragmentation — this is what happens, or used to happen, to your hard drive, and you have to defrag your hard drive, back in the day.

**[1:20:15]** So, there's two types of fragmentation. One is that you have to allocate enough buffer, because you don't know a priori when you're going to stop — you might have a max token limit of, say, 1024, so you have to allocate all this memory, and you can't put anything else in there, because you're going to generate until you hit the max tokens, and that's very wasteful. That's internal fragmentation. And then, there's also — there could be space between different requests, and that space is maybe too small to be used effectively, so that's just wasted space. So, the solution here is — these are systems people, so they know their operating systems. They said, "Okay, well, we solved this problem once

**[1:21:00]** before, so let's just use the same idea here." So, we're going to divide the KV cache of a sequence into non-contiguous blocks. Okay, so if you have this sequence, "Four score and seven years ago our fathers brought forth," we're just going to chunk it up into blocks of size four. It doesn't matter where the blocks go, but they'll be aligned according to the block boundaries, so there's some uniformity there. So, when two requests share the same prefix, they can actually share the same KV cache. So, you might have this block and this block — this block might go here

**[1:21:47]** and here, and this block might be over there — they're kind of interspersed, but as long as you have the indices and keep track of where everything is, that's fine. So, in particular, if you have system prompts, you can just cache the KV cache of the system prompt once, and that can be useful for all the queries. This is very useful, because if a lot of people are using the same system prompt, you don't have to compute the KV cache for every request. Also, there are many applications where you have the same prompt and you want to generate multiple responses. In that case,

**[1:22:33]** you can also just share the KV cache for the prompt, and have unique responses coming out. So, for example, if you were to generate multiple generations from "fourscore and seven years ago, our ___," what would happen here is you'd have "fourscore and seven," and then you'd start with, say, "years ago" or "our." This is called copy-on-write semantics — you keep this, and now you have these two samples, and if they happen to sample the same token, you just continue with that. But if they sample different tokens, you split the

**[1:23:18]** block, and continue from there. So, you're basically sharing as much of the prefix cache as you can. There's a bunch of other optimizations — kernels, and so on — that I won't have time to go over, but the general idea is that you're using these operating-systems metaphors to manage your inference. Okay, so, summary here: inference is really, really important. It's very different from training, even though it's the same model — you're asking the model to do something very different. It ends up being very memory-bound, and it's also dynamic, if you're in a live chatbot use

**[1:24:05]** case. We saw a variety of different techniques to improve inference. You can quantize, you can come up with new architectures, you can prune and distill, and you can also use speculative sampling. But all of these are driven by the same principle: reduce your KV cache, but don't hurt accuracy too much. And then there are ideas from systems, like paging and speculative execution, that can be brought to bear for live inference servers. One thing we didn't really get a chance to talk about, which was discussed briefly, is that I think new architectures actually have huge potential for improvement — things like state-space

**[1:24:51]** models, or linear attention, or diffusion. At some level, the KV cache, and the way that attention is built, fundamentally makes it an inference-unfriendly kind of architecture. So, if you can come up with a new architecture that's designed for inference in the way that the Transformer was not, this could maybe unlock a lot. Okay, so I'll stop there. And next class, Tatsu will return and talk about scaling laws, part two.
